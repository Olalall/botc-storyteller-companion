# UI 排版与流程优化方案

- 日期：2026-08-04
- 基准：钟楼百科 429 页快照（`data/wiki-ground-truth/`）+ botc.games 前端产物逆向（Unofficial Digital Grimoire，同领域最成熟工具）
- 方法：5 个界面审计 + 1 个设计系统审计 + botc.games 对标 + 百科主持流程基线，共查出 **109 条问题（high 51 / medium 49 / low 9）**；
  再由三名设计者按不同第一原则各出一版方案（流程连续性 / 魔典视图中心 / 减量抽象），经评审排序后取最优并移植其余两版的可取主张。
- 结构化数据：`data/wiki-ground-truth/compare/ui-analysis.json`

---

## 一、诊断：「乱」的五个根因

1) 相位推进被切成三段跳：夜里点「检查并关闭」→ onExit 回 Dashboard → 在第三屏块找「进入白天」（App.tsx:31-44、NightCloseFooter.tsx:25、Dashboard.tsx:182-193）。而百科流程里真实存在的「黄昏」「黎明」两个交接步骤在工具里全部 missing，于是每次交接都退化成一次导航操作加一次纯记忆负担。
2) 视图按数据表而不是按流程划分：App.tsx:17 的 View = dashboard|night|day|timer 四个互斥全屏，连倒计时都是与工作台并列的第四视图——想把大字倒计时给玩家看必须离开白天工作台，票型草稿只能靠暂存兜底。
3) 上下文不跟随说书人：夜间看不到白天事实（createNextNightRun.ts:183 把 history 置 undefined，回溯型能力全靠脑记）、白天没有「本局记录」入口、Dashboard.tsx:128-145 的「当前阶段」卡在段落关闭后显示的是下一个阶段——三处都在同一个「我在哪、上一步发生了什么」上失灵。
4) 视觉权重与节奏反相关：最大最亮的是全夜不变的「第三夜」、250px 装饰转盘、hero 大字（night-workbench.css:33/642、dashboard.css:1306），而每 30 秒换一次的「当前是谁、下一步做什么」和主确认动作被压到折叠线以下，连续操作变成「滚动—找按钮—确认—滚回」的循环。
5) 护栏与不可逆动作错位：一夜一次的「确认关闭」贴在一夜十几次的「确认并下一位」下方约 20px 且同为金色 primary；离开守卫因 progress 判定错误（NightWorkbench.tsx:82）从不触发；窄屏 role=status 被 night-workbench.css:599 的 display:none 吃掉。节奏最紧的时刻恰恰没有回执，最不该点的按钮恰恰最好点。

### 量化佐证

| 指标 | 现状 | 说明 |
| --- | --- | --- |
| `dashboard.css` | 457 选择器 / 1741 行 / **239 处硬编码颜色** | 有完整 token 系统却大面积不走 token |
| `night-workbench.css` | 772 行 / 109 处硬编码颜色 | 同上 |
| 响应式断点 | **760 / 720 / 560 / 520 / 420 五个混用** | 无统一栅格，各页各自发明 |
| 顶层入口 | Dashboard 挂 **9 个** | 配板/发身份/计时/剧本库/夜/昼/结束/复盘/玩家状态并列 |
| 二级全屏页 | 16 处 `presentation="page"` | 层级深、返回路径不一致 |
| 死代码 | `.dashboard__phase-*` 约 **689 行**（tsx 零引用）| 连带 36 处 `!important` |
| 未定义 token | `--space-2/7/9/22/26/28/30/34/36/44/48` 共 **24 处引用** | 档位漂移，写了等于没写 |

---

## 二、目标信息架构

顶层收敛为 2 个视图 + 1 个交接层，App.tsx 的 View 由 4 个变为 'deck' | 'archive'。

A.「主持台 Deck」——唯一常驻工作视图，占据全部主持时间。
  · 顶部 PhaseTrack（48px sticky）：黄昏 · 夜 · 黎明 · 白天 · 提名投票 · 处决 六节点，当前节点是全屏唯一暖金；节点下一行 12px 显示「第N夜/第N天」。右端两枚常驻入口：「本局记录 N」与「收尾」。
  · 中部「步骤台」：内容随当前节点切换，但容器与滚动位置不变——夜=唤醒卡流（现 CurrentWakeCard），白天=讨论/提名/举手/暂列的单列步骤序列，黄昏/黎明=交接卡。
  · 底部单一 StickyActionBar：固定 [次要动作] [还差提示] [唯一主动作 + 推进开关] 契约，位置永不随节点变化。
  职责唯一：产出并确认本相位的记录，以及在说书人点确认时推进相位。

B.「本局档案 Archive」——只读与更正，覆盖式 page，关闭后精确回到主持台原节点原滚动位。
  收纳现 Dashboard 的非入口部分（玩家状态板、配板、发身份、开场白、切换板子）、本局记录/日记、结束归档与复盘。职责唯一：查与改历史，不承担任何相位推进。

C. 交接层 PhaseHandoff——页内覆盖卡，不是新视图：黄昏交接、黎明播报、白天收尾、局终收尾。它是相位之间唯一的门，也是全部不可逆动作的唯一落点。

切换方式：主持台与档案由 PhaseTrack 右端单一入口互切（档案是覆盖层，主持台不卸载）；相位推进只在主持台内由交接卡的主动作完成，永不经过首页；已完成节点可点开只读回看，顶部持续挂「回看中 · 修改不会前推」条。倒计时不再是视图，降级为白天步骤内的「投屏」全屏遮蔽层。

---

## 三、改造清单（16 项）

### 1. 新增共享组件 src/components/ui/PhaseTrack.tsx + 挂载点 src/app/AppFrame.tsx  `M`

**改什么**：新建 48px sticky 阶段轨道，六节点（黄昏/夜/黎明/白天/提名投票/处决）横排，每节点为 44px 高触控目标，含图标+中文标签+状态（done 冷灰实心 / current 暖金实心 / next 描边 / 回看中加青色下划线）。节点下方一行 --type-meta 显示「第N夜」。右端固定两枚 compact 按钮：「本局记录 N」（N 取 projectEffectiveTimelineEntries 长度）与「收尾」。轨道数据由 session.phaseSegments + activeNightRun 推导，不引入新状态。同时把 night-header（96–160px）、day-workbench__header（约190px）、dashboard hero（150–240px）三套页头压成单行 32px 的「本局名 · 剧本 · 人数」。

**为什么**：说书人抬头 0.5 秒要回答的只有一个问题：现在在哪一步、下一步是什么。目前这个答案分散在三套页头、两个都叫「夜序」的数字和一张会说谎的 hero 卡里。轨道把它压缩成一行且全局位置不变，同时回收约 200–400px 首屏给主任务。

**依据**：百科《规则概要》二/三节的昼夜循环节点；botc.games 顶栏常驻 Town Info（Alive/Dead/Votes/Majority）零交互恒定位置的做法。

```
Before:
[Dashboard]  eyebrow/h1/hero 大卡(240px, 「当前阶段 第3天」可能是错的)
[Night]      night-header 96~160px + 转盘 250px + 通知 38px  → 主任务在 1000px 处
[Day]        header 约190px（h1 与 480px 计时卡平分）
After:
[全局 sticky 48px]  ● 黄昏 — ◉夜 第3夜 — ○ 黎明 — ○ 白天 — ○ 提名投票 — ○ 处决      [本局记录 42] [收尾]
[各页头 32px 单行]  乌鸦渡口 · 12人
[步骤台]            当前唤醒卡（首屏即主任务）
```

**风险**：轨道 sticky 与各 feature 的 @container 断点体系叠加，可能在 620/760 缝隙里与现有页头残留双层吸顶；需先删旧页头再挂轨道，不能并存。

### 2. src/App.tsx（View 类型与三个 enter* 函数）  `L`

**改什么**：View 由 'dashboard'|'night'|'day'|'timer' 收敛为 'deck'|'archive'。新增 deck 状态 { phaseNode: 'dusk'|'night'|'dawn'|'day'|'vote'|'execution' }，由 phaseSegments 推导初值。enterNight/enterDay 不再 setView，改为「打开对应交接卡 → 说书人确认 → dispatch open-phase-segment → setPhaseNode」。archive 以 Sheet presentation="page" 覆盖渲染，deck 保持挂载（滚动位置与草稿全部留存）。删除 'timer' 视图分支与 Dashboard.tsx:172-175 的「公聊倒计时」按钮。

**为什么**：四个互斥全屏是「上下文丢失」的机制性来源：每次切换都卸载组件、丢滚动位、丢正在看的那一行。改成一个不卸载的主持台后，跨相位与查记录都变成覆盖与替换内容，而不是导航。

**依据**：botc.games 的 /play 画布从不卸载，所谓「切换视图」全是覆盖其上的浮层；且它把玩家可见面物理隔离成独立页面，而说书人自己的操作面从不分裂。

```
Before: dashboard ⇄ night ⇄ day ⇄ timer 四个互斥全屏，每次切换整棵子树卸载
  夜结束: [关闭本夜] → onExit() → Dashboard(滚到第三屏) → [进入白天]   （3 次导航 + 1 次找按钮）
After:  deck 常驻 + archive 覆盖
  夜结束: 轨道 [收尾] 或最后一项确认后自动浮出「黎明播报」卡 → [已宣布睁眼 · 进入第3天]   （1 次确认）
```

**风险**：DayWorkbench/NightWorkbench 目前依赖 onExit 卸载来清理局部 state（nominationTarget、pendingResolution、openPanel）；改为常驻后需显式在相位切换时重置，否则会把上一天的 pendingResolution 带进新的一天。

### 3. 新增 src/features/hosting-deck/handoff/DuskHandoff.tsx（步骤台的 dusk 节点）  `M`

**改什么**：页内交接卡，三段只读清单 + 一个主动作：① 上一白天结论回执（处决/无处决、谁死亡、门槛与票数，取自 timeline）；② 黄昏准备清单（本夜为首夜/其他夜、需翻面提示、上一夜「持续至黄昏」的效果到期候选、需要预先想好的三张伪装与错误信息——每条只是勾选备忘，勾选不写任何状态）；③ 本夜队列预览（前 3 项 + 总项数 + 待核对数）。底栏唯一主动作「所有玩家闭眼 · 开始第N夜」，次要动作「回上一天补记」。

**为什么**：黄昏是百科里明确存在、工具里完全缺失的一步，现状是说书人在 Dashboard 上凭记忆完成。把它做成交接卡，既补上流程缺口，又给「白天→夜」这次跳转一个自然的落点，替代掉「返回首页找按钮」。

**依据**：《规则概要》二.1 准备夜晚阶段（翻面夜序表、增删提示标记、预先筹划伪装与错误信息）；《规则概要》三.3 末段「花三十秒左右准备夜晚阶段」。

```
Before: 白天点[结束今天] → 回 Dashboard → 自行回忆要翻夜序表/要想伪装 → 点[进入夜晚] → 直接落在第一张唤醒卡
After:  白天收尾确认后 → 页内浮出
  ┌ 黄昏 · 准备第3夜 ─────────────────┐
  │ 上一天：处决 7 号（已死亡） 4票/门槛4  │
  │ □ 夜序表已翻到「其他夜」            │
  │ □ 到期效果已核对：僧侣保护(第2夜)     │
  │ □ 伪装三张已想好                    │
  │ 本夜队列 9 项 · 待核对 1            │
  │ [回上一天补记]     [闭眼 · 开始第3夜] │
  └──────────────────────────────┘
```

**风险**：清单极易滑向「代办」。必须严格保持勾选项不写 session、不改队列、不生成记录，只做本地 UI 备忘并在离开时丢弃；文案统一用「请核对」而非「已处理」。

### 4. 新增 src/features/hosting-deck/handoff/DawnHandoff.tsx（步骤台的 dawn 节点）  `M`

**改什么**：黎明播报卡：以「黄昏那一刻的生死快照」与「当前 projectCurrentPlayerStates」做差分，用 --type-page-title 级大字列出本夜死亡与复活的座位号（无变化时明确显示「本夜无人死亡，仍要宣布」）。下方一行冷青护栏文案「只报生死，不报原因」。再下方是核对提醒条：「本夜有 N 项确认记录标注了死亡类结果，但玩家状态未更新」，附「去更新状态」跳玩家状态页（仍需说书人手改）。主动作「已宣布睁眼 · 进入第N天」，次要动作「回夜间补记」。倒计时式 5–10 秒缓冲以只读进度条呈现，不阻断按钮。

**为什么**：黎明是全流程里唯一一次说书人必须当众开口播报的信息，也是工具里 missing 的一步。做成差分卡后，说书人不需要在脑内比对「夜里我确认了什么、谁真的死了」，也天然把「夜→白天」这次跳转收进流程内。

**依据**：《规则概要》二.2「等待五到十秒」「宣布这个夜晚死亡和复活的玩家（相对黄昏而言状态变化的才宣布），但不告诉原因」；《夜晚行动顺序一览》「黎明」。

```
Before: 夜里最后一项确认 → [检查并关闭] → 二次确认 → onExit → Dashboard → [进入白天]；死了谁全靠回忆
After:
  ┌ 黎明 · 第3夜结束 ────────────────┐
  │ 死亡   ⑦                          │
  │ 复活   —                          │
  │ 只报生死，不报原因                  │
  │ ⚠ 本夜 2 项记录标注死亡结果，状态未更新 [去更新] │
  │ [回夜间补记]   [已宣布睁眼 · 进入第3天] │
  └──────────────────────────────┘
```

**风险**：差分依赖说书人已在夜间手动改过状态，否则会播报「无人死亡」。因此核对提醒条是必需项而非可选项，且措辞必须是提示不是断言；绝不能由夜间结果自动推死亡。

### 5. src/features/day-workbench/DayWorkbench.tsx 主体版式  `M`

**改什么**：把 day-workbench.css:32 的两栏并排（提名卡 | 举手卡）改为单列「步骤序列」：讨论(计时) → 提名 → 举手 → 暂列 → 处决/无处决。同一时刻只展开一个步骤，其余折成 44px 高摘要条（如「① 提名 · 3号提名7号 ✎」），点摘要条回退编辑。提名与举手座位网格在展开态使用 repeat(auto-fill, minmax(56px,1fr))，保证 ≥52px 触控。步骤内的确认（记录本轮票型 / 记录处决）全部改由底部 StickyActionBar 承载，pendingResolution 确认条改为接管底栏而不是追加在页面尾部。

**为什么**：「步骤 1 / 步骤 2」的编号说先后、并排布局说同时，这个矛盾让说书人视线在两列间来回跳。改成序列后，屏幕上任何时刻只有一件事，且与真实提名投票的时序一一对应；确认条也不会再落到屏幕外让人重复点击。

**依据**：《规则概要》三.2 提名与投票的严格时序（受理提名 → 辩护 → 计票 → 判定 → 下一个提名）；botc.games 的提名/投票是一个居中浮窗里的单一焦点流程，不并排。

```
Before（宽屏两栏，提名卡下方大片空底色；确认条追加在最底部，常在屏外）
  ┌ 步骤1 选择提名 ┐ ┌ 步骤2 记录举手 ┐
  │ [提名人][被提名]│ │ 门槛[4] 座位×12 │
  │ 座位 6 列×50px │ │ 举手3 死票0     │
  └───────────┘ └───────────┘
  … 暂列结果 … （滚动）… 处决确认条（屏外）
After（单列序列，一次一步）
  ① 讨论 · 公聊 03:12               ✎
  ② 提名 · 3号提名7号                ✎
  ③ 举手  ← 展开中：座位网格 56px，举手3 / 门槛4
  ④ 暂列 · —
  ─────────────────────────
  [稍后]        还差：门槛未达      [记录本轮票型]
```

**风险**：折叠会让「我还能不能改上一步」变得不明显；摘要条必须带铅笔图标与可点态，且回退编辑时不清空后续步骤已填内容。

### 6. src/features/day-workbench/components/DayTimer.tsx + PublicChatTimerPage.tsx + App.tsx  `S`

**改什么**：删除独立 timer 视图。DayTimer 增加「投屏」按钮，点击后以全屏遮蔽层（复用 PublicChatTimerPage 内容，presentation=page 覆盖层）显示大字倒计时，层内只有时间、阶段名和一枚「收起」；退出后精确回到白天步骤台的原步骤原滚动位。同时补齐 DayTimer.tsx:53 的空洞：公聊超时后渲染「重新开始公聊 / 回到私聊 / 重置」三选一，与全屏页保持同一套按钮集。

**为什么**：「把大字倒计时转给玩家看」是每局必做动作，现在的代价是整个白天工作台消失、票型草稿只靠暂存兜底——这是最典型的跨视图跳转导致的上下文丢失。改成遮蔽层后，主持台一直在下面活着。

**依据**：《规则概要》三.1 白天讨论；botc.games 把玩家可见面做成独立页面而说书人操作面从不卸载的原则（此处玩家可见面是临时遮蔽层，因为本项目不做玩家常驻端）。

```
Before: 白天工作台 →[返回本局]→ Dashboard →[公聊倒计时]→ 全屏页 →[退出]→ Dashboard →[进入白天]→ 从头找步骤
After:  白天步骤台 → DayTimer 上的 [投屏] → 全屏大字层 → [收起] → 回到原步骤原滚动位
```

**风险**：遮蔽层必须真正遮住秘密信息（不能只是半透明），且退出层的按钮不可与主确认按钮同色同位，避免在把平板拿回手里的那一刻误触确认。

### 7. src/components/ui/StickyActionBar.tsx（升格为全产品唯一底栏契约）  `M`

**改什么**：把 StickyActionBar 固化为三槽组件：slotSecondary / hint / slotPrimary，内置 env(safe-area-inset-bottom)、min-height 76px、统一背景与模糊。夜间底栏按规范改为「确认后：停留 ⇄ 下一位」分段开关 + 唯一主动作「确认本项」，草稿未齐时只显示缺失项与禁用的主动作，不提前占两行。逐一替换手搓底栏：history.css:52 的 .timeline-correction__actions、night-workbench.css:523 的 .role-change-actions（当前无 safe-area）、.night-close-footer、.preview-action-bar、game-end 的中部主动作、setup 的非吸底底部条。

**为什么**：拇指落点必须是全产品恒定的一个位置。现在 7 套底栏意味着每换一页都要重新找主动作，而且两枚只差三个字的金色确认并排——选错的后果是夜序光标悄悄多走一位、直接漏叫一个人。

**依据**：dev-docs/ui-design-system.md:77「底部先选择确认后：停留/下一位，再点击唯一主动作」、:80；botc.games 的底部控制簇位置随布局迁移但始终只有一处。

```
Before（夜间）
  [稍后处理]        [确认本项][确认并下一位]   ← 两枚金/灰确认并排，132/172px
  ── 约 20px ──
  [检查并关闭]（金色 primary，一夜一次的不可逆动作）
After（夜间）
  还差：选目标
  [稍后处理]     确认后： (停留)(下一位)     [确认本项]
  （「结束本夜」已移出页面，改挂轨道右端「收尾」）
```

**风险**：game-end 与 setup 的长表单加吸底栏后可用高度进一步压缩，需同时压掉这两页的巨型 hero（否则窄屏首屏只剩标题）。

### 8. 危险动作出栈：NightCloseFooter.tsx / DayWorkbench.tsx:274-277 / Dashboard.tsx:219-235 / SetupPanel.tsx:303-308 / DayRecordSheet.tsx:104-105  `S`

**改什么**：所有不可逆动作从页面尾部移除，统一改为 PhaseTrack 右端「收尾」入口 → 页内交接卡内的 variant="danger" 按钮；主确认按钮永远是暖金 primary，破坏性动作永远是 danger，二者不同区、不同色、不同排。「放弃草稿」「清空草稿」「结束对局」「历史复盘」拆开：历史复盘移入档案页且不再挂「危险动作」徽标，与结束对局至少隔一个区块。

**为什么**：危险动作与高频动作共用颜色和相邻位置，是暗光下滚动惯性直接导致的当众翻车。改造后误触路径固定为「点收尾 → 读交接卡 → 点红色按钮」，摩擦力与破坏性成正比。

**依据**：dev-docs/ui-design-system.md:113 禁止危险动作紧邻主按钮；botc.games 的 reminder 删除需长按进入 Removal Mode 再点 X 再确认、揭示魔典需二次确认。

```
Before: [确认并下一位](金) ↓20px [检查并关闭](金) → 二次确认里还是[确认关闭](金)
After:  轨道右端 [收尾] → 页内卡「关闭第3夜？还有1项未处理」→ [继续处理](次要) [关闭本夜](danger 红)
```

**风险**：把「结束本夜」藏进收尾菜单后，习惯了旧位置的说书人第一次会找不到；轨道上的「收尾」需要在本相位所有项都已处理时给出冷青提示点。

### 9. src/features/night-workbench/NightWorkbench.tsx:88-104（missingReason 判定链）+ CurrentWakeCard 结果区禁用规则  `S`

**改什么**：把判定顺序改回规范的 目标 → 角色 → 结果：先判 targetCount > draft.targets.length，再判 roleChoices && !roleChoice，最后才判 outcome。同时在目标未选齐时对全部结果按钮加真 disabled（含 requiredInputs 为空、当前会被 outcomeReady 判为 ready 的项，如气球驾驶员的「未受影响」），并在结果区顶部显示一行「先选目标」。

**为什么**：这是流程连续性里最贵的一处断裂：提示把说书人引向唯一可点的错误按钮，一次误按就写下可确认、可进记录的假结果，第二天被质疑时只能当众走更正流程。

**依据**：dev-docs/ui-design-system.md:69「默认使用目标→角色→结果的点击路径」；《规则概要》二.2 唤醒后先确认目标再给信息的手势流程。

```
Before（空草稿进入气球驾驶员）
  还差：选结果
  结果： [信息已给](disabled .34)  [未受影响](可点) ← 唯一能按的就是错的
After
  还差：选目标
  目标： 座位网格（高亮可点）
  结果： 先选目标  [信息已给](disabled) [未受影响](disabled)
```

**风险**：有极少数确实零输入的项（纯信息类）会因此多一步；需按 targetCount===0 豁免，而不是一刀切禁用。

### 10. 新增全局 src/components/ui/HostNotice.tsx（单例）+ 移除 night-workbench.css:599 的 display:none  `S`

**改什么**：在 AppFrame 内 PhaseTrack 正下方放一条全局 role="status" 回执带：常态 0 高度，有回执时展开 32px，3.5 秒后自动收回但文本保留在轨道右端「最近回执」的 tooltip/长按里。窄屏不隐藏、不移出无障碍树，只缩短文案。接管现有全部散落回执：nightWorkbenchReducer 的五条 guard 回执、TimelineHistorySheet 的「更正已追加」（现在写入的同时切走了渲染它的视图）、GameEndSheet 的「归档校验失败」「JSON已导出」（现在只挂在第 2 步 DOM 里）。

**为什么**：操作失败时屏幕毫无变化，是现场最危险的状态：说书人要么反复戳，要么以为记上了直接喊闭眼，整段技能结果丢失且几轮后才被发现。回执必须与相位无关、与滚动位置无关、与屏宽无关。

**依据**：dev-docs/ui-design-system.md:82 状态必须同时用图标、文字与颜色；botc.games 的操作反馈始终在固定的 navbar/toast 位置而非随内容滚动。

```
Before（≤520px）: 点「确认本项」被 guard 挡下 → 按钮没动、无 toast、无震动、屏读也读不到（display:none 摘出无障碍树）
After: 轨道下方
  ⚠ 当前草稿不能确认：还差目标        （3.5s 后收回，可在「最近回执」里回看）
```

**风险**：单例会把不同来源的回执互相覆盖；需要一个短队列（最多 2 条依次播），并对破坏性失败（重置失败）改为常驻不自动收回。

### 11. src/features/night-workbench/state/createNextNightRun.ts + 新增 WakeItem.kind='system' + CurrentWakeCard 的系统步骤形态  `L`

**改什么**：在首夜队列头部插入两张系统步骤卡：「爪牙信息」「恶魔信息」（其他夜不插）。爪牙信息卡：多座位对象（自动列出全部爪牙座位）、要出示的信息标记文案、确认清单「已逐个指认恶魔」；恶魔信息卡：爪牙名单 + 三张不在场善良角色的选择器（从 scriptRoles 减去在场角色，建议两镇民一外来者，仅记录不校验），确认后写一条 night_action 并在档案里可回看。两张卡沿用同一底栏契约。

**为什么**：首夜真正开始的那两步在工具里根本不存在，说书人只能凭记忆完成并事后忘记自己给了哪三张伪装——而这恰恰是后续几夜判断恶魔叙事空间的关键上下文。补上后，首夜的界面顺序才真正等于百科顺序。

**依据**：《规则概要》二.2(1)(2) 爪牙信息与恶魔信息；botc.games 的 bluff sets（可命名多组不在场角色，参与存档序列化）。

```
Before（第1夜队列）: 1/9 洗衣妇 → 2/9 图书管理员 → …（爪牙/恶魔信息只在「官方」参考页里作为只读 phaseMarker 存在）
After: 1/11 爪牙信息（对象：4、9号｜出示「他是恶魔」）→ 2/11 恶魔信息（爪牙4、9｜伪装：占卜师/管家/圣徒）→ 3/11 洗衣妇 → …
```

**风险**：WakeItem 目前绑定单一 seatId，多座位对象需要扩类型，会波及 projectWakeDraft / gameRecordProjection / AI 建议链路；建议先做「无目标的纯记录卡」最小版，多座位指认只做勾选清单不建模。

### 12. src/features/night-workbench/state/createNextNightRun.ts:183 + CurrentWakeCard 新增「白天事实条」  `M`

**改什么**：恢复 WakeItem.history 的投喂，并新增一条只读「今天发生了什么」事实条，仅对回溯型角色（送葬者、杂耍艺人、造谣者、卖花女孩、城镇公告员、提刑官、月之子）的唤醒卡渲染。内容取自 timeline 的当日 execution / vote_round / day_action：今天处决了谁（及其角色，遮蔽态下隐藏）、谁提名过、谁投过票、白天公开宣告清单。样式为冷青单层事实条，位于技能说明与目标区之间，绝不参与结果推导。

**为什么**：回溯型能力是「跨视图跳转」代价最高的一类：现在必须退出夜间 → 开日记 → 翻当天 → 记住 → 关掉 → 找回原位，牌桌上要五六秒。把事实带到需要它的那一步，是流程连续性最直接的兑现。

**依据**：百科《回溯型能力》全文与《夜晚行动顺序一览》其他夜晚的送葬者/杂耍艺人/卖花女孩/城镇公告员/造谣者/提刑官/月之子条目；botc.games 的统一游戏事件历史流按日/夜可回放。

```
Before（送葬者唤醒卡）: 角色能力说明 → 目标座位网格 → 结果按钮   （今天处决了谁？退出去查）
After:  角色能力说明
        ┌ 今天（第3天） ────────────┐
        │ 处决：7号（造成死亡）        │
        │ 提名：3→7、5→2             │
        │ 公开宣告：无                │
        └────────────────────┘
        目标座位网格 → 结果按钮
```

**风险**：事实条会让说书人误以为工具已替他判定（如「今天处决7号=送葬者应看7号角色」）。文案必须是纯客观事实陈述，不出现建议动词；且遮蔽态下必须完全不进 DOM。

### 13. 记录入口归一：PhaseTrack 右端 + 删除 Dashboard.tsx:198-217「最近记录」卡右上角入口、GameReviewPanel「当局日志」、GameEndSheet「本局记录」三处别名  `S`

**改什么**：全产品只保留一个入口，文案固定「本局记录 N」，位置固定在轨道右端，夜/白天/黄昏/黎明/档案共用。打开为覆盖 page，按「第N夜 / 第N天」分组、组内最新在前；关闭回原位。白天工作台由此首次获得记录入口。TimelineHistorySheet 的可更正条目改为「先详情后更正」：点条目进详情，详情页底栏才有「追加更正」，不再点一下直奔可写表单。

**为什么**：被玩家质疑「你昨晚是不是记错了」时需要 3 秒内翻到记录，而现在入口叫四个名字、散在三处、白天根本没有。入口位置恒定是流程连续性的底线之一。

**依据**：dev-docs/ui-design-system.md:92「页头常驻的是『本局记录 N』入口」；botc.games 的 History 是单一入口且回放中的修改不前推。

```
Before: 「日记」(Dashboard 第5块卡右上角, 无N) /「最近记录」(同卡标题) /「本局记录」(GameEndSheet 底部横条) /「当局日志」(复盘页)；白天工作台无入口
After:  轨道右端 [本局记录 42]，六个相位与档案页全程可见，点开分组只读，关闭回原位
```

**风险**：更正入口多一层会让高频更正变慢；对夜间工作台内的「追加更正」保留原路径（它本来就在正确的上下文里），只改档案里的日记列表。

### 14. 焦点色纪律：src/styles/tokens.css + night-workbench.css / dashboard.css / day-workbench.css 的 --accent 用法  `M`

**改什么**：规定 --accent 系列只允许经两个 hook 出现：[data-focus="phase"]（轨道当前节点）与 [data-focus="action"]（当前唯一主动作 / 当前选中项：选中座位、选中 chip、选中结果）。其余现有 12+ 处暖金一律降级——Token 舞台光晕与进度条填充改冷灰、页头 kicker 与夜序数字改 --text-muted、记录数角标与分组标题改冷青、hero 装饰金点删除、Dashboard「发身份」取消镀金。同时把选中态补齐为「勾选 + 描边 + 颜色」三重（复用 SeatButton 的 __check）。

**为什么**：暗光下人眼先抓最亮的暖色块。现在最亮的是不可点的装饰，而「你已经选了3号」只是同族金色之一，导致说书人靠读字而不是扫视确认——这正是读秒催促下漏看选中态的机制。轨道要成为主导航，前提是它是全屏唯一暖金。

**依据**：dev-docs/ui-design-system.md:5「暖金＝当前焦点」、:113 禁止状态仅靠颜色。

```
Before（一屏暖金计数 ≥12）: kicker、夜序数字、进度条+外发光、Token 光晕、正在处理徽标、能力小标、选中座位、选中chip、选中结果、记录角标、Sheet当前项、分组标题、主按钮
After（暖金计数 = 2 类）: 轨道当前节点 ◉夜；当前主动作/当前选中项（且都带勾选与描边）
```

**风险**：大范围改色会让既有截图/文档失效，且 dashboard.css 的 39 处 !important 有 36 处落在死区，改色时容易改到不渲染的选择器上；应先删死区再改色。

### 15. 状态语义统一：CurrentWakeCard.tsx:140-151 座位网格 / DayWorkbench.tsx:226,239 / SeatButton 用法  `S`

**改什么**：夜间目标网格投影 playerStates：死亡座位显示 Skull 角标 + 「亡」字 + 语义色，中毒/醉酒显示对应图标角标；行动者本人不再用 subdued，改为号码左上「本人」文字角标。白天的死亡同样改为图标+文字+颜色三重，subdued 回归纯低强调语义。「标死亡票」药丸从 26px 提到 44px，且改为座位键内的第二行常驻占位（选中才可点），不再一勾就把整行推下去。

**为什么**：白天刚被训练成「虚线=死了」，夜里同一套虚线变成「这是他自己」；恶魔选刀时活人和死人长得一样，是两秒决策窗口里最典型的选错目标场景。语义在昼夜之间必须一致，否则每次相位切换都要重建一次肌肉记忆。

**依据**：dev-design-system.md:30 subdued 不得被业务层当规则限制、:55 死亡须同时用图标文字与语义色；《规则概要》三.3 死亡玩家规则（不能提名、整局仅剩一次投票）。

```
Before  夜间: [3][4][5]  其中 5 号虚线62%透明 = 「这是行动者本人」；死人与活人外观完全相同
        白天: [3][4][5]  其中 5 号虚线62%透明 = 「已死亡」
After   夜间: [3] [4💀亡] [5·本人]   死/毒/醉均带图标+文字
        白天: [3] [4💀亡] [5]        同一套编码，昼夜一致
```

**风险**：夜间目标格投影生死后，说书人可能误以为工具在限制可选目标；必须保持死亡座位仍可点（很多能力可以选死人），只做视觉标注不做禁用。

### 16. 相位回看护栏：PhaseTrack 已完成节点 + 已关闭 phaseSegment 的只读态  `M`

**改什么**：点轨道上已完成节点进入回看：步骤台以只读态渲染该相位内容，顶部常驻一条冷青条「回看第2夜 · 此处修改不会前推，需回当前相位」，底栏只剩一枚「回到第3夜」。回看态下所有写操作入口（确认、记录、结束）隐藏而非禁用；仅保留「追加更正」这一条显式写路径，且必须二次确认。

**为什么**：说书人被质疑时会本能地往回翻，而现在往回翻的唯一方式是离开工作台。给轨道加回看能力等于把「查上一夜」也收进主持台，同时用明确的护栏防止在过去的相位里误写当前记录。

**依据**：botc.games 的「navigating back through days/nights will show the game state at the end of that period. Any changes made will not be carried forward」；《规则概要》三.2「判定一经作出不因之后场上变化而重算」。

```
Before: 想看第2夜 → [返回本局] → 日记 → 找到第2夜分组 → 关闭 → [进入夜晚] → 重新定位
After:  点轨道上灰色的「夜 第2夜」→ 步骤台变只读 + 顶部「回看中 · 修改不会前推」→ [回到第3夜] 一键复位
```

**风险**：只读态与现有 isPreviewing / isReadOnly / isCorrecting 三种态叠加会产生组合爆炸；需要先把这三个布尔收敛成一个显式的 mode 枚举再叠回看态。

---

## 四、分批落地

分三批，每批 2–3 周，批与批之间可独立上线。

第一批「骨架：让相位成为导航」（PhaseTrack、App 视图收敛、底栏统一契约、HostNotice、危险动作出栈、还差顺序修复）
验收标准：
· 从第 1 夜连续主持到第 2 夜，全程不出现 Dashboard，导航次数从 6 次降到 2 次（两张交接卡的主动作）。
· 在 520px 逻辑宽度单手竖握下，nightWorkbenchReducer 的五条 guard 回执 100% 可见且可被屏读读出；display:none 对 role=status 的引用为 0。
· 误触「结束本夜/结束今天/结束对局」的最短路径 ≥ 2 次点击，且终点按钮为 variant="danger"；主确认与破坏性动作的垂直间距 ≥ 96px 或不同屏区。
· 空草稿进入气球驾驶员类项目时，提示为「还差：选目标」，结果区全部按钮不可点。
· iPad 竖屏 820×1180 下，夜间主任务（目标区）首屏可见，确认按钮位于视口底部固定位。

第二批「交接：补上黄昏与黎明」（DuskHandoff、DawnHandoff、白天步骤序列化、倒计时投屏、记录入口归一）
验收标准：
· wikiFlow 中「黄昏准备」「黎明宣布」两条从 missing 变为 partial（有专属界面步骤、有清单/差分、无自动结算）。
· 把倒计时投屏给玩家看再收回，白天票型草稿、已选提名双方、滚动位置零丢失。
· 白天从「受理提名」到「记录处决」全程底栏位置不变，确认条不再出现在视口之外；提名座位键在 iPad 横屏下实测 ≥52px。
· 全产品「本局记录」入口数 = 1，命名一致，白天/夜间/交接卡/档案页均可见并显示条数 N。

第三批「上下文与视觉纪律」（跨相位事实条、首夜爪牙/恶魔信息、焦点色纪律、状态三重表达、相位回看、主干四文件死代码与未定义 token 清理）
验收标准：
· 送葬者/杂耍艺人等七类回溯型角色的唤醒卡上可直接读到当天事实，处理这类角色时的跨视图跳转次数为 0。
· 首夜队列头部出现爪牙信息与恶魔信息两步，伪装三张被记录且可在档案中回看。
· 一屏内 --accent 的语义用途 ≤ 2 类（相位焦点 / 动作焦点）；所有选中态同时具备勾选、描边、颜色。
· dashboard.css / night-workbench.css / day-workbench.css / game-end.css 中未定义 token 引用数为 0，`.dashboard__phase-*` 死区（约 689 行）与其上的 36 处 !important 全部删除。
· 昼夜两个相位的死亡/中毒表达完全一致，subdued 不再承载任何规则含义。

### 第 0 批（先于一切，纯清理）

- 【来自 reduce-first，设为 flow-first 的第 0 批，先于 PhaseTrack 落地】补齐 13 个未定义 token：不新增档位，把 --space-2/7/9/22/26/28/30/34/36/44/48 的 24 处引用就近归并到现有 12 档（tokens.css 实测只定义 4/6/8/10/12/14/16/18/20/24/32/40），--radius-pill 改 999px 或 --radius-xl，script-library.css:54,72 的 var(--text) 改 --text-body；同时删掉 dashboard.css:1438 那条压制 --space-26 塌陷的 !important 症状补丁，否则会双重留白。
- 【来自 reduce-first】删除 night-workbench.css:191-192 的 .night-action-zone > .sticky-action-bar{position:static}，并给 .night-content-grid 加 padding-bottom≈96px——这是确认按钮落到约 1500px 的直接机械成因，且必须靠预留而非遮挡满足 ui-design-system.md:78『不能恢复覆盖内容的假吸底栏』。
- 【来自 reduce-first】下沉 Card 与 Field 两个共享原语，Card 硬性两层上限并在开发态对第三层 outline 红框 + console.error；这是 flow-first 的 PhaseTrack/交接卡/步骤台不变成第 166 个手写卡片的唯一保障。Field 顺带修 setup.css:27 的 40px（低于 44px 触控下限，而配座位恰是开局最密集的输入）。
- 【来自 reduce-first】SeatButton 增加语义属性 dead（虚线 + Skull 图标 + 语义色三重编码），把 subdued 还原为纯低强调；比 flow-first 在业务层加 class 的写法更符合 ui-design-system.md:30『业务层不得把 subdued 当规则限制』。同时坚守：显示生死只是显示，不 disable 座位、不改适用性、不动夜序光标。
- 【来自 reduce-first】40 枚自造 999px 药丸徽标统一到 StatusBadge（现已 48 处使用，能力够用，剩下的是历史遗留）、7 套空态收敛为 .ui-empty；并特别删掉 day-workbench.css:40 那条把 .day-card__heading span 整体染青、压过 StatusBadge--warning 特异度的父选择器，否则『暂列/未达门槛』替换后仍然同色。
- 【来自 reduce-first】死代码清理与验证手法：dashboard.css 中 tsx 命中数为 0 的 .dashboard__phase-* 约 689 行连同落在其中的 36 处 !important 一并删除（实测 dashboard.css 共 39 处 !important），night-workbench.css 的 V3/V3.1 叠层合并回基础层并把窄屏转盘按原始设计意图取 196px；删除前后用各断点 getComputedStyle 快照做逐字节 diff 机械验证。
- 【来自 reduce-first】三条 CI 守门必须与改造同批固化，否则半年后长回来：引用未定义 var 报错、--type-* 之外的硬编码 font-size 与 8/14/22 之外的圆角报警、无 tsx 引用的 CSS 类报警。
- 【来自 reduce-first】--type-page-title 从 clamp(...,4cqw,...) 改为 4vw——它是全系统唯一的响应式字号 token，用 cqw 定义会在四种容器里算出四种字号，这会直接破坏 flow-first 的『页头压成单行 32px』契约。
- 【来自 reduce-first】三处语义修：SetupCandidateBrowser 补一个调用现有 setShowCandidates(false) 的返回出口（当前唯一出口『采用为草稿』会重建 draft、作废全部手动交换且无撤销）；exportArchive 去掉无归档时隐式 saveCurrentGame 的分支，改为 disabled + 『请先保存本局』；resetAcknowledged 与 winner 加入 GameEndSheet 的 open effect 重置列表，避免两步确认因关页面再打开退化成一次点击。

---

## 五、botc.games 对标结论

### 它是什么

## 一、路由层（真正的页面切换只有这几个）
从 bundle 里能确认的路径：`/`（首页/开局向导）、`/draw`（抽牌）、`/play`（魔典主视图）、`/night-order`（夜序表，可打印/扫码）、`/player`（玩家手机端，独立客户端）、`/distribute`（发牌会话）、`/town.html`（**独立页面、独立 bundle**，玩家可见的"城镇视图"）、`/faq`、`/privacy`。
更新日志明确记录了一次架构决策：*"Moved Town outside of the App container to its own page. This removes unused and confusing UI elements that don't do anything, and gives more space to the town view itself."* —— 玩家可见面被**物理隔离成另一个页面**，而不是同一 app 里的一个 tab。

## 二、主流程是线性状态机，不是自由导航
Setup（选剧本 → 选角色 → 定人名/座位）→ Draw（抽牌）→ Play（魔典）→ End Game。
- Setup：三张官方剧本大卡（Trouble Brewing / Bad Moon Rising / Sects and Violets，各带一句难度描述）+ Custom Scripts（URL / 文件 / JSON / botcscripts.com 全库 / 高级按角色检索）。角色数校验用一条 overfill 进度条 + 文案 `select_more_roles:"Select {count} more roles to continue"` / `too_many_roles:"Remove {count} roles to continue"`。
- Draw：四种分发方式并列 —— Bag（点袋子抽）、Tokens（平铺牌面自选）、Mobile（扫码到手机）、Manual/Garden（后台指定，`garden_draw`：预设抽牌顺序，"给玩家随机的错觉"）。
- Play：`gamePhase` 从 `assign` 进入正式对局；顶部有 Day/Night 与轮次计数，可前后翻页回看历史。

## 三、`/play` 内部：**一个画布 + 三层浮层**，没有第二个"主视图"
主视图只有**一个**：`.grimoire-canvas`（黑色贴图背景，夜晚叠 `night-clouds` 云层渐变）。所谓"切换视图"全部是覆盖在它之上的浮层，画布本身从不卸载。

**A. 顶部 navbar（可折叠，"to reclaim some more vertical space"）**
- Game：New / Save / Buy a Grimoire / Send to Clocktracker / Town View / Create Town
- Sheets：Character / Night-Order / Rules（内嵌完整官方开场白与提名规则全文）/ Open in Script Tool
- Settings：Token Size、Reminder Size（两条独立滑杆）、Abilities、Travellers、Fullscreen、Official/Playtest Icons、About
- 右上角常驻 **Town Info 组合件**：`Alive / Travellers / Dead / Votes Remaining / Majority`（`.comp-townsfolk/.comp-outsider/.comp-minion/.comp-demon` 圆点显示阵营构成）

**B. 画布层（绝对定位，三类对象共存）**
1. 玩家 token stack：`.token-stack` → `.grimoire-token-wrapper`。一个座位承载 `roles:[]` 数组 + primary index，多角色时可展开（`.stacked-token.stacked-behind{top:8px;left:8px;opac

### 逐模式判断

| 模式 | 判断 | 理由摘要 |
| --- | --- | --- |
| 核心隐喻：实体魔典的 1:1 数字复刻（而非游戏引擎） | ❌ 不借鉴 | 这是它与本项目的根本分野。它复刻实体魔典是因为要**取代**实体魔典；本项目的前提恰恰相反——说书人手边就有实体魔典，实体物件承担全部空间隐喻。再造一张数字桌面等于让说书人同时维护两套状态，是净负担。本项目的隐喻应该是"庭审书记员的笔录本"，不是"第二块桌面"。 |
| 座位环布局与角度公式（Circle / Ellipse / Rainbow∩ / Smi | 🔶 改造后借鉴 | 座位环本身值得留，但降级为**只读索引条**而非可编辑画布：一圈小圆点，点一下选中玩家并跳到该玩家的记录条目，显示座次/生死/幽灵票。它解决的是"说书人报座位号时我要快速定位"的问题，不是"我要在这里摆牌"。绝不要做拖拽、绝不要做布局形状选择——那是给数字魔典用的，线下说书人的座位形状已经由真实桌子 |
| reminder token 是自由漂浮的独立物件（不挂在角色 token 上） | 🔶 改造后借鉴 | 隐喻要留、实现要换。线下说书人的实体魔典上已经有真实纸片，工具再做一遍空间摆放毫无意义且极易与实物不同步。改成**显式归属的结构化数据**：每位玩家一行，行内是标记 chip 列表（来源角色 + 标记名 + 加上的夜次 + active/inactive）。位置信息完全丢弃。这样 AI 才能读懂"谁 |
| 批量投放当夜提示标记（Add First / Other Night / All Rem | ✅ 直接借鉴 | 这是 AI 顾问最自然的落点，而且是**低风险高价值**的那类落点。把它从"一键铺满"升级成"AI 生成今夜应加标记的草稿清单"：根据剧本 + 当前场上角色 + 已知状态，列出"本夜可能需要记录的标记"，说书人逐条勾选或忽略。这完全符合"AI 只给草稿"——AI 提议，人确认，工具落账。注意保留它的 |
| 提示标记的停用态（deactivated）而非删除 | ✅ 直接借鉴 | 记录台的第一原则是**信息只增不减**。停用而非删除，既保住了历史（AI 复盘时需要知道"这个标记曾经存在过、第几夜失效的"），又给了说书人纠错空间。删除路径故意做得费劲（长按 → 模式 → 点 X → 确认）这个设计取舍也值得抄：破坏性操作的摩擦力应该和它的破坏性成正比。 |
| 夜序画廊：把夜序变成可横滑的逐步执行清单，并与魔典双向联动 | ✅ 直接借鉴 | **全场最值得抄的一个**，而且它天然就是本项目的形状。线下说书人过夜的真实痛点就是"下一个叫谁、叫醒后说什么、忘了给标记"，这正是记录台该解决的。移植时保留：一步一卡、行动文本原样呈现、该步需要的标记就在手边、死亡角色明显淡化、可自定义排序。去掉：拖拽到画布（改成点选玩家）、maximise 联动 |
| 首夜/普通夜切换 + 夜序可拖拽自定义 | ✅ 直接借鉴 | 值得抄，而且抄的理由正是 changelog 里那句 "Enables Alejo snake-charmer" —— 官方夜序在某些边缘情形下需要说书人临场调整，工具必须允许人推翻默认顺序。这就是"不自动结算规则"的具体体现：给默认值，但永远留一个手动覆盖的口子。对本项目尤其重要，因为 AI 给的 |
| 数字键盘式计票面板（不追踪每个人投了谁） | ✅ 直接借鉴 | **第二值得抄的**，而且它的克制正是精髓：连一个完整的数字魔典都没有做"逐人点选投票"，因为线下投票是说书人转一圈肉眼数手，逐人点选反而更慢更容易错。一个 0..N 的大按钮网格 + 三色阈值提示，是对线下动作的精确映射。本项目直接照搬，甚至可以做得更极端：数字键盘做大做满，单手拇指可达。三色染色 |
| 票数门槛的三档计算与展示（majority / to execute / to tie  | ✅ 直接借鉴 | 抄逻辑，但**必须保持它"只显示不裁定"的姿态**。注意它做的事：算出门槛显示给你看，然后由你按数字。它不会因为票数够了就自动把人送上断头台，也不会自动判定处决。这正是本项目要的分寸——AI/工具算门槛（这是纯算术，不是规则裁定），说书人宣布结果。to_tie / to_execute 双阈值同时显 |
| 提名台账：当日提名列表 + 状态色带 + 二次提名警告 | ✅ 直接借鉴 | 直接抄。白天提名投票是线下说书人最容易记乱的环节（谁提名过了、谁被提名过了、当前最高票是谁、谁的幽灵票用掉了），而这些恰恰是纯记录工作、零裁定风险。特别要抄那条"二次提名给警告而不是禁止"——它是本项目态度的完美范例：工具知道规则，但工具知道自己可能不知道全部（Banshee 就是反例），所以只提醒 |
| 统一的游戏事件历史流 + 按阶段回放（且回放中的修改不前推） | ✅ 直接借鉴 | **第三个必抄项，而且对本项目比对它更重要**。对 botc.games 这只是个复盘附加功能；对一个"记录台 + AI 顾问"，这条事件流**就是产品本体**——它既是说书人的笔录，又是喂给 AI 的唯一上下文。AI 能给出多好的建议，完全取决于这条流记得多完整。直接采用它的事件词汇表（死亡/复活 |
| Town Info 组合件：五个数字常驻不动 | ✅ 直接借鉴 | 五个数字、零交互、永远在屏幕上——这是信息密度与认知负荷的最优解。这些恰好是说书人被问得最多、也最容易口算错的量（尤其 Majority 会随死亡实时变化）。本项目应该把它做成顶栏常驻条，且**必须包含 Majority**，因为它是唯一一个需要计算而非计数的量。 |
| Town View：玩家可见面做成独立页面 + 密码 + 默认不揭角色 + 两级揭示保险 | 🔶 改造后借鉴 | 本项目明确不做玩家常驻端，所以整套实时同步的公共屏不要。但其中两个原则要提取出来用：(1) **玩家可见的东西必须在物理上另起一个页面/URL**，而不是同一界面里的一个开关——开关会被误触，页面不会；(2) **揭示是仪式，要加摩擦**（二次确认 + 可选逐个揭示）。落到本项目：做一个**赛后一次 |
| 移动端发牌（QR 扫码 + 玩家自助抽牌 + ready 状态机） | ❌ 不借鉴 | 这是纯粹的"线上游戏平台"能力，与本项目定位正面冲突。线下就是一个布袋子传一圈，物理抽牌的仪式感和信息安全性都比手机强，且不需要每个玩家装/开任何东西。更关键的是：一旦做了玩家端，就必须维护连接状态、断线重连、会话过期——这套复杂度（看 join.* 里那 8 种错误文案就知道）会吃掉本项目全部的工 |
| Spy View：定向出示的洁净视图 | 🔶 改造后借鉴 | 具体功能对本项目意义不大——线下间谍看的是实体魔典，说书人把本子转过去就行。但它揭示的**设计原则必须内化**：工具里的每一屏都要能回答"这一屏如果被玩家瞥见，泄露什么？"。本项目的记录台会有大量比数字魔典更敏感的东西（AI 的推理草稿、说书人给过的假信息记录、下一步计划），所以需要一个**一键遮蔽 |
| Bluff 套组：可命名的多组不在场角色 | ✅ 直接借鉴 | 轻量、零风险、纯记录，且解决一个真实痛点：说书人给恶魔展示了哪三张牌，几轮之后自己会忘，而这直接影响后续判断"恶魔为什么这么说"。多套并存也是必要的（Lunatic、多个小兵各看不同的）。本项目做成一个简单列表即可：套组名 + 三个角色 + 展示给了谁 + 第几夜。这条信息对 AI 顾问也极有价值— |
| Token Stack / Gain Ability：一个座位多张角色牌 | 🔶 改造后借鉴 | 这个数据模型的洞察必须抄，UI 不必抄。血染钟楼的核心难点就是"玩家以为自己是 A，实际是 B"（酒鬼、傀儡、间谍、Lunatic、Legion），说书人记乱这一层就会毁掉整局。但本项目不需要"堆叠 token"这种空间表达，改成每位玩家两个显式字段：**真实角色** 与 **玩家自认角色**，不一 |
| Token 径向菜单：一次触摸抵达全部常用操作 | 🔶 改造后借鉴 | 要抄的是**动作集本身**（这九个动作就是说书人一整局会反复做的全部原子操作，等于一份需求清单白送），以及"按阶段隐藏无关动作"（白天才显示提名）这个上下文感知。不要抄径向布局：它是为触屏单手优化的，本项目若在桌面/平板上以键盘鼠标为主，径向菜单反而慢且不可扫视。改成玩家记录条上的一排图标按钮或右键 |
| Setup 阶段的"警告但不阻断" | ✅ 直接借鉴 | 这套措辞和交互就是本项目"AI 只给草稿、说书人保留裁定权"的现成范本。注意它每一处都是"我告诉你我注意到了什么 + 请你自己复核"，从不阻断、从不代做决定，连不推荐的选项也照样给你开。本项目的 AI 建议应该完全采用这个语气和这个交互形状：黄色提示条 + 具体说明 + 继续按钮永远可点。反面教材是 |
| 双层笔记：全局 Game Notes + 每玩家 Notes | ✅ 直接借鉴 | 自由文本是任何结构化模型都兜不住的兜底层，而说书人恰恰需要记大量非结构化的东西（"这人说话前后矛盾"、"我给洗衣妇的信息是假的"）。双层拆分很对：局级笔记记叙事线，玩家级笔记记个体观察。对本项目还有额外价值——这两处自由文本是 AI 顾问最好的输入之一，也是 AI 输出草稿最自然的落点（AI 建议可 |
| 垂直空间经济学：可折叠顶栏、按布局迁移的控制簇、名字牌下移 | ✅ 直接借鉴 | 这不是功能而是纪律，但它是这个产品成熟度最真实的证据——每一条都是被真实使用磨出来的。本项目的使用场景更极端：说书人在昏暗房间里、一手拿实体魔典、一手操作设备、还要抬头看玩家。所以要比它更狠地执行同样的原则：默认隐藏一切非当前阶段所需的 UI、关键操作永远在拇指可达区、任何文字都要在低光照下可读。它 |
| 存档、跨局持久化与对外导出（Clocktracker） | 🔶 改造后借鉴 | 跨局持久化的玩家名单（Friends）要抄——线下同一群人反复开局，每次重打名字是纯损耗，而且有了稳定的玩家身份，AI 才能积累"这个人上局演得很凶"这类跨局上下文。存档 + 事件历史要抄，那本来就是记录台的产出物。对外导出改成**开放格式导出**（JSON / Markdown 战报）而不是绑定某 |
| 说书人展示卡片系统（Cards）与夜序步骤的关联 | 🔶 改造后借鉴 | 卡片本身不要做——线下说书人手里就是实体卡片，屏幕上再来一套是多余的。但"**夜序步骤 → 本步需要展示的实体卡**"这条关联要保留，做成夜序卡片上的一行文字提示（"本步需出示：这是恶魔 / 这些是你的爪牙"）。这解决的是真实的遗漏问题（首夜给恶魔看爪牙时忘了出示卡片），成本极低。它这套卡片文案清单 |

---

## 六、百科主持流程 vs 工具支持度

钟楼百科定义的一个完整昼夜循环（以《规则概要》二/三节 + 《夜晚行动顺序一览》为主干）：黄昏（宣布所有玩家闭眼并核对 → 翻夜晚顺序表到「首夜/其他夜」正确一面 → 按叶子把今晚要用的提示标记摆到魔典左侧、不用的摆右侧 → 七人及以上首夜在「爪牙信息」「恶魔信息」旁放夜晚标记；有旅行者时在「黄昏」旁放夜晚标记 → 预先想好要给恶魔的三个不在场角色和给醉酒/中毒玩家的错误信息 → 结算在黄昏行动的旅行者/传奇/奇遇角色：官员、窃贼、学徒、咖啡师、流莺、集骨者、公爵夫人）→ 首夜（爪牙信息：同时唤醒全部爪牙、展示「他是恶魔」信息标记、手指恶魔、确认每个爪牙都看清，并把告密者/落难少女等对爪牙暴露的信息一并给出后再让他们闭眼；罂粟种植者/遗忘之门在场则整段跳过，魔术师在场则调整；恶魔信息：唤醒恶魔、展示「他们是你的爪牙」标记并逐个指认爪牙、展示「这些角色不在场」标记并展示三个不在场善良角色（建议两镇民一外来者），国王/提线木偶/疯子/书生等对恶魔暴露的信息一并给出）→ 按夜晚顺序表自上而下逐个唤醒（拍肩/膝两下唤醒 → 玩家指向目标时说书人同步指向以确认 → 用手势给信息：点头/摇头、拇指上下、手指数字、指向玩家、把角色标记拿到玩家30厘米内展示 → 需要时放置该角色的提示标记（夜序表上紫色圆点）→ 用手遮眼示意入睡；玩家用错能力就摇头让他重选；被唤醒前已死亡的角色不再唤醒；死亡即时失去能力并移除其提示标记、加帷幕标记；死亡触发能力与延迟/回溯型能力按夜序位置作为「最晚提示时机」补结算）→ 黎明（等5–10秒 → 宣布「所有玩家睁眼」 → 宣布相对黄昏发生生死变化的玩家：死亡与复活，均不说原因）→ 白天讨论（玩家自由公开/私下交谈，可离座私聊、可私下拜访说书人问规则和自身能力；说书人不参与玩家聊天；处理公开触发能力的公开宣告并配合伪装者；裁定疯狂表现并给奖惩；任何玩家可随时提议流放旅行者，全体举手表决，门槛为全体玩家人数的一半，不算提名也不算投票，能力不能影响，一天可流放任意多次）→ 提名（宣布进入提名并让所有人回座 → 复述「A提名B」以受理 → 校验：同一时间只能有一个提名、只有存活玩家可提名、每人每天只能提名一次且只能被提名一次 → 给被提名者10–30秒辩护）→ 投票（说书人站到圈中央，一手持魔典、一手指向被提名者，喊出他的名字开始计票 → 顺时针缓慢旋转并大声报数，转回被提名者结束，被提名者自己的票最后计 → 活人每天可投任意多次，死人整局只剩一次并在计票后收走投票标记 → 判定：票数≥存活人数一半、且是当天所有被提名者中唯一最高、且至少1票，则进入「即将被处决」；与先前的即将被处决者同票则双方都失去该状态 → 宣布结果，继续询问下一个提名 → 最后走到中央喊「最后一次提名！3…2…1…」并宣布提名阶段结束）→ 处决（处决当前「即将被处决」的玩家，宣布

### 缺口表

| 阶段 | 说书人实际动作 | 支持度 | 缺口 |
| --- | --- | --- | --- |
| 黄昏 | 宣布「所有玩家，闭眼」，必要时绕场核对每个人是否真的闭眼；提醒夜里不得谈论当晚内容、首夜不谈自己角色。 | ❌ 缺失 | 夜间工作台没有黄昏/开夜这一步。createNextNightRun.ts 直接把 activeCursorId 设为 queue[0]（第一个角色），进入 NightWorkbench.tsx 就是第一张唤醒卡。「暮/Dusk」只在 NightQueueSheet 的「官方」参考页里作为 phaseMarker 只读行出现（offici |
| 黄昏 | 把夜晚顺序表翻到首夜/其他夜正确一面；按角色标记上的叶子，把今晚要用的提示标记摆到魔典左侧、不用的摆右侧；七人及以上首夜在「爪牙信息」「恶魔 | ❌ 缺失 | 整个仓库没有提示标记(reminder token)实体。PlayerState 只有 life/poisoned/drunk/markers（playerTypes.ts:18），markers 只有渲染路径（PlayerStatusSheet.tsx:67、PlayerStatusBar.tsx:47、PlayerStatusBoar |
| 黄昏 | 结算在黄昏行动的旅行者/传奇/奇遇角色（首夜：官员、窃贼、学徒、咖啡师；其他夜另加流莺、集骨者、公爵夫人）。 | ❌ 缺失 | 工具完全没有旅行者概念：features/ 下 grep「旅行者/流放/traveller/exile」零命中。createNextNightRun.ts 只从 pack.nightOrders 里匹配已分配角色，旅行者既不能加入座位（无「中途加入旅行者、选阵营、倒置角色标记、放旅行者生命标记、公开宣告」流程），也不会进夜序队列，黄昏槽位 |
| 首夜 | 爪牙信息：同时唤醒所有爪牙让他们互认，展示「他是恶魔」信息标记并用手指认恶魔，确认每个爪牙都看清；若告密者/落难少女等对爪牙暴露的角色在场， | ❌ 缺失 | 「爪牙信息」不是可执行步骤。officialNightOrder.ts 里 minioninfo 只是官方参考页的只读 phaseMarker 行；各剧本 packs/*/night-orders.ts 的 firstNight 条目全是 roleId，没有 minioninfo/demoninfo 系统步骤，所以本局队列（NightQu |
| 首夜 | 恶魔信息：唤醒恶魔，展示「他们是你的爪牙」信息标记并依次指认每名爪牙；再展示「这些角色不在场」信息标记，展示三个不在场的善良角色（建议两镇民 | ❌ 缺失 | 同上，demoninfo 只在官方参考页存在。没有「选三个不在场善良角色作为恶魔伪装」的挑选器（scriptRoles 有全量角色，但没有 in-play/not-in-play 差集视图，也没有把伪装选择写进本夜记录的地方），也没有把疯子/提线木偶等附加信息挂到恶魔信息这一步的机制。复活.wiki/重要细节.wiki 强调「中途变成恶魔 |
| 首夜/其他夜 | 角色行动后按夜序表紫色圆点放置该角色的提示标记（如投毒者「中毒」标记放到目标角色标记旁）；玩家醉酒/中毒时把其提示标记倒置或忽略；玩家死亡时 | ❌ 缺失 | 没有提示标记体系（见上）。只有 dashboard/PlayerStatusSheet.tsx 的中毒/醉酒两个布尔开关，且入口在本局面板而非夜间工作台内联，无法表达「谁因为谁的能力中毒」「标记倒置=暂时失效」「随源头玩家死亡自动失效」。重要细节.wiki 二.3 的连锁规则（投毒者死→目标解毒；A毒B、C毒A→B解毒）在工具里完全靠说书 |
| 首夜/其他夜 | 玩家以错误方式使用能力（僧侣选自己、侍女选死人等）时立即摇头，用手势让他重新选择。 | ❌ 缺失 | CurrentWakeCard.tsx 的座位网格只把自己座位做 subdued 弱化（第146行 subdued={seat === item.seatId}），仍可点选；没有任何合法目标校验（不可选自己/必须选存活/必须与上一晚不同，如驱魔人、魔鬼代言人）、没有提示文案。 |
| 首夜/其他夜 | 死亡即时结算：被杀者立刻失去能力、其持续效果终止、移除其提示标记；行动前已死亡者不再唤醒；死亡触发能力（守鸦人、贤者、心上人、报丧女妖、教授 | 🔶 部分 | 夜间确认不会改变任何玩家状态——CurrentWakeCard.tsx:270 明写「不自动改身份、阵营、死亡、毒醉」，gameSessionAdapter.ts 的 commit 只写 nightRun + records + roleChanges，死亡必须事后到 dashboard/PlayerStatusSheet 手动改。因此「 |
| 首夜/其他夜 | 结算延迟/回溯型能力：在生效节点只判玩家此刻是否醉酒中毒，在回溯节点回看白天发生的客观事实或该玩家当时是否以该角色行动过（送葬者看今天处决对 | ❌ 缺失 | 夜间工作台看不到白天事实。projectGameRecordEntries（gameRecordProjection.ts:38）只投影本 nightRun 的确认记录和角色变更，GameRecordSheet 里没有 vote_round / execution / day_action 条目；WakeItem.history 在 cr |
| 黎明 | 到达黎明后先等5–10秒（防止最后行动的玩家暴露），再宣布「所有玩家，睁眼」。 | ❌ 缺失 | 没有黎明步骤。NightCloseFooter.tsx 只有「检查并关闭」→ close-active-night-run，提示语是「只关闭本夜记录，不会进入白天」，没有等待缓冲、没有睁眼播报，也没有从夜到昼的相位推进（要回 Dashboard 手动点「进入白天」）。 |
| 黎明 | 立刻宣布相对黄昏生死状态发生变化的玩家：黄昏存活而现在死亡的宣布死亡、黄昏死亡而现在存活的宣布复活；不说明原因；无人死亡也要宣布。 | ❌ 缺失 | 没有「黄昏快照 vs 黎明状态」的差分。夜间确认不写死亡（见上），死亡靠 PlayerStatusSheet 单人手改并追加 player_state_changed，Dashboard 只会显示「N号状态已更新」（Dashboard.tsx:36）。缺一个黎明播报页：本夜净死亡/复活名单、无人死亡也要播报、以及「不得说出死因」的护栏。 |
| 白天讨论 | 处理公开触发能力：玩家公开宣告「我是猎手，我现在用能力，我选X」；说书人配合任何宣称者（包括伪装者）假装其发动，因此无效果的公开发动不能自证 | 🔶 部分 | DayRecordSheet.tsx 有「技能」与「公开事件」两类记录，DaySkillRecordFields 能记「宣称角色 claimedRole vs 实际角色 actualRole」和目标、结果（daySkill.ts buildDaySkillContext），这点相当贴合「伪装者也要配合」。但没有公开触发能力的角色清单/触发 |
| 白天讨论 | 裁定疯狂：观察被洗脑师/鹰身女妖标记的玩家、以及畸形秀演员等自带疯狂条件的玩家，判断其是否真的在努力向他人证明；据此给奖励或惩罚（惩罚可以是 | ❌ 缺失 | 疯狂在工具里不是状态。PlayerState 只有 life/poisoned/drunk/markers（playerTypes.ts:18），statusMeta.ts 的 impairmentMeta 只有 poisoned/drunk；PlayerStatusSheet 也只有这两个开关。洗脑师的夜间项能记「要求声称的角色」（ca |
| 白天讨论 | 处理旅行者流放：任何玩家（含死亡玩家）在白天任意时刻可提议流放某旅行者；所有玩家举手表决，说书人统计；门槛是「全体玩家总数的一半」而非存活人 | ❌ 缺失 | 完全没有流放流程，也没有旅行者座位类型。DayWorkbench.tsx 只有唯一一条「提名→举手→暂列→处决/无处决」链路，门槛硬编码为存活人数一半（voteRound.ts executionThresholdForAliveCount = ceil(alive/2)），与流放的「全体人数一半」冲突；死亡票开关（toggleGhost |
| 提名 | 宣布「现在进入提名环节」，确认所有玩家已回到原座；听到提名后复述「小美提名道哥！」表示受理；执行限制：同一时间只能有一个提名未结算、只有存活 | 🔶 部分 | DayWorkbench.tsx 步骤1能选提名人/被提名人并显示「A号提名B号」，死亡座位做 subdued 弱化。但三条硬规则一条都没校验：voteRound.ts 的 canCompleteVoteRound 只查 threshold>0 与双方已选，死亡玩家可被选为提名人、同一人可在同一天反复提名或被反复提名、也没有「上一提名未结 |
| 投票 | 站到圆圈中央，一手持魔典、一手指向被提名者，喊出他的名字开始计票；缓慢顺时针旋转并大声报数，转回被提名者结束；被提名者自己的票最后计；举手即 | 🔶 部分 | 举手记录做得直白：DayWorkbench.tsx 步骤2 的座位网格 toggleRaisedVote，死亡且举手的座位额外出现「标死亡票」按钮（toggleGhostVote），汇总显示举手数/死亡票数/门槛。缺的是流程与账本：没有顺时针唱票光标（无起点=被提名者、无逐座推进、无被提名者最后计）、没有跨轮的死亡票余额（同一死人可在多轮 |
| 提名 | 觉得提名够了就走到场地中间给最后机会：「最后一次提名！3…2…1…」；无人再提名则宣布当天提名阶段结束。 | ❌ 缺失 | 没有「最后一次提名」倒数动作或提名阶段收束的状态。DayWorkbench 里从「暂列结果」直接跳到「记录处决/记录无处决」两个按钮，缺一个显式的提名截止节点（这也是「投票判定一经作出不再重算」在时间轴上的锚点）。 |
| 处决 | 死亡的物理动作：让该玩家把生命标记翻到较暗一面并在其上放一枚投票标记；在魔典中他的角色标记上放帷幕标记；移除他的角色提示标记和夜晚标记；不公 | 🔶 部分 | 死亡本身写得对：confirm-day-execution 追加 player_state_changed（after.life='dead'），PlayerStatusBoard/SessionRail 会显示。但物理配套全缺：没有帷幕标记/提示标记清理（无标记体系）、没有为该玩家发放并跟踪一次性投票标记（死亡票余额，见投票步）、没有「 |
| 处决 | 按优先级结算：1) 处决与死亡同时发生；2) 死者若无处决相关/死亡触发/死后保留能力则立即失能；3) 检查胜负条件并优先结算影响胜负的能力 | 🔶 部分 | 处决只写一条 execution + 可选 player_state_changed，没有五步结算清单：不检查胜负（GameEndSheet 是纯手动结束对局，无「最后恶魔死亡/仅剩两人存活（旅行者不计）」的自动提示）、没有死亡触发/处决关注型能力（送葬者、圣徒、镇长、涡流、主谋、吟游诗人）的提醒钩子。并且与百科的「处决后白天立即结束进入 |
| 黄昏 | 处决结算完毕后花约30秒准备下一夜：增删提示标记与夜晚标记、第一个白天结束时把夜晚顺序表翻到「其他夜」一面；然后宣布白天结束、要求所有玩家闭 | 🔶 部分 | 相位骨架在：phaseSegments + close-open-segment(day) + createNextNightRun 会按 sequence 自动选 first/other 夜序（createNextNightRun.ts:33/57），夜序表翻面这件事是自动的。但没有「白天→黄昏」的交接检查单（要移除/新增哪些标记、哪些 |
| 贯穿全程 | 给任何信息前先判断「登记/被当作」异常：某些玩家会被当作其他角色或阵营（间谍、隐士、陌客、活尸等），说书人据此决定厨师数对、共情者数字、占卜 | ❌ 缺失 | features/ 下 grep「登记/当作/register」几乎零命中（只有 catfishing 样例的一句能力文案）。没有 registerAs 字段：座位只有真实角色（SetupAssignment.role）与 life/poisoned/drunk，说书人给信息时工具算不出也提示不了「这一位今晚应被当作X」。AI 结果建议（ |
| 贯穿全程 | 维护「状态与玩家绑定而非与角色绑定」的六项状态：角色、阵营、生死、醉酒、中毒、疯狂；角色/阵营变化时第一时间秘密告知本人（展示「你是」标记＋ | 🔶 部分 | 角色变更有专门通道：RoleChangeSheet + roleChanges.ts + setup_changed 事件，且会在卡片上提示「本夜仍按{旧角色}」（PlayerStatusBar.tsx:52）、记录里写「当前夜序快照未自动重排」。但六项状态只实现了三项半：阵营（善恶转变，如舞蛇人、异教领袖、食人魔、莽夫）没有独立状态位— |

---

## 七、明确不做

- 不做玩家常驻端、不做实时同步的公共屏、不做手机扫码发牌——本工具只在说书人设备上，玩家可见内容只以临时全屏遮蔽层（倒计时投屏）形式出现。
- 不做数字魔典画布、不做可自由拖拽的提示标记、不做座位环的形状编辑——实体魔典仍是主局面视角，本工具不再造第二张桌面；座位环若出现也只是只读索引。
- 不自动结算规则：不自动改身份/阵营/死亡/毒醉、不自动判胜负、不自动推进昼夜。PhaseTrack 只做导航与「建议下一步」，任何相位推进都必须由说书人在交接卡上点主动作；黄昏/黎明清单只提示不代办。
- 本次不新增旅行者、流放、疯狂、登记（当作/register）等领域模型——它们是真实缺口但属于数据层工程，本方案只在交接卡与状态栏预留插槽，不承诺行为。
- 不做全量 CSS 重写：只清理主干四个文件（dashboard / night-workbench / day-workbench / game-end）中与流程主干相关的死代码与未定义 token；setup、ai-settings、script-library 的样式债留待后续。
- 不引入卡片原语、表单原语、elevation/宽度/断点 token 的完整设计系统重建——这是正确的方向但与本次「流程连续性」主张不同轴，只在本次触及的组件里就近收敛，不做横扫。
- 不改动 AI 的能力边界与提示词：AI 仍只产出可被否决的草稿，本次只调整它在界面上的位置（结果只出现一份、依据只留一行、取消独立 AI 卡片）。

### 评审否决项（含「看起来很酷但违反定位」的提议）

- 【定位重罚】不要把 SeatRing 做成跨昼夜常驻的『全 App 唯一局面视图』。PRODUCT_VISION 明写『不是第二套官方魔典，不复制官方面板的完整空间局面』『以官方/实体魔典为主要空间视角』，ui-design-system.md 也写『角色 PNG 与圆形 Token 只保留给 RoleDisc，避免把座位操作误解为另一套魔典』。即便只读、无 PNG、不可拖，把座位环升为顶层持久视图就等于要求说书人同时维护桌面摆放与屏幕摆放两套局面——这正是定位刻意回避的成本，也是本轮最像『很酷但会把工具变成第二个数字魔典』的提议。座位环若要存在，只能作为单玩家页/记录页里的只读索引，不得常驻、不得成为选人主入口。
- 【定位重罚】不要做可拖拽画布、Circle/Ellipse/Rainbow 布局选择、环上放角色 PNG、环上直接摆提示标记——这些是同一条线更深处，一旦开始就无法回头。
- 不要用『数字键盘计票』取代逐座位举手记录。raisedSeatIds 的逐人留痕与死亡票逐座位标注是记录台的证据链，也是 AI 复盘的输入；为了少点几下而只留总数，是拿本产品的本体换输入速度。双阈值显示可以抄，输入模型不能换。
- 不要让记录页的阶段回看去『重建该阶段末的完整局面状态』。timeline 不足以重建的字段会被投影函数猜出来，而这个猜测会以权威只读态呈现给正被玩家质疑的说书人。只回看已写下的记录条目；状态快照若要做，必须先证明数据充分，无法重建的字段宁可留空。
- 不要整体删除 NightPlayerCarousel。它在 ui-design-system.md 的业务组件清单里，PRODUCT_VISION 也明写夜间借鉴播放器的『当前播放/上一首/下一首/播放列表/进度』认知。正确做法是减重不删除：按 reduce-first 恢复窄屏 196px（当前 :567 的 226px 与 :576 的 196px 都被 :643 的 250px 覆盖）、删掉 ::before 椭圆台面与 ::after 光晕、把上下位预览的裸箭头（42px，低于 44px 且误触即进预览态）换成带标签的可点区域。
- 黎明播报卡绝不能由夜间确认结果自动推断死亡。它只能对『说书人已手改的玩家状态』做黄昏快照差分，并以提示条（不是断言）指出『本夜 N 项记录标注了死亡类结果，但玩家状态未更新』。一旦从技能结果自动推死亡，就是 PRODUCT_VISION 明确排除的自动结算。
- PhaseTrack 不能实现为单一线性相位指针。昼夜记录段合同写明『一个开放的白天段与一个开放的夜晚段可以同时存在，便于说书人按现场情况自由补记』『进入、浏览、预览都不创建记录段，也不自动切换阶段』。轨道必须允许两个段同时为 open、允许在任一已开放段内补记，当前节点只是『建议下一步』的高亮而非锁；交接卡的主动作只推进 UI 焦点，是否 dispatch open-phase-segment 要与现有 App.tsx:37/42 的时机重新对齐（当前一进入就开段，本身就与合同有出入，应顺带收敛为首次权威保存才开段）。
- 黄昏/黎明清单的勾选项一律不得写 session、不得改队列、不得生成记录，离开即丢弃，文案统一用『请核对』而非『已处理』。任何让清单变成待办系统的演进都会滑向代办。
- 不要新增 --space-22/26/34 等档位来『修复』未定义 token。代码里出现过 22/26/34 恰恰证明档位太多而非太少；固化它们就是把漂移写进规范。
- 首夜爪牙信息/恶魔信息第一版不要为『多座位对象』扩 WakeItem 类型。它会波及 projectWakeDraft、gameRecordProjection 与 AI 建议链路，与本轮 UI 改造耦合。第一版做无目标的纯记录卡 + 勾选清单（伪装三张仅记录不校验），多座位建模单独立项。
- 不要做玩家常驻端、公共屏 Town View、扫码移动发牌、实时同步、多人协作说书、跨设备会话——三份方案都正确排除了，此处再确认：任何『把一块屏常驻留给玩家』的形态都出界。倒计时投屏只能是临时全屏遮蔽层，且必须不透明、秘密内容不进 DOM、收起按钮不与主确认同色同位。
- 不要把 timer 继续留作第四个顶层视图，但也不要让 PublicChatTimerPage 顺势发展成玩家端雏形——它降级为白天步骤内的遮蔽层后，功能边界锁死在『大字时间 + 阶段名 + 收起』三件事。
- 不要在早期批次做全量 stylelint 开启与断点全量归并（16→3）。前者会产生数百条报错淹没信号，应逐规则 opt-in、先开零争议的『禁未定义 var』；后者会大面积改变现网断点行为（iPad 竖屏 768 目前在 setup 与 game-end 里落在相反的命中位置），必须排到最后一批并配六宽度逐页截图对照。
- 不要引入 CSS Modules / Tailwind / CSS-in-JS 或组件库，也不要做卡片原语之外的『完整设计系统重建』。本轮用共享原语 + 命名前缀 + CI 守门压住症状即可，模块化迁移单独评估。
- 不要新增旅行者/流放、疯狂状态位、registerAs 登记异常等领域模型。它们是真实的百科覆盖缺口，但属数据层工程，与 UI 改造耦合会让所有低风险承诺失效；交接卡与状态栏可预留插槽，不承诺行为。

---

## 八、方案评审记录

| 视角 | 得分 | 评语 |
| --- | --- | --- |
| flow-first | 8.4 | 最佳。唯一同时覆盖两类 high 的方案：既修了可验证的界面级致命项（missingReason 判定顺序倒置——已在 src/features/night-workbench/NightWorkbench.tsx:89-104 核实，'选结果'确实排在目标/角色判定之前；night-workbench.css:599 的 .night-notice{display:none}；危险动作与主动作同色相邻；StickyActionBar 采用率实测 1/32），又补上了百科流程里真实缺失的黄昏/黎明/爪牙信息/恶魔信 |
| reduce-first | 7.9 | 证据最扎实、风险最低、与设计系统最自洽，但主张不足以单独成立。逐条抽查全部命中：CSS 总行数 6103（dashboard 1741 / night 772 / game-end 1045 / setup 980），!important 43 处（dashboard 39），--space-22/26/34、--radius-pill、var(--text) 确为未定义引用，night-workbench.css:191 的 .night-action-zone > .sticky-action-bar{posi |
| grimoire-first | 6.3 | 零件质量最高，主张本身踩线。核心问题在 thesis：把 SeatRing 做成『全 App 唯一局面视图』且跨昼夜常驻，直接对撞 PRODUCT_VISION『不是第二套官方魔典，不复制官方面板的完整空间局面』『以官方/实体魔典为主要空间视角』，也对撞 ui-design-system.md『角色 PNG 与圆形 Token 只保留给 RoleDisc，避免把座位操作误解为另一套魔典』。方案自己加了很多护栏（只读、无 PNG、不可拖、窄屏退化为 grid），说明作者意识到了这条线，但把它命名为 GrimoireS |

**采纳**：`flow-first`，并移植其余两版共 18 条主张（见第四节第 0 批与改造清单）。
---

## 附录：109 条问题明细

### 夜间工作台 NightWorkbench（/src/features/night-workbench/NightWorkbench.tsx 

<details><summary>界面区块结构（点开）</summary>

```
整页是一个普通竖向滚动页（<main class="night-workbench">，无任何吸底/吸顶元素），从上到下 7 段：

① 页头 .night-header（非 sticky，会随页面滚走；css:14-45 / 633-641）
   宽屏(>860)：一行三栏 [标题区 | 夜序进度 | 操作区]，min-height 96px
     · 标题区：小字"夜间工作台" + h1"第三夜"(29-36px 衬线) + "剧本·N人"
     · 夜序进度：数字 3/12 + 金色进度条 + 12px 小字"已确认N / 暂缓N / 待核对N"
     · 操作区（右对齐）：[返回本局][夜间顺序][本局记录 N]
   ≤860：变两行 —— [标题 | 操作]，第二行整宽放进度条
   ≤520：变三行 —— 标题 / 进度 / 操作；操作区变成等宽三格 [返回][夜序][记录]，按钮文案缩成两字；"剧本·N人"隐藏
   条件显隐：草稿提示条打开时"返回本局"整个消失（tsx:143），三格网格塌成两格，另两个按钮左移

② 离开确认条 LeaveWorkbenchNotice（仅 leavePromptOpen 时；tsx:170）琥珀色整宽条，右侧 [继续处理][返回本局]

③ 系统提示 .night-notice（仅 state.lastNotice 非空且未打开离开确认条时）约 38px 灰条，承载"记录已确认""已暂缓""当前草稿不能确认"等全部反馈。★ ≤520 宽度下被 display:none 直接隐藏（css:599）

④ 角色转盘 .night-carousel（常驻，高 250px；≤520 为 186px）
   宽屏：[上一位缩略卡 164px][当前角色舞台：椭圆3D台面+金色光晕+Token 148-164px+"正在处理"徽标+角色名 26px+"7号玩家"][下一位缩略卡]
   ≤860：邻位卡缩成 76px 圆片、文字隐藏；≤640：邻位卡整体消失，左右换成 ‹ › 大字块
   ≤520：变单列，左右箭头改成 42px 绝对定位的圆形浮标压在 Token 两侧
   点击左右箭头 = 立刻进入"预览模式"，整页下半部分换成只读态

⑤ 主工作卡 .wake-workspace（container: night-content 计算断点）
   >860：两栏卡片 —— 左栏 .wake-facts(0.78fr) / 右栏 .wake-recorder(1.22fr)，顶部一条整宽 .player-state-bar 横跨两栏
   ≤860：塌成单列，顺序 状态栏 → 能力事实 → 记录区
   内部结构：
     · 状态栏：状态徽标（存活/中毒/醉酒/标记/本夜仍按X）+ 右侧 [更换角色]
     · 左栏事实：金色小标"角色能力" + 能力正文 +（条件）需要核对徽标 +（条件）琥珀警示条 +（条件）[适用][不适用] +（条件）折叠的"上次记录"
     · 右栏记录：标题"本项记录"+ 状态徽标（预览/已确认/已暂存）+（条件）[清空重选]
        - 目标 fieldset（targetCount>0 时）：6列座位格（≤520 变4列，按钮 54px）
        - 角色 fieldset（roleChoices 存在时）：圆角 chip 网格，最高 188px 内部滚动
        - 结果 fieldset（常在）：2列结果按钮 54px +（条件）[AI推荐] + 待选择/待确认徽标
        - （条件）"确认后写入"青边预览卡
        - （条件）"辅助判断"青边面板 → 内含 article → 内含"建议结果/建议记录/告知玩家/状态确认/风险提醒"五张小卡
        - （条件）"告知玩家"预览卡 + [展示信息]
   遮蔽态（privacyShielded）：整张卡被 .privacy-curtain 取代（230px：盾牌图标 + "已遮蔽" + 金色主按钮[解除遮蔽]），状态栏/目标/结果全部消失

⑥ 确认区 .night-action-zone（用了 StickyActionBar 组件但被强制 position:static，css:192，即"不吸底"，跟在长卡后面）
   常态：左 [稍后处理/恢复处理/追加更正] 右 [确认本项][确认并下一位]，上方一行 12px 灰字"还差：xxx"
   预览态：整条换成 [回到N号][处理N号]
   ≤520：三行堆叠 hint / 次要 / 主要（主要两键并排）

⑦ 结束页脚 .night-close-footer（常驻，透明度 .82）："第三夜 关闭前检查未处理项" + [检查并关闭] → 点后原地变成琥珀确认块 [取消][确认关闭]（金色主按钮）

浮层：夜间顺序 / 本局记录 / 更换角色 三个 Sheet 均为不透明全屏页；"展示信息" 触发全屏黑底大字 PrivateRevealOverlay 并同时把主页面切到遮蔽态。
```

</details>

**🔴 high · 信息架构｜同一条"结果"在一屏内被复述 3–4 次，说书人分不清哪一份会真正写进记录**

- 证据：CurrentWakeCard.tsx:197-221（结果按钮本身，选中金色 + 10.5px 的"AI建议"小徽标）、:224 <ConfirmDraftPreview>（青边卡"确认后写入"+ 结果全文 + "告知：…"）、:226 <SettlementAssistPanel>、:234-242 .information-preview（再放一次 draft.informationGiven）；SettlementAssistPanel.tsx:57 与 :72 在同一张卡里把 aiOutcomeLabel 写了两遍（一次做 pill、一次做"建议结果"小卡），:74 又出现第三份"告知玩家"。设计规范 ui-design-system.md:74-76 明确写着"'结果'只出现一份""不再增加独立AI卡片""告知玩家不与内部结算结果混排"，SettlementAssistPanel 就是被禁的那张独立 AI 卡。
- 后果：暗光下四块深色面板轮廓相似、字号都在 12-15px，说书人要逐块读完才能确定"我按下确认，写进去的是哪一句"。真实场面是玩家已经睁眼等结果，说书人往往只扫一眼最下面的面板（其实是 AI 建议而非最终草稿），照着念出与实际写入不一致的内容；事后复盘对不上账，只能翻记录重来。

**🔴 high · 状态反馈｜窄屏下所有系统反馈被 display:none 吃掉，操作失败时屏幕毫无变化**

- 证据：night-workbench.css:599 `@container workbench (max-width: 520px){ .night-notice { display: none; } }`；而 .night-notice 是 NightWorkbench.tsx:176 唯一的 role="status" 出口，承载 nightWorkbenchReducer.ts 里的全部回执：'记录已确认，夜间光标未移动'(:117)、'已暂缓本项'(:128)、'当前草稿不能确认'(:96)、'请先完成或清空当前草稿，再更换角色'(:208)、'缺少原确认快照，不能开始更正'(:152)。display:none 同时把它从无障碍树里摘掉，屏读也读不到。
- 后果：单手竖握平板（≤520 逻辑宽度）时，点"确认本项"若被 guard 挡下，界面完全静止：按钮没动、没有 toast、没有震动。说书人会以为自己没点准而反复戳，或者以为已经记上了就直接喊"闭眼"，导致这一夜的技能结果整段丢失，而丢失是在几轮之后才被发现的。

**🔴 high · 交互流程｜"还差：xxx"提示的判定顺序与规定的 目标→角色→结果 相反，会把人引向唯一可点的错误按钮**

- 证据：NightWorkbench.tsx:96 把 `!draft.outcomeId && readyOutcomes.length > 0 → '选结果'` 放在了 :98 的目标判定和 :100 的角色判定之前；readyOutcomes 由 projectWakeDraft.ts:48-53 计算，requiredInputs 为空的选项在零输入时就算 ready。数据里确有这种项：catfishingOtherNightQueueLate.ts:120-142 气球驾驶员（progress:'pending'、targetCount:1、'未受影响' requiredInputs:[]）。空草稿进入该项时提示写着"还差：选结果"，而结果区里 '信息已给' 因缺目标处于 disabled(opacity .34, css:265)，唯一能按的就是 '未受影响'——按下即生成"本夜能力未影响目标"并可直接确认。规范 ui-design-system.md:69 要求默认路径是 目标→角色→结果。
- 后果：气球驾驶员这类"必给信息"的角色，一次误按就写下"未受影响"的假记录，且这条记录是可确认、进本局记录的。线下节奏快、说书人照提示行事，等到第二天玩家质疑"我昨晚没收到信息"时，记录已经串了，只能靠追加更正补锅，当场信誉受损。

**🔴 high · 排版布局｜首屏 400+px 让给展示性转盘，真正的主任务（选目标/选结果/确认）永远在折叠线以下**

- 证据：night-workbench.css:634 页头 min-height 96px（≤860 变两行约 160px）+ css:642-643 .night-carousel min-height:250px（含 ::before 3D 椭圆台面 css:649-657、::after 光晕 css:97-106）+ css:47-62 通知条 38px；确认区被显式取消吸底（css:191-192 `.night-action-zone > .sticky-action-bar { position: static }`），排在 CurrentWakeCard.tsx:137-242 全部条件面板之后。iPad 竖屏 820×1180 实测排布下，结果区大约落在 1000-1100px、确认按钮约在 1500px，需要整整上滑一屏。转盘承载的信息（角色名、座位）在 ④ 下方的状态栏和记录卡里各自又出现了一次。
- 后果：每处理一位角色都要"上滑找按钮 → 确认 → 回滚看下一位"，一夜 12-18 项就是几十次盲滑。单手托平板、另一只手在做手势时，拇指够不到屏幕下半区，只能把平板换手或放桌上，动作暴露在围观玩家视野里；而屏幕最显眼的位置常年是一张不参与决策的角色图。

**🔴 high · 排版布局｜不可逆的"结束本夜"按钮常驻在主确认按钮下方约 20px 处，且用与主动作相同的金色 primary**

- 证据：night-workbench.css:696 确认条 padding 收到 var(--space-10)，紧接 css:698-702 `.night-close-footer { margin-top: var(--space-4); padding-block: var(--space-10); opacity:.82 }`——即 [确认并下一位] 与 [检查并关闭] 之间只有约 14-24px 垂直间隙；NightCloseFooter.tsx:25 的最终动作 `<Button variant="primary">确认关闭</Button>` 与 NightActionBar.tsx:69 的 `<Button variant="primary">确认并下一位</Button>` 是同一枚金色按钮（ui.css:24）。对照 DayWorkbench.tsx:267 的同类破坏性动作用的是 `variant="danger"`。规范 ui-design-system.md:113 明令禁止"危险动作紧邻主按钮"。
- 后果：这个动作 dispatch close-active-night-run 之后直接 onExit()，一夜只该用一次。它却贴在一夜要按十几次的按钮正下方，颜色语言还完全一致。暗光下滚动惯性多走 20px，拇指落点就从"下一位"变成"关闭本夜"，二次确认弹的还是同一枚金色按钮，惯性再点一次就退出工作台，被围观的玩家会直接看到主持流程崩掉。

**🔴 high · 状态反馈｜"未完成草稿已保留"的离开守卫实际永远不触发，返回键在窄屏还与两个无害入口等宽同排**

- 证据：NightWorkbench.tsx:82 `hasInProgressDraft = state.queue.some(item => item.progress === 'draft') || Boolean(state.correctionItemId)`，但 progress 被写成 'draft' 的地方只有 nightWorkbenchReducer.ts:145（resume 分支）；正常的 target/role-choice/outcome（reducer:67-89）只写 state.drafts、并不改 item.progress。所以选完目标和结果后点 tsx:143 的"返回本局"会直接 onExit()，规范 ui-design-system.md:79 要求的页内确认条 LeaveWorkbenchNotice 从不出现。对照 DayWorkbench.tsx:179 用的是真实的 hasUnrecordedVote/hasUnrecordedDayAction。此外 css:739-748 把 ≤520 的页头做成等宽三格，"返回"和"夜序/记录"同尺寸同色同排。
- 后果：草稿其实存在 session 里没丢，但说书人得不到任何"保留了"的承诺。半夜误触 1/3 宽的"返回"直接跳回本局页，人在众目睽睽下会本能地认为刚才的记录没了，于是重新走一遍目标-结果流程，很容易写出重复记录；而重复记录又只能靠"追加更正"清理，越修越乱。

**🔴 high · 视觉层级｜暖金被 12 种不同含义同时占用，"当前焦点"这个信号失效**

- 证据：同一屏上使用 --accent 系列的有：页头 kicker(css:31)、夜序数字(css:40)、进度条填充+外发光(css:44)、Token 舞台光晕(css:166-167)、"正在处理"徽标(ui.css:45)、左栏"角色能力"小标(css:221)、选中座位(ui.css:71)、选中 chip(css:260)、选中结果(css:266)、记录数角标(css:493)、夜序 Sheet 当前项(css:483)、记录分组标题(css:497)，加上金色主按钮(ui.css:24)。ui-design-system.md:5 规定"暖金＝当前焦点"，只应有一个。
- 后果：低照度下人眼先抓最亮的暖色块。现在最亮的往往是装饰性的 Token 光晕和进度条，而真正需要注意的"你已经选了 3 号"和"这个结果被选中了"只是同一族金色里的一员。说书人要靠读字而不是扫视来确认自己选了什么，在读秒催促下极易漏看选中态，出现"以为选了其实没选"。

**🔴 high · 一致性｜座位号卡的 subdued 在夜间表示"自己"、在白天表示"死亡"，夜间目标格还完全不显示生死**

- 证据：CurrentWakeCard.tsx:146 `subdued={seat === item.seatId}`（虚线+62% 透明 = 行动者本人）；DayWorkbench.tsx:226/239 `subdued={playerStates[seatId]?.life === 'dead'}`（同样的虚线+62% 透明 = 死人，day-workbench.css:56 再加一层 dashed）。两处共用 ui.css:72 的同一套样式。夜间 seat-grid（CurrentWakeCard.tsx:140-151）只渲染号码，不投影任何 playerStates，只有行动者自己的状态出现在 PlayerStatusBar 上。规范 ui-design-system.md:30 也写明 subdued 不得被业务层当规则限制用。
- 后果：说书人白天刚被训练成"虚线=死了"，夜里同一个键盘上虚线变成"这是他自己"。恶魔选刀、投毒者选人时，屏幕上活人和死人长得一模一样，得靠脑内记忆或翻别的页面核对；在被十几个人盯着的两秒决策窗口里，这是最典型的选错目标场景，而选错目标写进记录后必须走更正流程当众暴露。

**🟡 medium · 视觉层级｜标题层级与实际重要性颠倒：夜次是 h1 且最大，当前角色只是 strong，"本项记录"反而被压小**

- 证据：night-workbench.css:33 `.night-header__title h1 { font: 900 clamp(1.8rem,3cqw,2.25rem) }`（最大，内容是"第三夜"，全夜不变）；css:183 当前角色名是 `.carousel-current strong` clamp(1.45-1.65rem)，没有任何 heading 语义；css:241 的 `.section-heading h2` 又在 css:675 被改小成 clamp(1.16rem,1.8cqw,1.32rem)。整页只有 1 个 h1 + 1 个 h2。同时 tokens.css:8-9 的 --type-section / --type-page-title 在本页一次都没用（对比 dashboard.css:10、day-workbench.css:41 都在用）。
- 后果：扫一眼屏幕，最抢眼的是一个全夜都不会变、完全无需提醒的"第三夜"，而每 30 秒就要换一次的"当前是谁、在做哪一项"却排在第二、第三级。说书人切换角色时缺少一个大而稳定的锚点，容易在被玩家打断后回到屏幕时找错行——尤其在预览态和正常态之间。

**🟡 medium · 交互流程｜底栏两个"确认"并排且常驻，与规范的"唯一主动作 + 停留/下一位分段"相反**

- 证据：NightActionBar.tsx:66-70 无论草稿是否齐备，都同时渲染 [确认本项](secondary) 和 [确认并下一位](primary)，未齐备时两枚一起 disabled；对照 ui-design-system.md:77"底部先选择'确认后：停留 / 下一位'，再点击唯一主动作'确认本项'"和 :80"记录可确认后才展开'停留 / 下一位'，避免底栏提前占用两行"。css:533-534 还给它们分别锁死 min-width 132/172px，≤520 时（css:612）两枚并排挤在一行 0.9fr/1.12fr。更正态下文案又变成 [确认更正]/[更正并下一位]（NightActionBar.tsx:68-69），四种"确认"文案共存。
- 后果："确认本项"和"确认并下一位"只差三个字、并排同宽，暗光下要读完才能分辨；选错的后果不是报错而是夜序光标悄悄多走一位，说书人接着按着屏幕上的角色去叫醒玩家，就跳过了一个人。等发现漏叫时，中间几项的记录顺序已经乱了。

**🟡 medium · 信息密度｜卡片套到第 4 层，违反最多两层的硬规定，深色面板层层叠加后边界读不出来**

- 证据：层级链：.wake-workspace（css:208-218，1px 边框 + radius-xl + 大投影）→ .settlement-assist（css:277-286，青边 + radius-lg + 径向渐变）→ .settlement-assist article（css:308-315，再一层边框 + radius-md）→ .settlement-assist__draft-card（css:351-359，第四层边框 + radius-md）。此外同一张 .wake-workspace 里 css:676-681 又给每个 fieldset 加了边框和底色（V3 追加层），形成第二条三层链。ui-design-system.md:111 明确"卡片套卡片超过两层"属于禁止项。
- 后果：暗环境里这些边框都是 7%-24% 白，相邻两层的明度差不到一档，视觉上糊成一团灰。说书人无法一眼判断"风险提醒"属于 AI 建议还是属于本项记录本身，只能靠读小字标题；被玩家催的时候干脆整块跳过不看，AI 给出的"状态确认/风险提醒"就等于白做。

**🟡 medium · 交互流程｜窄屏 42px 无标签箭头压在 Token 上，一碰就把整页切进预览模式**

- 证据：night-workbench.css:755-767：≤520 时 .carousel-hit 变成 `width: 42px; min-height: 82px; position: absolute; left/right: 10px` 浮在 Token 两侧，内容只有 css:605-606 的 '‹' '›' 伪元素字符（图标在 css:568 已被 display:none）。42px < ui-design-system.md:10 规定的 44px 触控下限。点击 dispatch 'preview'（NightWorkbench.tsx:187-188），reducer:57 只改 previewEntryId，随即 isPreviewing 为真：整个记录表单变 opacity .48 只读（css:246）、底栏换成 [回到N号][处理N号]（NightActionBar.tsx:49-55）。
- 后果：单手托平板时拇指根部正好搭在屏幕左右边缘，这两个浮标就在拇指的自然落点上。误触后表单变暗、按钮换了两个新词，说书人第一反应是"程序卡了"或"我把记录清空了"，要理解自己进了预览态、再找到"回到N号"退出，通常要 3-5 秒且伴随明显的困惑动作，围观玩家会读出说书人的慌乱。

**🟡 medium · 排版布局｜同一张卡的上下两半用了两个不同的容器做断点，平板竖屏出现"卡已单列、页头还宽屏"的错位**

- 证据：外层 `container: workbench`（css:2）管页头和转盘（css:564/575/586/704/711/716/739 共 7 个断点块），内层 `container: night-content`（css:189）管工作卡（css:551/558 两个断点块）。而 night-content 的宽度是 `min(1080px, 100% - clamp(20px,4cqw,40px))`，恒比 workbench 窄 20-40px，于是同一个 860/520 阈值在两套体系里对应的实际页面宽度差 20-40px。状态栏又同时被两边管：css:223 定义、css:572 在 workbench≤860 改 padding-inline、css:600 在 workbench≤520 改成三栏 grid，而它上下相邻的 .wake-facts/.wake-recorder 的 padding 却由 night-content≤860/≤520 决定（css:553-554、559-560）。
- 后果：iPad 竖屏 820pt 正落在这条缝里：工作卡已经塌成单列长条，页头和转盘还按宽屏排。翻转平板演示时，同一块内容会在两个不同角度先后跳变两次，左右内边距还对不齐（状态栏 18px vs 下面正文 16px），看起来像界面坏了；说书人为了避免这种跳变，往往固定一个不顺手的持握方向硬撑一整局。

**🟡 medium · 视觉层级｜遮蔽态同屏出现两枚金色主按钮，且此时仍可确认一条自己看不见的记录**

- 证据：privacyShielded 为真时 CurrentWakeCard.tsx:65-76 直接早退成 .privacy-curtain，里面是 `<Button variant="primary">解除遮蔽</Button>`（:73）；但 NightWorkbench.tsx:221-243 的 NightActionBar 不受遮蔽影响，NightActionBar.tsx:69 的金色 [确认并下一位] 依然可用——canConfirmDraft（workbenchGuards.ts:12-20）不检查 privacyShielded。两枚金色 primary 相距约 100px。文案上还有三种说法：PrivateRevealOverlay.tsx 的"收起并遮蔽"、curtain 里的"恢复说书人视图"、按钮上的"解除遮蔽"。
- 后果：给玩家看完信息、平板刚拿回手里的那一刻，屏幕上有两个一模一样的金色按钮，一个是恢复视图、一个会直接把记录落库并推进夜序。按错了不会报错（窄屏连提示条都被隐藏），说书人在看不到任何记录内容的黑幕状态下就把这一项确认掉了。

**🟡 medium · 状态反馈｜两个都叫"夜序"的数字在预览时互相打架**

- 证据：页头 NightWorkbench.tsx:127-133 用的是 activeIndex（来自 activeCursorId，useNightWorkbench.ts:23）显示"夜序 3/12"并按此填进度条；转盘 NightPlayerCarousel.tsx:64 用的是 current.orderIndex（来自 previewEntryId，useNightWorkbench.ts:22/24）显示"夜序 5"。预览时两者必然不同却共用同一个词。另外页头进度条 css:44 是一条带发光的金色填充，画的其实是"光标位置"而不是"完成度"，真正的完成度"已确认 N"只是 css:45 里 12px 的灰色 meta 文本。
- 后果：说书人瞄一眼顶部以为自己在第 3 项，屏幕中间大字写着第 5 项，两个数字都带着"夜序"二字。真实场景里这一步就是判断"我到底该叫醒谁"，读错就会跳着叫人；而那条最醒目的金色进度条越走越满，会让人误以为夜晚快结束了，实际上暂缓和待核对的项一个都没做完。

**🟡 medium · 信息架构｜页头承担了 5 类不同性质的东西又不吸顶，二级入口滚出屏幕后要来回长滚**

- 证据：night-workbench.css:14-45 的 .night-header 同时塞进：身份标识（kicker + h1 + 剧本人数）、进度仪表（数字+进度条+三个计数）、离场动作（返回本局）、两个二级工作台入口（夜间顺序、本局记录）。它没有 position:sticky（css 全文 position 声明见 66/81/99/151/156/171/192/464/465/471/497/523/758，页头不在其中），却仍写着 `backdrop-filter: blur(20px)`(css:28)——这层模糊在非吸顶元素上没有任何视觉作用，只留下合成开销。
- 后果：因为主任务的确认区在页面下半段（见前述折叠问题），说书人一旦滚下去，进度、待核对计数和"夜间顺序/本局记录"入口就全部离开视野。中途想核对一句"上一夜谁被下毒"，动作是：滚到顶 → 开全屏记录页 → 关闭 → 再滚回底部找回原位。这个来回在牌桌上要五六秒，玩家会以为流程出了问题。

**🟡 medium · 视觉层级｜次要动作与最高频动作共用 secondary，一屏 5 个同款按钮看不出轻重**

- 证据：同为 `variant="secondary"`（ui.css:26 灰底细边）的有：PlayerStatusBar.tsx:55 [更换角色]、CurrentWakeCard.tsx:108 [适用]、CurrentWakeCard.tsx:238 [展示信息]、NightActionBar.tsx:52 [回到N号]、NightActionBar.tsx:68 [确认本项]、NightCloseFooter.tsx:33 [检查并关闭]。其中"确认本项"是本页的核心写入动作，"检查并关闭"是一夜一次的不可逆动作，二者与"展示信息"完全同款。
- 后果：按钮的视觉重量不再传达任何信息，说书人必须逐字阅读才能选。在需要边说话边操作的场合，这直接把"看一眼就按"退化成"停下话头低头读屏"，桌上会出现明显的沉默间隙，也让玩家有机会从说书人低头的时长里推测夜间发生了什么。

**🟡 medium · 一致性｜字号、圆角、字重、对比度大面积绕开 token，最关键的两处小字在暗光下不可读**

- 证据：ui-design-system.md:12 规定只用 meta/label/body/section/page-title 五档、且辅助文本不得低于 11px；实际 css:269 `.outcome-button small { font-size:.66rem }`＝10.56px，而这正是区分"AI建议"和"核对建议"的唯一标记（CurrentWakeCard.tsx:217，两者共用同一套 info 配色）。css:540 页脚 `.7rem` 的 --text-subtle 文字又被 css:702 整体乘上 `opacity:.82`，实测对比度约 3:1，低于 AA。其余脱轨值：.66/.7/.72/.76/.78/.8/.82/.84/.86/.875/.9/.95rem 十余种自定字号；ui-design-system.md:13 只许 8/14/22 三档圆角，实际出现 24px(css:138)、28px(css:84)、`999px / 58%`(css:651)、以及 tokens.css:56-57 自增的 12/16；字重出现 750/760/850 等非常规档。
- 后果："AI建议"和"核对建议"是两种可信度完全不同的东西——一个是模型猜的，一个是本地规则算的（NightWorkbench.tsx:271-297 的赌徒核对）——却用 10.5px 同色小胶囊呈现。暗光下说书人根本分不清，要么无脑采纳 AI、要么连本地确定性建议也不敢用。底部页脚同理：唯一告诉你"正在关闭哪一个夜晚"的文字是最暗的一处，按下前无法确认对象。

**⚪ low · 一致性｜CSS 是三层叠写的历史地层：断点被重复打开 7 次，多条窄屏规则被后写的宽断点静默覆盖，还留着 4 组死规则**

- 证据：文件被切成三段：基础层(1-629)、`/* V3 live-hosting polish */`(632-720)、`/* V3.1 self-review polish */`(723-772)，同一个 `@container workbench (max-width: …)` 共出现 7 次（860:564/704，640:575/711，520:586/716/739）。因为同特异性后写覆盖先写，实际失效的有：css:589 `.carousel-current__token-stage{width:136px}` 被 css:713 的 144px 覆盖、css:588 `--disc-size:104px` 被 css:714 的 108px 覆盖、css:567 的 226px 与 css:576 的 196px 转盘高度被 css:643 的 250px 覆盖——即窄屏"缩小转盘"的意图在 520-860 区间全部落空。另有从未被 TSX 引用的孤儿规则 .ai-inline-note(271-274)、.resolution-hint(275-276)、.action-disabled-reason(535
- 后果：对使用者的直接后果就是上面那条"首屏被转盘占满"：设计上原本已经写好了窄屏把转盘压到 196px 的方案，却因为文件叠层而从未生效，平板上白白多占 54px。对维护者的后果是任何一次"把转盘调矮一点"的修改都会看起来无效，只能继续往文件末尾追加第四层，问题会持续恶化。

**⚪ low · 一致性｜与白天工作台的同类控件长得不一样：返回位置左右相反、选中色三种、破坏性动作配色相反、断点值也不同**

- 证据：返回位置：DayWorkbench.tsx:194 的 [返回本局] 在页头最左，NightWorkbench.tsx:143 的在页头最右侧、和两个 Sheet 入口挤在一起（css:35 justify-content:flex-end）。选中色：夜间 chip/结果选中是暖金（css:260/266），白天分段是青（day-workbench.css:44 is-active 用 info），白天结算选项又是琥珀（day-workbench.css:103）。破坏性动作：DayWorkbench.tsx:267 用 `variant="danger"`，NightCloseFooter.tsx:25 用金色 `primary`。类名体系：夜间用 BEM 的 --selected/--active，白天用 .is-active。断点：白天是单一的 `@container (max-width: 500px)`(day-workbench.css:114)，夜间是 860/640/520 三档。
- 后果：一局游戏里说书人要在夜/日两个工作台之间来回切换十几次。返回键左右横跳意味着每次切换都要重新找；"选中"在两个页面是不同颜色，肌肉记忆无法迁移；最危险的是"红色=不可逆"的约定在夜间被换成金色，白天养成的警觉到了夜里正好失效。旋转平板时两页还在不同宽度换挡，加深"这软件不太稳"的观感。

### 白天工作台 DayWorkbench（src/features/day-workbench/）

<details><summary>界面区块结构（点开）</summary>

```
外框：main.day-workbench 自带 container-type:inline-size（css:1），所有断点都是容器查询而非屏幕查询；AppFrame 在 ≥1024px 给白天视图挂 216px 侧轨（≥1280px 264px），故工作台可用宽 = 屏宽 − 轨道宽；min-height 用 calc(100dvh−58px)，但框架里并不存在 58px 顶栏。

A 页头（常驻，min-height 96px；列间距因 --space-28 未定义而塌成 0）
  A1 「← 返回本局」ghost 键；容器 <500px 时用 font-size:0 变成 44px 纯图标
  A2 focus 区，两栏 0.42fr / 0.86fr
     a 标题组：青色小字"白天工作台" + h1「第N天」(最大 2.1rem 衬线 900) + 金色徽标「记录中」
     b DayTimer 独立卡（自带边框/渐变/内阴影，min-width 480px、min-height 58px）：阶段名 + 倒计时 1.08rem + 「可开始提名」(仅公聊超时) / 「下一段：公聊N分」(仅私聊阶段) / 右侧三键 时长(开全屏设置页)·开始|暂停(或"开始公聊")·重置(图标)；公聊已超时 → 开始|暂停键整个消失，只剩重置
  断点：容器 ≤900px → 返回键独占第 1 行（右半永远空着），focus 整行落到第 2 行；≤720px → focus 变单列，标题在上计时卡在下，页头实高可达约 190px

B 离开确认条 LeaveWorkbenchNotice（条件显示：有票型/技能草稿时点"返回本局"才插入，位于页头与内容之间）

C 内容区（width = min(1060px, 100%−32px)，两栏 0.9fr / 1.2fr，gap 16px；容器 ≤900px 转单栏）
  C1 第N轮状态条（整行，青色描边细条）左：「第N轮」+「待选提名人 / 待选被提名人 / X号提名Y号」；右：「记技能/事件」按钮 → 打开全屏白天记录页
  C2 卡片「步骤 1 · 选择提名」（两栏时在左列）：提名人/被提名人二选一标签页（各自显示已选号）+ 6 列座位键网格（容器 ≤500px 转 4 列）；死亡座位只有虚线 + 62% 透明，无文字无图标
  C3 卡片「步骤 2 · 记录举手」（两栏时在右列，与 C2 并排并被拉成等高）：标题行右上塞了「处决门槛」数字输入框 + 建议值小字（完整解释只在 hover title 里）/「X号提名Y号」（同一事实第三次出现）/ 6 列举手网格，死亡且已勾选的座位下方长出 26px 高的「标死亡票」药丸并撑高整行 / 举手·死亡票·门槛 三格 54px 大数字面板 / 整宽主按钮「记录本轮票型」+ 未选齐时的黄色提示行
  C4 卡片「本日票面 · 暂列结果」（整行）：徽标（暂列/已更新/同票/未达门槛/暂无）+ 大字「N号暂列 · X票 · 门槛Y」+ 右下按钮组「记录无处决」(secondary) /「记录处决N号」(danger，仅 leading|replaced 时出现)
  C5 处决确认条（条件显示：点了 C4 按钮后追加在 C4 下方；红框，含「本次处决造成死亡」勾选框 + 取消 / 确认记录）
  C6 结束白天确认条（条件显示：点了 C7 后出现；黄框；有草稿 → 继续处理 / 清空并结束，无草稿 → 取消 / 确认结束）
  C7 页尾（条件显示：当天未关闭时；整行、非吸底、位于长滚动最末）左：「结束白天 / 关闭当前白天记录；不会开始夜晚」右：ghost 按钮「结束今天」
  断点 ≤500px：C4 按钮组与 C5 / C6 / C7 由横排改竖排整宽

D 由本页拉起的全屏页（Radix Portal 挂到 body，脱离容器）：D1 白天记录页（技能/公开事件切换 → 发动者 6 列座位 → 角色下拉 ×2 → 目标 6 列座位 → 结果三选一 → 预览句 →「记录技能」+ 紧邻的「清空草稿」）；D2 白天时长设置页（私聊/公聊两列输入 + 保存时长）。这两页写在 css:114 里的 ≤500px 响应式规则全部不生效

E 界面里没有的：已记录轮次列表、本局记录入口、吸底操作栏、从工作台直达全屏公聊倒计时的入口
```

</details>

**🔴 high · 状态反馈｜暂列结果徽标的 tone 被父选择器整体染成青色，状态语义失效**

- 证据：day-workbench.css:40 `.day-card__heading span{color:var(--info-text);font-size:var(--type-meta);font-weight:800;letter-spacing:.1em}` 特异度 (0,1,1) 压过 ui.css:44 `.status-badge--warning{color:var(--warning-text)}` (0,1,0)；StatusBadge.tsx:7 根元素正是 span；DayWorkbench.tsx:250 恰恰靠 tone 区分「暂列/已更新」与「同票/未达门槛/暂无」
- 后果：全场最该一眼分辨的信号（有人暂列 vs 没达门槛）只剩极淡的底色差别，文字一律青色。暗光下隔着桌子瞄一眼，极易把「未达门槛」读成「暂列」，直接落锤处决

**🔴 high · 状态反馈｜未达门槛时正文照写「N号暂列」，与徽标直接矛盾**

- 证据：DayWorkbench.tsx:251 正文分支只判断 `standing.nomineeSeatId` 是否存在；voteStanding.ts:76-82 below_threshold 同样返回 nomineeSeatId，于是徽标显示「未达门槛」而大字显示「7号暂列 · 3票 · 门槛4」。voteStanding.ts:126 算出的 previousNomineeSeatId 在 UI 中从不展示
- 后果：同一张卡上两句话互相否认，被玩家追问时说书人得现场心算票数才敢下结论；replaced（换人领跑）这件最需要当众说明的事，界面里根本看不见换的是谁

**🔴 high · 排版布局｜平板横屏两栏把提名座位键挤到溢出重叠**

- 证据：day-workbench.css:32 `grid-template-columns:minmax(310px,.9fr) minmax(380px,1.2fr)` + :45 `repeat(6,minmax(44px,1fr))` + :37 卡片左右各 20px 内边距；而 ui.css:52-53 `.seat-button` 实际是 min-width/min-height 52px。iPad 横屏 1180−216 轨道 = 964 容器，左列约 392px，卡内约 352px，6 列每格仅约 50px < 52px
- 后果：最常见的横持平板姿势下提名区号码键互相压边甚至越出卡片；竖屏反而是单栏 116px 巨键。同一个人换个握姿看到两套完全不同的密度，单手快点时误触相邻座位

**🔴 high · 排版布局｜全屏 Sheet 走 Portal，本文件里的窄屏规则全部失效**

- 证据：Sheet.tsx:31 `Dialog.Portal` 把内容挂到 body；ui.css:122-144 `.sheet-content` 没有 container-type，body 之下已无容器祖先；因此 day-workbench.css:114 中 `.day-record-sheet__seats`、`.day-timer-settings__fields`、`.day-skill-fields__roles/__targets/__outcomes` 的 ≤500px 规则永不命中
- 后果：手机或窄窗口打开白天记录页时仍是 6 列座位、2 列角色下拉、3 列结果按钮，每格挤到 40px 出头；记一次白天技能要在全屏最小的控件上连点五六下，还是在被玩家围观时

**🔴 high · 交互流程｜公聊超时后没有开始/暂停键，且整局无法回到私聊**

- 证据：DayTimer.tsx:53 `: timer.hasElapsed ? null :` —— 公聊阶段超时后主动作渲染为 null，只剩 :58 的重置图标；discussionTimer.tsx:103-111 只有 startPublic 能改 activeStage，:112-119 resetCurrentStage 只重置当前阶段。对照 PublicChatTimerPage.tsx:43 同一状态却给了一颗禁用的「可开始提名」按钮
- 后果：第一天公聊一结束，计时器就永久停在公聊阶段，第二天想开私聊只能整局重开；而同一状态在工作台内和全屏页表现还不一样，主持人得记两套心智模型

**🔴 high · 交互流程｜破坏性确认条渲染在长滚动最底部，无滚动与焦点管理**

- 证据：DayWorkbench.tsx:258-272 pendingResolution / pendingDayClose 以普通 section 追加在 C4 之后，仅 `aria-live="polite"`，无 scrollIntoView / autoFocus；css:38 与 :105 让它们占整行；css:76 页尾也不是吸底（共享的 StickyActionBar 在本页完全没用）
- 后果：点「记录处决N号」后确认条常落在屏幕外，说书人以为没生效就再点一次，或干脆找不到确认按钮；同一屏最多同时存在离开确认条、处决确认条、结束确认条三种长得都不一样的「是否确认」

**🔴 high · 视觉层级｜页头两个竞争焦点，且列间距因未定义 token 塌成 0**

- 证据：day-workbench.css:124 `gap:clamp(var(--space-18),3cqw,var(--space-28))`，而 tokens.css:42-53 根本没有 --space-28，该声明 IACVT 后 gap 退回 normal=0，连 :2 与 :114 里原本的 16/12px 也一并被覆盖失效；:9 h1 clamp 到 2.1rem，:10 计时卡自带边框渐变阴影且 min-width 480px
- 后果：返回键与「第N天」标题贴死在一起，暗光下按返回容易蹭到标题区；标题与计时卡两个重量级块平分页头，真正的主任务（提名与举手）被压到首屏以下，≤720px 时页头能吃掉约 190px

**🟡 medium · 信息架构｜倒计时有两套入口两套形态，工作台内进不去全屏**

- 证据：App.tsx:61 Dashboard 的 onOpenTimer 与 :63 DayWorkbench 是并列视图；Dashboard.tsx:172-175 首页单独有「公聊倒计时」按钮；DayTimer.tsx 与 PublicChatTimerPage.tsx:40-49 对同一 useDiscussionTimer 状态给出不同按钮集；discussionTimer.tsx:72 导出的 summary 两处都没人用，各自另写一套措辞
- 后果：想把大字倒计时转给玩家看，必须先「返回本局」再点「公聊倒计时」，中途白天工作台整个消失、票型草稿只能靠暂存承诺兜底；同一个计时器在两处的措辞和可用动作都不同

**🟡 medium · 可发现性｜「记技能/事件」入口藏在名为「第N轮状态」的条里，标签还被染成青色**

- 证据：DayWorkbench.tsx:212 section 的 aria-label 是「第N轮状态」，:214 却把 DayRecordSheet 触发器放了进去；day-workbench.css:35 `.day-round-context span` 又把 DayRecordSheet.tsx:82 按钮内的 span 文字改成 info-text 青色 + .08em，而同一颗按钮的 BookPlus 图标仍是 text-strong 白
- 后果：白天技能与公开事件是与投票并列的主任务，却挂在一条看起来只读的状态带上；按钮文字青色、图标白色，不像可点的 secondary 按钮，新说书人在现场找不到它

**🟡 medium · 信息密度｜「标死亡票」药丸只有 26px 高，且一出现就撑高整行**

- 证据：day-workbench.css:57 `.day-ghost-vote{min-height:26px;padding:0 6px;font-size:var(--type-meta)}`，违反 ui-design-system.md:10 与 :82 的 44px 下限；DayWorkbench.tsx:240 该药丸只在 dead && selected 时作为 `.day-vote-seat` 网格项的第二行渲染
- 后果：死亡票是最容易记错也最要命的一票，却配了全屏最小的控件；勾中一个死亡座位就把整行座位往下推一截，连点几个人时后面的号码位置一直在跳，暗光下必然点错

**🟡 medium · 一致性｜用 subdued 表达「死亡」这一规则事实，死活只靠虚线与透明度**

- 证据：DayWorkbench.tsx:226 与 :239 用 `subdued={playerStates[seatId]?.life==='dead'}`；ui.css:72 `.seat-button--subdued` 只是 dashed + opacity .62 + text-subtle；ui-design-system.md:30 明写「subdued 仅表示低强调……业务层不得把它当规则限制」，:55 要求死亡同时用图标、文字和语义颜色
- 后果：暗光低亮度平板上 62% 透明的号码几乎读不出来；死人这一票能不能算要靠眼力判断而不是界面明说，与首页玩家状态卡的死亡表达也对不上

**🟡 medium · 信息架构｜同一条提名事实一屏内重复三遍，却看不到已记录的轮次**

- 证据：DayWorkbench.tsx:213 roundStatus、:220-221 两个标签页各带已选号、:233 day-vote-target 都在写「X号提名Y号」；同时 :82-84 只用 dayEntries 算轮次序号，界面从不列出已完成轮次，页头也没有 ui-design-system.md:92 要求的「本局记录 N」入口（夜间在 NightWorkbench.tsx:161-167 有）
- 后果：屏幕被同一句话占三次，真正稀缺的信息（前几轮谁被提名、各多少票）反而要退出工作台去首页翻记录；被玩家质疑上一轮票数时无法就地回答

**🟡 medium · 交互流程｜「清空草稿」紧贴主按钮，且「结束」有四种措辞**

- 证据：DayRecordSheet.tsx:104-105「记录技能」(primary) 与「清空草稿」(ghost + Trash2) 上下紧邻，css:91 两颗都是 width:100%，违反 ui-design-system.md:113「危险动作紧邻主按钮」；DayWorkbench.tsx:275 结束白天 / :276 结束今天 / :270 确认结束 / :267 清空并结束 四种说法，且承载阶段结束的是最低强调的 ghost 键
- 后果：填完整张技能表后手指顺势下滑就可能清空全部草稿且无撤销；「结束」这件事换了四个词，时间紧时说书人不敢确定按下去到底是关白天、清草稿还是进夜晚

**🟡 medium · 交互流程｜处决门槛靠手输数字，建议值从不跟随白天死亡自动更新**

- 证据：DayWorkbench.tsx:37 newDraft 只在创建草稿时取 executionThresholdForAliveCount；:80 suggestedThreshold 每次重算却只用于 :232 的提示文案，没有「应用建议」动作；:232 完整解释放在 `title` 属性里（触屏 hover 不可达），输入框 css:48 仅 72px 宽的 type=number
- 后果：白天出现死亡后门槛应当变化，界面只在角落用 12px 青字提示而不改值；说书人要在众目睽睽下调出数字键盘敲一个数，或者干脆忘了改，导致处决判定用错门槛

**🟡 medium · 一致性｜CSS 存在整块 V3 覆盖与大量一次性数值，说明结构没抽象好**

- 证据：day-workbench.css:117-143「V3 header alignment」把 :2-5 的 flex/absolute/居中方案整体推翻，致使 :112 的 `flex-direction:column` 成为死规则；:114 一行塞进 20 多条断点特例；字号 1.08/1.18/1.2/1.05rem 与 :9 的 clamp 绕开 tokens.css:4-9 的五档；圆角 999px/34px/26px（:57、188、263）越出 8/14/22，且 --radius-md=12 本身就与 ui-design-system.md:13 的 14 不符；:49 `.day-threshold__hint` 的 color/weight/letter-spacing 又被 :40 全数压掉
- 后果：同一处版式并存两套互相打架的定义，任何改动都会在某个断点炸开；现场看到的字号与圆角跟夜间工作台对不齐，主持人在昼夜切换时要重新适应一套视觉

**⚪ low · 排版布局｜两栏并排与「步骤 1 / 步骤 2」的顺序语义互相矛盾**

- 证据：DayWorkbench.tsx:218 与 :232 用「步骤 1」「步骤 2」标注先后，而 day-workbench.css:32 把二者放进同一行两列并默认 align-items:stretch，:37 卡片有实底背景；提名卡内容远少于举手卡，会被拉出大片空白
- 后果：编号说「先做这个再做那个」，排版说「两件事同时进行」；宽屏下提名卡下方一大片空底色，主持人的视线在两列之间来回跳，找不到当前该点哪儿

### 本局首页 / 常驻页（Dashboard）— src/features/dashboard/Dashboard.tsx + componen

<details><summary>界面区块结构（点开）</summary>

```
【容器】.dashboard：width min(1440px,100%)，自身 container-type:inline-size（css:1-7, 1675-1677）。所以本页所有 @container 断点算的是「内容区宽度」≈ 视口宽 − 左右 clamp(16~32px) 内边距，不是视口宽。iPad 竖屏 768px 视口 ⇒ 容器约 722px。
【整体】自上而下 5 个区块，全部单列堆叠；无侧栏、无吸底栏、无返回栏。

1) 页头 .dashboard__header（tsx:136-160）
 ├ 左列 div（占满整宽）
 │  ├ 眉标「本局」（12px 金色，css:9）
 │  ├ h1 板子名（宋体 clamp 1.8~2.6rem）
 │  └ ★巨型「当前阶段」卡 .dashboard__stage-current（min-height clamp(150~240px)、圆角 30px、金色渐变+内发光）
 │     ├ 左块 -main：小字「当前阶段」+ 超大阶段名「第N夜」（clamp 2.7~4.35rem）+ 金色胶囊 meta（记录中/已结束/下一步）
 │     ├ 右块 -info：毛玻璃小卡（板子名 + 「12 / 夜间处理」）
 │     └ 右上 -decoration：3 个纯装饰金点（aria-hidden；right 定位失效，见问题5）
 └ 右上角绝对定位齿轮 .dashboard__settings-trigger（44px 圆钮）= AI API 设置（AISettingsSheet.tsx:160）
 〔断点〕≤620：卡内改上下堆叠、min-height 升到 168px、阶段名放大到 clamp(2.55rem,13vw,3.45rem)，左列补 54px 右内边距给齿轮让位；>620 不让位。

2) 常用工具行 .dashboard__host-tools（tsx:162-180）
 5 个平级次要按钮，统一 60px 高：[开场白][AI配板][发身份][公聊倒计时][切换板子]
 〔断点〕默认 5 列 → 621~900px 3 列 → ≤620 与 ≤460 均 2 列（三条规则互相覆盖，见问题15）
 〔不齐〕「发身份」单独镀了金色渐变；「开场白」是 compact 变体（13px 字，其余 15px）

3) ★真正的主任务：工作台入口 .dashboard__phase-launch（tsx:182-193）
 两张 112px 高大按钮并排 = 52px 圆形图标 + 标题（进入夜晚 / 进入白天）+ 副标题（有开放段时「继续记录 · 第N夜」，否则「首次确认后建立记录」）+ 右箭头
 〔断点〕≤620 变上下一列；≤420 缩到 92px —— 这一条用的是 @media（视口宽），与全页 @container 体系不同源

4) 玩家状态板 .dashboard-card--players（PlayerStatusBoard.tsx:14-61）
 ├ 卡头：左「状态 / 玩家状态」；右「N人 · 存活x · 死亡y」+ 12px 灰字提示「点卡核对」
 └ 卡体二选一：
    ├ 0 人 → 虚线空态卡「暂无玩家 / 先进入 AI 配板或切换板子，录入人数和座位后再显示状态。」
    └ 有人 → 座位网格，每格是一张 128px 高的可点按钮卡：
         行1 座位号 + 生死图标（死亡时附红色「死亡」小标）
         行2 右上角浮层徽标（中毒/醉酒/标记，仅在有该状态时出现，绝对定位）
         行3 角色名（单行省略号）
         行4 昵称（单行省略号）
         行5 空占位（已 display:none）
      〔断点〕默认 6 列 → ≤620 4 列 → ≤420 3 列。注意 621~760 这一段没有规则，仍是 6 列 —— iPad 竖屏正好落在这里。

5) 底部双卡 .dashboard__grid（tsx:197-236）
 基础样式写的是 2 列，但被后面的 V3 补丁（css:330）强制改回 1 列，实际永远单列上下排：
 ├ ①「记录 / 最近记录」卡：右上角一个 compact 按钮「日记」（打开完整时间线全屏页），下方最多 3 条记录，无记录时「尚无确认记录。」
 └ ②「收尾 / 结束对局」卡（带黄色「危险动作」徽标）：一段说明文字 + 并排两个按钮 [结束对局][历史复盘]（两者打开同一张 GameEndSheet 的两个 tab）；≤620 时两按钮各占满一行、上下紧贴

【断点一览】同一文件混用两套坐标系：@container 420 / 460 / 520 / 620 / 760 / 860 / 900 / 1180，@media 420（仅 css:296 一处）。
```

</details>

**🔴 high · 状态反馈｜中毒/醉酒/标记/死亡的生死指示被整体 opacity:0 隐藏，状态只剩颜色**

- 证据：dashboard.css:1510-1515 对 .dashboard-player-seat--poisoned/--drunk/--impaired-stack/--marked 下的 .dashboard-player-seat__life 一律设 opacity:0；dashboard.css:1639-1643 又对 :not(--dead) 变体追加 visibility:hidden。被隐藏的正是 PlayerStatusBoard.tsx:46-49 里的 HeartPulse/Skull 图标和 <em>死亡</em> 文本。全文件 1741 行内没有任何规则把 opacity 复原。直接违反 ui-design-system.md:55「存活、死亡、中毒、醉酒、标记不能只靠颜色区分」。
- 后果：一张「死亡+中毒」的座位卡，骷髅图标和红色「死亡」二字都是 opacity:0，死亡只剩深红背景在表达；一个只被打了标记（--marked）的活人，连心跳图标都没了，卡上没有任何生死线索。暗光下背景色差本就最先失真，说书人扫一眼 12 张卡数存活人数会数错，而处决/胜负判定全靠这个数。

**🔴 high · 状态反馈｜最大的那张「当前阶段」卡，在阶段结束后显示的是下一个阶段**

- 证据：Dashboard.tsx:108-111 段落关闭后 state 变 'done'；Dashboard.tsx:128-131 的 currentStage 回退链找不到 current/open 就退到 'next'；Dashboard.tsx:144-145 直接渲染 <strong>{currentStage.label}</strong><em>{currentStage.meta}</em>。第3夜 closedAt 之后，卡片字面读作「当前阶段 / 第3天 / 下一步」。同时 aria-label 用的是另一套文案 summary「第3夜已结束，下一步第3天」（Dashboard.tsx:115,140），视觉与读屏结论不一致。违反 ui-design-system.md:62「不再使用两个含义不明的『当前』」。
- 后果：全屏最大、字号最大（4.35rem）的元素在说谎：说书人刚结完第3夜抬头，卡上写着「当前阶段 第3天」，但天还没开。被玩家围观时这是当众的信息错误，说书人要么照着念错、要么当场愣住去翻记录确认自己在哪个阶段——而这张卡存在的唯一理由就是免去这次确认。

**🔴 high · 信息架构｜真正的主任务入口排在装饰性大卡和 5 个次要工具之后**

- 证据：Dashboard.tsx 区块顺序为 136（页头+hero卡）→ 162（5 个工具按钮）→ 182（进入夜晚/进入白天）。hero 卡 min-height clamp(150px,16cqw,240px)（dashboard.css:1691）、阶段名 clamp(2.7rem,7cqw,4.35rem)（css:1306）；而主入口只有 112px 高、标题 1.15rem（css:102,107）。窄屏时 host-tools 变 2 列 3 行（css:1095-1097）再叠约 200px。
- 后果：一整局里说书人重复次数最多的动作就是「进入夜晚/进入白天」，却被一张只读的状态卡和「开场白/AI配板/发身份/倒计时/切换板子」这些每局最多点一次的工具挤到第三屏块。手机宽度下第二个入口已在首屏之外，单手持平板时要先滑动再点，且滑动会把状态板推出视野。视觉权重最大的是不可点的装饰卡，可点的主动作反而最小。

**🔴 high · 排版布局｜平板竖屏座位网格仍是 6 列，中毒/醉酒卡的角色名只剩约 2.8 个汉字宽**

- 证据：dashboard.css:116 基础 repeat(6)，css:279 仅 ≤620 降到 4 列，css:294 仅 ≤420 降到 3 列，621~760 区间无规则。iPad 竖屏 768 视口 ⇒ 容器约 722px ⇒ 每列 (722−5×14gap)/6 ≈ 108px。座位卡左内边距 20px（css:332），中毒卡右内边距 14px（css:1601），且 css:1644-1650 给 > b 再加 padding-right:38px 让位给右上角徽标，角色名可用宽仅约 37px，而字号是 13px（css:217）。
- 后果：iPad 竖屏是线下单手持握最常见的姿势，而这个宽度正好掉进没人写规则的 621~760 缝隙里。结果恰恰是最需要看清的中毒/醉酒座位，角色名被截成「洗…」「图…」。说书人要么横过屏幕（另一只手腾不出来），要么逐个点开全屏页核对——被玩家围观时每点开一次都是一次泄密风险窗口。

**🔴 high · 排版布局｜5 个 spacing token 根本没定义，间距被静默归零，团队还在给症状打补丁**

- 证据：--space-9（css:332）、--space-26（1260）、--space-28（119）、--space-36（1349）、--space-48（1695）在 src/styles/tokens.css 中均不存在（tokens 只定义 4/6/8/10/12/14/16/18/20/24/32/40），全仓库也无别处定义。后果：css:332 的 gap:var(--space-9) 覆盖了 css:149 的 gap:var(--space-8) 并 invalid-at-computed-value ⇒ 座位卡行间距变 0；css:119 的 padding:clamp(var(--space-18),3cqw,var(--space-28)) ⇒ 空态卡内边距变 0；css:1349 的 right:clamp(...,var(--space-36)) ⇒ 装饰点失去右锚点、退回静态位置落到卡片左上；css:1260 的问题被 css:1438-1441 用 padding:...!important 盖住，而那条补丁的注释自称「old responsive rules zeroed the curre
- 后果：没有任何构建报错，所以这类归零只有肉眼能发现，而肉眼在设计走查时会归咎于「排版有点挤」。座位卡角色名和昵称零间距贴在一起，正是暗光下最难分辨的两行文本；hero 卡右上角那三个金点跑到左上压住「当前阶段」眉标。更糟的是团队已经在用 !important 打症状补丁，下一个人还会踩同一批 token。

**🔴 high · 一致性｜dashboard.css 有 44% 是给已经删掉的 DOM 写的，24 层 V3.x 补丁叠罗汉**

- 证据：1741 行中约 775 行（44%）的选择器在任何 .tsx 里都找不到对应 class：.dashboard__phase-timeline / .dashboard__phase-step 占 css:368-1050 共 683 行（V3.2→V3.7 连续 6 次整体重写同一个已删除组件，.dashboard__phase-step 作为选择器出现 101 次）、.dashboard__utility-actions（12-13, 275-276）、.dashboard-card--setup（257）、.dashboard-state-chip（221-225, 353-354）、.dashboard__stage-context / .dashboard__stage-latest（1110-1229）。文件里有 24 条 /* V3.x */ 补丁横幅（303 → 1709）和 39 处 !important；css:1407「V3.9.1 reset old phase-card grid areas that caused text overlap」是用 grid-area:auto!important 
- 后果：这是「排版和逻辑有点混乱」最直接的机械成因：结构没抽象出来，每次改版都是往文件尾部再糊一层覆盖。任何一次改动都要先在脑内跑一遍 24 层的层叠顺序才知道最终值，导致像上面 opacity:0 和未定义 token 这样的真实缺陷长期无人发现。对现场的后果是所有视觉缺陷都变成「不敢改」，只能继续叠层。

**🔴 high · 交互流程｜破坏性动作与只读动作并排 10px，且被同一张「危险动作」卡包住**

- 证据：Dashboard.tsx:225-234 把 [结束对局] 与 [历史复盘] 放进同一个 .dashboard__end-actions flex 行，dashboard.css:1729-1733 的 gap 仅 var(--space-10)=10px；css:1737-1740 在 ≤620 时让两者各占满整行、上下紧贴。整卡挂着 StatusBadge「危险动作」（Dashboard.tsx:222），把只读的复盘也染成危险语义。违反 ui-design-system.md:113「危险动作紧邻主按钮」。
- 后果：「历史复盘」是局中说书人偶尔会想翻一下的只读功能，而它左边 10px 就是终结本局。暗光 + 单手持平板 + 拇指触达，误触概率不低；反过来，因为整卡被标成「危险动作」，说书人局中根本不敢碰这个区域，等于把复盘入口废掉了。

**🔴 high · 可发现性｜记录入口散落三处，且都不符合规范定义的「本局记录 N」**

- 证据：ui-design-system.md:92 明确要求「页头常驻的是『本局记录 N』入口」。实际入口是 TimelineHistorySheet.tsx:147 的一个 compact 小按钮，文案写作「日记」、没有计数 N、位置在整页第 5 块「最近记录」卡的右上角（Dashboard.tsx:201-207）。同一页面另有两个记录性入口：「最近记录」列表（Dashboard.tsx:209-216，只显示 3 条）和「历史复盘」（230-233）。
- 后果：说书人被玩家质疑「你昨晚是不是记错了」时需要 3 秒内翻到本局记录，而这个入口既不在页头、又叫一个跟规范/其他页都对不上的名字「日记」、还藏在要滚动到底才看得见的卡片右上角。三个记录入口分布在三处，说书人得先想清楚「本局的」和「历史的」分别归哪个按钮管，围观压力下这一步犹豫是致命的。

**🔴 high · 信息密度｜字号跌破规范硬性下限，状态徽标只有 9.8px**

- 证据：ui-design-system.md:12 规定只用 meta/label/body/section/page-title 五档且「辅助文本不得再临时缩到 11px 以下」。实际 .dashboard-state-mark 字号被压到 .61rem≈9.8px（dashboard.css:1665，这正是「中毒/醉酒/标记」徽标的文字本身），.dashboard-player-seat__life em 的「死亡」是 .66rem≈10.6px（css:213）。全文件另有 15 处硬编码 rem 字号绕过 token（css:107,134,210,299,319,353,609,710,820,989,1533 等）。
- 后果：「中毒」「醉酒」这两个词直接决定技能结算对错，却是全页最小的字（9.8px）。线下场地光线通常只有几十 lux、屏幕还要压暗防偷看、平板离眼睛比手机远，9.8px 的汉字在这个条件下基本不可读——说书人只能靠底色猜，而底色恰好又是问题1里唯一剩下的信号。

**🟡 medium · 交互流程｜同一功能三种叫法、两个入口：结束对局 / 保存重置 / 历史复盘**

- 证据：Dashboard.tsx:226 与 230 的两个按钮分别调 onOpenGameEnd('end') 和 ('review')，App.tsx:67 指向同一个 GameEndSheet；而 GameEndSheet.tsx:203-205 内部本来就有 role="tablist" 的「保存重置 / 历史复盘」两段切换。也就是首页把一个 tab 拆成了两个按钮，且点「结束对局」进去后看到的主标签叫「保存重置」。卡片说明文字（Dashboard.tsx:224）用了「保存」「重置」「归档」三个词，两个按钮却一个都没写。
- 后果：说书人在收场那一刻最紧张（玩家等着听结局、有人要拍照留档），此时按钮文案「结束对局」和进去后的标签「保存重置」对不上，会让人怀疑自己点错了，退出来重点一次——而这恰好是全应用唯一不可逆的流程。说明文字承诺的「保存」在界面上找不到对应控件，说书人无法确认本局到底存没存上。

**🟡 medium · 视觉层级｜标题层级与实际重要性倒置：h1 被自己的子元素压过一倍**

- 证据：h1 用 --type-page-title = clamp(1.8rem,4cqw,2.6rem)（dashboard.css:10 / tokens.css:9），但嵌在同一个 header 里的 .dashboard__stage-current-main strong 是 clamp(2.7rem,7cqw,4.35rem)（css:1306），窄屏还有 clamp(2.55rem,13vw,3.45rem)!important（css:1447-1449）。而语义上的二级标题「玩家状态」「最近记录」只有 --type-section 1.25rem（css:259）。
- 后果：页面上最大的字是一个 <strong>，h1 反而像副标题，h2 又和正文差不多重。说书人扫视时找不到稳定的层级落点，每次都要重新逐块阅读整页——这在需要「抬头 0.5 秒确认、低头继续主持」的节奏里，等于每次都得重新读一遍界面。

**🟡 medium · 视觉层级｜一屏内至少 5 处金色焦点互相竞争**

- 证据：hero 卡金色渐变+内发光（dashboard.css:1266-1274）、卡内金色胶囊 meta（1309-1319）、3 个纯装饰金点（1359-1372）；5 个平级次要按钮里「发身份」单独镀金（.dashboard__identity-entry, css:72-78）；主入口的 .dashboard__phase-icon 金色圆环（css:109）；座位卡 --drunk 整卡琥珀色（css:1556-1561）。设计系统把暖金定义为「当前焦点」色（ui-design-system.md:5）。
- 后果：暖金在这套系统里是「看这里」的唯一语义，现在同屏被用了 5 次，其中权重最高的两处（hero 卡、装饰点）根本不可点。暗光环境下人眼对暖色高光最敏感，说书人的视线会被反复拽向不可操作的装饰，而「发身份」被镀金后在视觉上压过了旁边真正的主动作「进入夜晚」。

**🟡 medium · 排版布局｜卡片套卡片达到四层，超出规范上限两倍**

- 证据：违反 ui-design-system.md:111「卡片套卡片超过两层」。一张中毒座位卡的实际层级：.dashboard-card--players 边框+底色（css:256）→ .dashboard-player-seat 边框+底色（css:141-157）→ ::before 内嵌虚线框（css:1608-1626）→ .dashboard-state-mark 自带描边+阴影的胶囊（css:1525-1538）。死亡卡同构（css:201-208 的 inset:8px 虚线框）。此外 hero 卡内还嵌了一张带 backdrop-filter 的 -info 小卡（css:1321-1331）。
- 后果：每多一层边框就多一条抢注意力的高对比线条。在 108px 宽的座位卡上塞四层框，留给真正内容（角色名、昵称）的宽度和视觉安静度都被吃光，卡片读起来像装饰件而不是数据。暗光下这些细线还会因屏幕压暗而糊成一团灰边。

**🟡 medium · 信息架构｜页头信息重复三次，且有一处无单位歧义**

- 证据：板子名同时出现在 h1（Dashboard.tsx:139）和 hero 卡右侧信息盒（Dashboard.tsx:149），相隔不到 100px。人数出现两次且格式不同：hero 卡里是 {storytellerSeats.length} / {currentStageMode}（Dashboard.tsx:149，渲染成「12 / 夜间处理」，无单位、斜杠含义不明），玩家状态板卡头里是「12人 · 存活8 · 死亡4」（PlayerStatusBoard.tsx:17）。
- 后果：「12 / 夜间处理」这个字符串无法自解释：是 12 号玩家？12 人？还是第 12 步？说书人得往下滚到状态板才能对上号。而 hero 卡右侧那块毛玻璃小卡占了全页最贵的位置，装的却是一条重复信息加一条读不懂的信息——本可以放「还有几个角色没处理」这类真正会变的东西。

**🟡 medium · 排版布局｜断点体系混乱：两套坐标系 + 9 个断点 + 互相覆盖的重复规则**

- 证据：同一文件里 8 个 @container 断点（420/460/520/620/760/860/900/1180）加 1 个 @media 断点（dashboard.css:296，全页唯一按视口宽计算的一条）。.dashboard__host-tools 的列数被三条规则依次覆盖：css:282（≤460 → 2 列）、css:290（461~900 → 3 列）、css:1095（≤620 → 2 列，因源码靠后而胜出），结果「3 列」实际只在 621~900 生效——这个结论只能靠通读源码顺序推出来，规则本身读不出。
- 后果：平板横竖屏切换是线下主持的常态（竖屏看状态、横屏做记录），而布局在 621/760 附近的行为无法预测，同一台设备转个方向就换了一套排版逻辑。对维护者来说，任何一次「把窄屏调宽一点」的改动都会命中三条互相覆盖的规则，只能继续追加 !important。

**🟡 medium · 可发现性｜座位卡唯一的可点提示是右上角 12px 灰字**

- 证据：座位卡是真按钮且带跳转（PlayerStatusBoard.tsx:43 的 <button> + onSelectSeat），但视觉上与纯信息卡无异：没有箭头、没有任何持久可点线索，只有 :hover 变色（dashboard.css:167）——触屏上不存在 hover。唯一的文字说明是卡头右侧的 <small>点卡核对</small>（PlayerStatusBoard.tsx:17），样式为 --type-meta 12px + --text-subtle #6f7881（dashboard.css:113-115），是全页对比度最低的文字之一。
- 后果：「点座位卡打开全屏状态页」是本页唯一通往逐人核对的路径（ui-design-system.md:58 也是这么规定的），但这条路径在界面上几乎不可见。新说书人会以为状态板只是块看板，转而去 AI配板 里找改状态的地方——那是另一条完全不同的流程，改错了要回滚。

**⚪ low · 排版布局｜宽屏下齿轮按钮可能压住长板子名**

- 证据：dashboard.css:1054-1061（V3.7.1）把 header 改成 display:block + position:relative，并给 > div 加 padding-right:calc(44px + 12px) 给绝对定位的齿轮让位；但 css:1699-1707（V3.10.5）又把宽屏的 padding-right 归零，只在 @container (max-width:620px) 时保留让位。齿轮 .dashboard__settings-trigger 仍是 position:absolute; top:0; right:0（css:1062-1068），h1 是块级、宽度 100%。
- 后果：板子名较长时（例如自定义剧本名），标题右端会钻到 44px 圆形齿轮下面。说书人开局向玩家报本局剧本时念到一半发现字被盖住，只能凭记忆补——被围观时是个小但可见的翻车点。同时它也是补丁互相覆盖的又一个例证：让位规则被后来的补丁按宽度砍掉了一半。

### 配板页（AI配板与调整）— src/features/setup/：SetupPanel.tsx + SetupStartPanel.tsx

<details><summary>界面区块结构（点开）</summary>

```
【容器】整页由 App.tsx:65 的 <SetupPanel> 渲染，内部是一个 Radix Dialog（Sheet.tsx:33，presentation="page"）→ 通过 Portal 挂到 body，inset:0 / width:100vw / 无圆角（ui.css:137-144）。因为走 Portal，页面脱离了 .app-frame 的容器查询上下文（app-frame.css:4），所有 `cqw` 实际按视口算。

【第 1 层：页头 sheet-header（常驻不滚动）】
  左：标题「AI配板与调整」(1.5rem 宋体) + 副标题（未开局＝剧本名；已开局＝「后续生效」四个字，无主语）—— SetupPanel.tsx:222
  右：44px 圆形关闭钮
  内边距用 `max(24px, (100vw-1120)/2+24)` 对齐一个 1120 栏（setup.css:335），而下面正文列是 `min(1120, 100%-48px)`（setup.css:342）→ 宽屏下页头文字比正文列多缩进约 15-24px。

【第 2 层：sheet-body（全页唯一滚动容器）】里放居中的 .setup-panel，宽 min(1120px, 100%-48px)。
  ⚠ .setup-panel 的 padding 写成 `clamp(var(--space-24), 4cqw, var(--space-36)) 0 72px`，而 --space-36 在 tokens.css 里不存在 → 整条 padding 失效为 0（>760px 时上下都没有留白，内容顶住页头分隔线、底部顶住屏幕边缘/iPad 手势条）。

【第 3 层：三选一的主区（互斥分支）】
  ▸ 分支 A：session.playerCount === 0 → SetupStartPanel（新局开局）
      hero 大标题「选择人数」font-size 到 4rem（setup.css:11） → 剧本名（与页头副标题重复）
      → 「开局板子」下拉 + 小字「开始配板后生效」（横排，≤760 改竖排）
      → 人数按钮 7…15，9 列网格（≤760 变 3 列）
      → 「玩家 / N个座位」+「复用昵称」ghost 按钮
      → 名单区：7-15 行「号码 / 昵称输入框 / 经验下拉」，桌面 3 列（≤760 单列）
      → 右下「开始配板」primary
  ▸ 分支 B：!effectiveDraft || showCandidates → SetupCandidateBrowser，本身又有两态：
      B1 列表态：区块标题「AI建议 / 角色组合」+ 右侧「AI推荐/重新推荐」→（点过 AI 后）一条横跨整行的青色 AI 条（首选 / 平衡分析 / 微调建议 + 预览调整按钮 / 说书人备注）→ 候选卡 3 列网格（≤760 单列），每张卡＝标题 + 徽标组（AI首选/节奏/质量标签）+ 质量提示 + 角色组合 3 列迷你格（≤760 两列）+ 建议 + [详情 | 采用为草稿]
      B2 详情态（点「详情」后整块替换 B1）：顶部「返回组合」胶囊 + 节奏徽标 → 大标题 hero → 6 列角色格（≤760 三列）→ 主持目标 / 开局操作·给信息·趣味节奏 3 列 / 玩家适配 3 列 / 可微调 → 恶魔伪装 3 列 → 底部 [返回列表 | 采用为草稿]
  ▸ 分支 C：有草稿且未展开候选 → 一行 76px 的「AI配板建议 / <候选名> / 打开」入口条
  ★ 注意：B 与下面的草稿区并非互斥。showCandidates=true 时，候选浏览器会整块叠在草稿区上方，且列表态没有任何「返回草稿/取消」出口。

【第 4 层：草稿区（effectiveDraft 存在时始终渲染，位于主区之下）】
  区块头「说书人草稿 / 角色与座位」+ 右侧「可确认/需调整」徽标
  ├─ .setup-panel__role-panel（有边框有渐变的卡）
  │    ├─ 座位网格：>720px 6 列 / ≤720 4 列 / ≤420 3 列，每格「N号 + 角色名」，可拖拽
  │    ├─ .setup-panel__seat-action（有边框的盒）→ 内含 .setup-panel__seat-actions（又一个有边框的盒）→ 三段控件「交换角色 / 更换角色 / 修改昵称」（≤560 变竖排 3 行）；下方一行绿色操作回执
  │    └─ .setup-panel__bluff-card（卡中卡）：「恶魔 / 伪装建议」+「未在场」徽标 → 3 个 132px 高的伪装格（≤560 单列），每格＝伪装N + 建议/可选胶囊 + 下拉 + 一枚 3 字青色胶囊
  ├─ .setup-panel__rule-card：「开局人数 / 人数修正」+「已核对/待核对」徽标 → 4 格阵营计数条（≤720 两列）→ modifier 提醒（金色警告样式，2 列 / ≤720 单列）
  ├─ .setup-panel__checks：黄底校验清单（草稿过期时首行为红色 alert）；每行单行省略号截断
  └─ .setup-panel__footer：[放弃草稿 or 重新载入] + [确认配板 / 确认调整]，右对齐、**不吸底**，随页面滚动（≤420 竖排）
  ⚠ 上述 5 块之间的间距靠 `.setup-panel__draft { gap: var(--space-22) }`（setup.css:413），--space-22 同样未定义 → gap 塌成 0，各卡边框互相贴死。

【第 5 层：叠加全屏页】SetupSeatEditorSheet（layer=nested + presentation=page，再盖一整屏）
  角色态：当前角色→新角色预览条 / 角色库 4 列网格（≤760 两列，全剧本角色，已在场置灰）/ 影响说明 / 底部 [取消 | 应用替换]（不吸底）
  昵称态：一个输入框 + 说明 + [取消 | 保存昵称]

【断点总览】760（候选栅格、伪装、详情各栏、角色库塌栏；分散在 6 个独立的 max-width:760 块里）、720（座位格 6→4、计数条 4→2）、560（三段控件与伪装格→单列）、420（座位格→3、底部按钮竖排）。iPad 竖屏 768px 高于所有断点，拿到的是完整桌面布局。
```

</details>

**🔴 high · 排版布局｜断点全部设在 760px 及以下，iPad 竖屏（768px）拿到桌面三栏布局，候选卡里的角色名被省略号吃掉**

- 证据：setup.css:29 `.setup-panel__candidates { grid-template-columns: repeat(3, ...) }`；setup.css:587-592 `.setup-candidate__roles { grid-template-columns: repeat(3, ...) }`；setup.css:611-618 `strong { overflow:hidden; text-overflow:ellipsis; white-space:nowrap }`；塌栏规则全在 setup.css:198 / 209 / 210 / 211 / 557 / 677 / 682 / 947 / 976，最大断点 760。768px 视口下 .setup-panel = min(1120, 768-48) = 720px → 候选卡 (720-32)/3 ≈ 229px → 卡内 padding 14 后 201px → 3 列角色格每格 ≈ 63px，扣内边距后文字宽约 45px（12px 字号 ≈ 3.7 个汉字）。
- 后果：iPad 竖屏是线下说书人单手持握最常见的姿势，而它恰好卡在 768 > 760，所有响应式规则一条都不生效。结果是「这个候选板子里到底有哪些角色」——选板子唯一真正要看的信息——被截断成「洗衣…」「共情…」。说书人只能横过来看或点进详情页再退回，开局前几分钟被玩家围着等，每退一次都要重新找刚才看到的那张卡。

**🔴 high · 排版布局｜tokens.css 里不存在 --space-22 / --space-36，导致草稿区卡片间距塌成 0、整页上下留白消失**

- 证据：setup.css:343 `.setup-panel { padding: clamp(var(--space-24), 4cqw, var(--space-36)) 0 72px; }`、setup.css:413 `.setup-panel__draft { gap: var(--space-22); }`、setup.css:232 同样引用 --space-22；tokens.css:42-53 只定义了 4/6/8/10/12/14/16/18/20/24/32/40，没有 22 和 36。含未定义自定义属性的声明属于 invalid at computed-value time，整条声明退回 unset → padding 变 0、grid gap 变 normal(0)。窄屏侥幸有 setup.css:563 的 `padding-top: var(--space-20)` 兜住上边距，>760px 连上边距都没有。
- 后果：这是用户说「排版乱」最直接的物理原因：角色面板、人数修正卡、校验清单、底部动作条四张带边框的卡零间距贴死，相邻边框叠成一条 2px 的假分隔线，看起来像一整块没做完的表格；同时内容顶住页头分隔线、底部顶到 iPad 手势条，最后一行「确认配板」按钮贴着屏幕边缘，暗光下单手持平板很容易误触到系统上划手势而不是按钮。

**🔴 high · 交互流程｜点开「AI配板建议」后没有返回草稿的出口，唯一逃生门是「采用为草稿」——而它会覆盖全部手改**

- 证据：SetupPanel.tsx:63 `const [showCandidates, setShowCandidates] = useState(false)`；SetupPanel.tsx:226 分支 `!effectiveDraft || showCandidates ? <SetupCandidateBrowser .../> : <button className="setup-panel__advice-entry" onClick={() => setShowCandidates(true)}>`；setShowCandidates(false) 全文件只出现在 SetupPanel.tsx:120（selectCandidateDraft 内部）；SetupCandidateBrowser.tsx:9-18 的 props 里没有任何 onCancel/onClose。列表态（SetupCandidateBrowser.tsx:203-258）也没有返回按钮，只有详情态才有（:166、:196）。
- 后果：说书人手动交换了 4 个座位后想「再看一眼 AI 原本推荐了什么」，点进去就回不来了——页面顶部变成候选列表，自己的草稿被顶到下面看不见，唯一能让列表消失的操作是「采用为草稿」，而 selectCandidateDraft 会用候选重建 draft（SetupPanel.tsx:112-116），四次手动交换全部作废且没有撤销。现场只能关掉整个全屏页再重进。

**🔴 high · 状态反馈｜校验失败项的类名 `is-fail` 在 CSS 里根本不存在（只写了 `.is-failed`），失败提示与普通灰字同色，还被单行省略号截断，且文案是肯定句**

- 证据：types.ts:22 `status: 'pass' | 'needs_choice' | 'fail' | 'review'`；SetupPanel.tsx:301 `className={`is-${check.status}`}` → 实际输出 `is-fail`；setup.css:164-167 只定义了 `.is-passed` / `.is-failed` / `.is-needs_choice` / `.is-review`，`.is-fail` 与 `.is-pass` 无任何样式。另 setup.css:542-546 `.setup-panel__checks li span { overflow:hidden; text-overflow:ellipsis; white-space:nowrap }`；setupRuleEvaluator.ts:127-138 的失败 summary 写作「座位已一一分配」「阵营人数符合已选开局修正」（陈述规则而非陈述违规），真正的数字在 `detail` 里（如「当前 …；目标 …」），而 SetupPanel.tsx:301 从不渲染 detail。
- 后果：最严重的一档（fail）反而没有红色，比它轻的 needs_choice 却是金色——严重度倒挂。加上文案是「阵营人数符合已选开局修正」这种肯定句、被截成一行、缺口数字（差几个外来者）永远不显示，说书人只看到右上角一个「需调整」和一行灰字，完全不知道要改什么。暗光下这行 13px 灰字（--text-muted #9ea6ad）在 warning-soft 底上对比度本来就低，等于把唯一的拦截信息藏起来了。

**🔴 high · 一致性｜配板座位格自绘按钮，hover 与已选状态样式完全相同，且选中态只有颜色没有勾选——同时绕过了共享 SeatButton**

- 证据：setup.css:136 `.setup-seat-grid button:hover, .setup-seat-grid button.is-selected { border-color: var(--accent-border); background: var(--accent-soft); }`（两者一模一样）；SetupPanel.tsx:239-249 手写 `<button>` 内含 `<span>{seatId}号</span><strong>{role.name}</strong>`；对比共享组件 SeatButton.tsx:20-21 选中时额外渲染 `.seat-button__check` 勾选角标，ui.css:69-71 的 hover(surface-hover/border-strong) 与 selected(2px accent-strong) 明确分开。ui-design-system.md:28-30 要求座位键「选中态必须同时显示勾选、描边和颜色」「不再把数字和『号』拆成两行」。setup.css:20 的人数按钮也有同一个 hover===selected 问题。
- 后果：交换角色要连点两个座位，第一次点完必须能一眼看出「我选中的是 7 号」。现在选中只是底色微金（accent-soft 仅 11% 不透明度），而触屏上 :hover 在点击后会粘住，于是刚点过的、真正选中的、以及手指路过的格子长得一模一样。暗光下说书人很容易把 7↔8 交换点成 7↔9，而配板一旦确认就写进本局身份，错了要走更正流程。

**🔴 high · 可发现性｜三段控件是「模式开关」，改变点击座位的后果，但解释文字被 display:none，座位格在三种模式下外观完全一致**

- 证据：SetupPanel.tsx:135-150 selectSeat 根据 seatAction 分别走「打开角色全屏页 / 打开昵称全屏页 / 两点交换」；SetupPanel.tsx:244 给座位加了 `is-action-${seatAction}` 类，但 setup.css 里只有 `.setup-seat-grid button.is-action-swap { cursor: grab }`（:134），is-action-role / is-action-nickname 无任何样式；唯一的说明行 `.setup-panel__seat-hint` 被 setup.css:448-453 设为 `display: none`。另 SetupPanel.tsx:241 `draggable={seatAction === 'swap'}` 与 setup.css:134 的 grab 光标是纯桌面拖拽提示，触屏上 HTML5 DnD 不生效。
- 后果：平板上没有 hover、没有光标、没有提示文字，三种模式下座位格长得完全一样，模式状态只由上方一枚描边按钮承载。说书人上一步刚用「更换角色」改完 3 号（selectSeat 后模式不复位，SetupPanel.tsx:177-181 也不清空 seatAction），下一步想交换 5、6 号，手指一点 5 号——直接弹出一整屏角色库。在玩家围观、要求「快点开始」的时候，这种「点下去出来的东西不是我要的」会反复发生，而且每次都要退出一层全屏页。

**🔴 high · 交互流程｜昵称立刻写入本局、角色只改草稿，两种提交语义被塞进同一组三段控件；「放弃草稿」撤不掉昵称**

- 证据：SetupPanel.tsx:183-187 saveSeatNickname → `dispatch({ type: 'update-seat-nickname' ... })` 直接改 session；SetupPanel.tsx:177-181 replaceSeatRole → 只 setDraft。两者由同一个 selectSeatAction（:171）切换、同一个座位网格触发。SetupSeatEditorSheet.tsx:55-58 的说明文案也自相矛盾：角色态写「选择后只改草稿；确认配板前不会影响本局」，昵称态只写「昵称只供说书人辨认」，从不提示已经生效。SetupPanel.tsx:306 的「放弃草稿」只做 setDraft(null)，对已 dispatch 的昵称无效。
- 后果：同一个入口、同一个网格、同一句「说书人草稿」标题下，一半操作是可回滚的草稿、一半是即时落库。说书人以为「反正没点确认配板，随便试」，改了昵称又点「放弃草稿」，结果角色回滚了、昵称留下了——本局玩家状态卡从此显示错误昵称，夜间叫人时叫错人名，只能再进一次配板页手改。

**🔴 high · 状态反馈｜已开局状态下点「确认调整」，恶魔伪装的修改会被静默丢弃，页面照常关闭**

- 证据：SetupPanel.tsx:195-219 confirmDraft：!started 分支同时 dispatch `set-setup-draft` + `confirm-setup`；started 分支（:201-216）只遍历 assignments 差异 dispatch `append-setup-change`，既不写 draft 也不写 demonBluffs，随后 :217-218 无条件 `setDraft(null); onOpenChange(false)`。而伪装数据来源是最后一条 setup_confirmed（setupSession.ts:14 `demonBluffs: setupEntry.setup.draft.demonBluffs...`）。若本次只改了伪装、没改角色，循环一次都不进，等于什么都没 dispatch。
- 后果：开局后临时改伪装是很常见的补救（比如恶魔要的身份已经被玩家自曝）。说书人改完三个下拉、点「确认调整」、页面关闭、没有任何报错——完全是成功的反馈。等到夜里给恶魔发伪装时才发现还是旧的三张，当场只能凭记忆口述，或者在玩家面前重开配板页翻找。

**🟡 medium · 交互流程｜「放弃草稿」是不可撤销的破坏性操作，却与主按钮并排、无二次确认，设计系统明令禁止**

- 证据：SetupPanel.tsx:303-308：`<Button variant="ghost" onClick={() => setDraft(null)}>放弃草稿</Button>` 与 `<Button variant="primary" ...>确认配板</Button>` 同在 .setup-panel__footer，setup.css:168 `display:flex; justify-content:flex-end; gap: var(--space-12)`（间距仅 12px）；setup.css:211 在 ≤420px 改为 `flex-direction: column-reverse` 让它直接压在主按钮上方且同为 100% 宽。ui-design-system.md:113 「禁止：状态仅靠颜色、危险动作紧邻主按钮、隐藏式长按入口」。
- 后果：两个按钮只差 12px、同样高度、都是文字按钮，唯一区别是 ghost 与 primary 的填充；在暗光下 ghost 按钮的边框几乎看不见。手指偏 1cm 就把整局的手动配板改动清空，而且没有二次确认、没有撤销。手机窄屏下它还被排到主按钮正上方，误触概率更高。

**🟡 medium · 视觉层级｜卡片套卡片到三、四层，超出设计系统「不超过两层」的硬性限制**

- 证据：伪装链路：`.setup-panel__role-panel`（setup.css:421-429，边框+渐变）→ `.setup-panel__bluff-card`（setup.css:465-471，边框+渐变，JSX 上确实嵌在 role-panel 内，SetupPanel.tsx:237 开、:281 闭，:259 是子节点）→ `.setup-panel__bluff-grid label`（setup.css:485-495，边框+径向渐变）→ `.setup-panel__bluff-grid small`（setup.css:524-537，又一枚带边框的胶囊）。三段控件链路：`.setup-panel__seat-action`（setup.css:441-447 边框+底色）→ `.setup-panel__seat-actions`（setup.css:141 边框+底色）→ 内部按钮。ui-design-system.md:111 「禁止：卡片套卡片超过两层」。
- 后果：每多一层边框就多一条在暗屏上发灰的细线，四层套下来「伪装建议」区域有 4 组同色描边，眼睛找不到哪一层是可点的。更要命的是层级与语义不符：恶魔伪装被物理嵌在「角色与座位」卡里，而人数修正是它的兄弟节点——说书人会以为伪装是座位网格的附属设置，而不是和配板并列、需要单独核对的一项。

**🟡 medium · 视觉层级｜一屏内三枚同权重 StatusBadge + 五套彩色卡片在抢焦点，真正的总状态「可确认 / 需调整」毫无视觉优势**

- 证据：同屏并存：SetupPanel.tsx:235「可确认/需调整」(success/warning)、:262「未在场」(info)、:286「已核对/待核对」(success/warning)，三者用同一个 StatusBadge 组件、同样尺寸。配色上金卡（setup.css:462 rule-card 金边）、青卡（:465-471 bluff-card 青边）、黄框校验清单（:295-301 warning-soft）、青色 advice-entry（:350-365）同屏。标题层级几乎没有差：setup.css:5 `h3 { font: 800 var(--type-section) }`=20px，setup.css:6 `h4 { font: 800 1.05rem }`≈16.8px，两者只差 3px 且同字重同字体。ui-design-system.md:5 规定「暖金＝当前焦点、冷青＝信息提示」，但这里金/青只是装饰性区分模块。
- 后果：说书人在配板页只需要回答一个问题：「现在能不能点确认」。现在这个答案（右上角一枚小徽标）和另外两枚长得一样的局部徽标混在一起，还要和五块彩色卡片争注意力。暗光下扫一眼分不清哪个金色是「有问题」哪个金色只是卡片装饰，只能逐块阅读，开局前的 30 秒被拖成 2 分钟。

**🟡 medium · 一致性｜同一份「座位号+角色名」有三套外观，同一条伪装理由在详情页是整段、在草稿页被压成 3 个字**

- 证据：三套角色组合外观：候选卡 SetupCandidateBrowser.tsx:248-250 + setup.css:587-619（3 列、12px 字、省略号）；详情页 SetupCandidateBrowser.tsx:174-176 + setup.css:748-782（6 列、58px 高、13px 字）；草稿 SetupPanel.tsx:239-249 + setup.css:131-139/437-440（6 列、74px 高、可拖拽）。伪装理由：详情页 SetupCandidateBrowser.tsx:187-192 完整渲染 `item.reason` 与 `item.risk`，草稿页 SetupPanel.tsx:276 经 setupPresentation.ts:1-9 的 compactBluffHint 压成「首夜线 / 长期线 / 可讲身份」等固定短语并做成小胶囊（setup.css:524-537）。
- 后果：说书人是在详情页读完「为什么建议伪装成图书管理员」才决定采用的，回到草稿页想复核时，那句理由变成了三个字的标签，原文再也找不到；同时同一份角色名单换了三种排布，脑子里要重新建立一次「几号是谁」的映射。线下带板时这类重复认知直接转化为叫错人。

**🟡 medium · 交互流程｜改一个座位的角色要 5 次点击 + 两层全屏页，且模式不复位；主动作还不吸底**

- 证据：路径：点「更换角色」(SetupPanel.tsx:254) → 点座位(:137 打开 nested page) → 在 SetupSeatEditorSheet.tsx:80-95 的全剧本角色栅格里滚动找角色 → 点「应用替换」(:103-107，位于长栅格末尾，无吸底) → 回到草稿页滚到底点「确认配板」(SetupPanel.tsx:307)。SetupSeatEditorSheet 用 `layer="nested" presentation="page"`（:65-67）整屏盖住整屏。返回后 seatAction 保持 'role'（replaceSeatRole 未复位）。底部动作条 setup.css:547-553 是普通块级元素；对比夜间工作台 NightActionBar.tsx:3/50 使用共享 StickyActionBar（ui.css:155-165 position:sticky）。
- 后果：5 次点击里有 2 次是「找按钮」而不是「做决定」，且两次都要滚到长列表底部才够得到主按钮——单手持平板时拇指要跨过半个屏幕。夜间工作台的确认按钮是吸底的，配板页不是，同一个 App 里两种手势记忆；说书人在配板页习惯性往下够，够到的是空白。

**🟡 medium · 一致性｜CSS 是三代样式层层覆盖的产物：同一选择器改写 3-4 次、6 个同断点媒体块、16 处脱离字号 Token 的硬编码、5 个已经没人用的死类**

- 证据：`.setup-panel` 在 setup.css:2 / 215 / 341 定义三次（padding 从 20px → 未定义 clamp）；`.setup-panel__draft` 在 :130 / 231 / 410 三次（从「有边框的卡」改成「透明容器」）；`.setup-panel__rule-card` 在 :151 / 234 / 454 / 462 四次；`max-width: 760px` 的媒体块散在 :198 / 557 / 677 / 682 / 947 / 976 六处，中间还夹着 720/560/420。字号硬编码 16 处（:6 1.05rem、:11 clamp(2rem,7cqw,4rem)、:73 1.02rem、:120 1rem、:270 1.04rem、:393/845/886 1.08rem、:480 1.18rem、:826 clamp(1rem,2cqw,1.15rem) 等），而 ui-design-system.md:12 规定「字号层级只使用 meta/label/body/section/page-title 五档 Token」。:111-112 有两处 !imp
- 后果：这是「乱」的结构性根因：任何一次视觉修复都要同时读三代规则才知道最终值，于是修 A 崩 B（--space-22/--space-36 两个空 Token 就是这样漏进来的）。对现场的直接后果是字号出现 12/13/16/16.3/16.6/17.3/18.9/64px 等至少 8 档，暗光下没有稳定的「这一档是标题、那一档是说明」的节奏，说书人每块都得重新聚焦阅读。

**🟡 medium · 信息架构｜开局面板把 12-15 行昵称录入挡在主任务前面，剧本名重复出现三次，而换剧本的控件最不显眼、开局后彻底消失**

- 证据：SetupStartPanel.tsx:91-109 渲染 7-15 行「号码 / 昵称 input / 经验 select」，桌面 3 列（setup.css:24）；hero 大标题「选择人数」font-size 到 4rem（setup.css:11），远超 tokens.css:9 `--type-page-title` 的 2.6rem 上限。剧本名三处重复：SetupPanel.tsx:222 页头 description、SetupStartPanel.tsx:46 hero 的 `<p>{scriptName}</p>`、:52-62 下拉本身；真正的切换控件是 setup.css:13-17 里一个夹着「开始配板后生效」小字的普通 select。SetupPanel.tsx:41 `activeScriptId = session.playerCount > 0 ? session.scriptId : setupScriptId` → 一旦开始配板，页面内再无任何入口改剧本。页头 description 在已开局时变成孤零零的「后续生效」四个字（SetupPanel.tsx:222）。
- 后果：线下开局时人还没坐齐、昵称还在问，说书人真正想立刻做的是「选 12 人、看 AI 给什么板子」，却先撞上一屏 15 个输入框和一个 64px 的「选择人数」大字。剧本名被念了三遍，唯一能改的那个下拉却最小；等发现选错剧本时人数已经录完，只能整页退出重来。页头那句无主语的「后续生效」也说不清是「谁」后续生效。

**⚪ low · 状态反馈｜人数修正提醒被重复渲染两次，且通过项也套着金色警告样式**

- 证据：SetupPanel.tsx:85 `modifierReminders = legality.filter(check => check.id.startsWith('modifier-'))`（不过滤状态，pass 也进来），:295-297 渲染进人数修正卡；SetupPanel.tsx:86 `visibleChecks = legality.filter(check => check.status === 'fail' || 'needs_choice')` 又会把同一条 modifier-* 收进 :299-302 的校验清单。样式上 setup.css:276-284 给 modifier 提醒加了 `border: 1px solid var(--warning-border); background: var(--warning-soft)`，而 setupRuleEvaluator.ts:165-172 产出的 pass 项（如「洗衣妇：+1 外来者」）也走这套金色。
- 后果：同一句「缺少配板修正：X」在同屏出现两次、两种样式，说书人会以为是两个不同的问题去找两次；而已经核对通过的修正项顶着警告金，又让人误以为还有事没做。核对人数本来是配板里最需要「一次看完、确认无误」的步骤，现在这块的信噪比最差。

### 结束/复盘/历史：GameEndSheet（结束归档 + 历史复盘双模式全屏页）、GameReviewPanel、GameAIReviewP

<details><summary>界面区块结构（点开）</summary>

```
【入口层 · 首页 Dashboard】
  dashboard__grid（两栏卡片）
  ├─ 卡片「最近记录」…… 卡头右侧塞了一颗 compact 按钮「日记」= TimelineHistorySheet 的 trigger（Dashboard.tsx:198-208）
  └─ 卡片「结束对局」（红调边框 + StatusBadge「危险动作」）
       ├─ 按钮「结束对局」 → 打开 GameEndSheet(mode=end)
       └─ 按钮「历史复盘」 → 打开同一个 GameEndSheet(mode=review)
  ⇒ 同一张全屏页有 2 个外部入口 + 页内 tab + 页内 ghost 按钮 + 空态按钮，共 4 条路能切换模式。

════════ A. GameEndSheet（presentation="page"，铺满 100vw/100dvh，无吸底动作条）════════
[固定页头 sheet-header ≈92px 高]  标题「结束与复盘」(1.5rem) + 副标题「归档 · 历史复盘 · 重置」 …… 右侧 44px 圆形 ✕
[sheet-body：整页唯一的外层滚动容器]
 └ .game-end（居中 ≤1180px；上下 padding 因未定义 token 实际为 0，内容紧贴页头分割线）
    ├ ① HERO 卡（128~172px 高，金色渐变）
    │    左：小标签「本局收尾/历史对局」→ 巨型标题「结束对局/复盘系统」(最大 67px) → 一行状态「暂不声明 · 待保存」
    │    右：两段开关 [保存重置 | 历史复盘]（48px 高，仅靠底色区分选中）
    │    ≤760px 容器：改为纵向堆叠；≤620px：开关变整行满宽
    │
    ├ ② 主体（二选一，由 HERO 里的开关决定）
    │
    │  ── 模式 end：一张大卡 .game-end__finish-card，内部三段带 ①②③ 圆形序号、用细横线分隔 ──
    │     步骤1「声明胜方」：3 个 88px 高的选项卡（善良/邪恶/暂不声明）
    │                       宽屏 3 列；≤760px 容器 → 竖着排成 3 行（首屏立刻被吃掉 ~300px）
    │     步骤2「保存本局」：说明文 → 「归档位置」胶囊（因缺 token 实际渲染成直角方块）
    │                       → 3 格数字 [玩家|记录|死亡] → [保存本局(金/主)] [导出备份(次)]
    │                       → 全页唯一的 notice 提示行（条件显隐，会顶开下方内容）
    │     步骤3「重置游戏」（红调）：警示条 → 「清空 / 保留」两格对照卡（padding 归零，文字贴边）
    │                       → 复选框「我已保存本局，确认重置游戏」→ [重置游戏(红，默认禁用)] …… [查看复盘(ghost)]
    │                       ≤760px 容器：两颗按钮都变满宽，红色重置直接压在「查看复盘」正上方
    │     步骤3 之后：一条青色「本局记录 N」摘要横条（数字口径与步骤2 的「记录」不一致）
    │
    │  ── 模式 review：.game-review 两栏（左 260~360px / 右 自适应）──
    │     左栏：日期下拉筛选 + 归档列表（每张卡 ≥104px；宽屏内部独立滚动 max-height:100dvh-260px）
    │     右栏 .game-review__detail（≥520px 的大卡，内部再套 4 层卡）：
    │           胜方横条 → 4 格数字（塞进写死 3 列的栅格，第 4 格「投票」孤零零掉到第二行）
    │           → 4 格 dl 指标 → 「当局日志」列表（第 3 个独立滚动区，max-height 420px/48dvh）
    │           → GameAIReviewPanel（永远展开、不可折叠）：
    │                AI 声明行 → 2 张评价卡 → 关键转折 → 玩家评分（第 4 个独立滚动区，逐人分数+「锐评」）→ 优先回看
    │           → 最底部才是唯一动作 [导出这局]
    │     ≤860px 容器：变单列，且列表 max-height 解除 → 全部归档卡（两列）整段堆在正文之前
    │     ≤620px 容器：归档列表变单列 → N×104px 全部展开，详情被推到 2~3 屏之下
    │     无归档时：整块替换为居中空态（图标 + 文案 + 按钮「去归档本局」）
    │
    └ 无吸底操作条：主动作「保存本局」在页面中部，危险动作「重置游戏」在页面底部，都要靠滚动够到。

════════ B. TimelineHistorySheet「日记」（同为 100vw 全屏页，与 A 并列不嵌套）════════
[sheet-header]  标题会在三态间变脸：「日记」/「记录详情」/「更正记录」；副标题同步换成条数、昼夜标签或免责声明
[sheet-body → .timeline-history（居中 ≤920px）]
  态1 列表（默认）
    ├ 筛选区：3 个并排下拉 [昼夜|玩家|类型]（标签 11.2px）；≤560px 媒体查询 → 竖排 3 行
    ├ [清除筛选] ghost（仅有筛选时出现，出现即把下方整段推下去）
    ├ notice 成功条（条件显隐，且只在本态渲染）
    └ 按「第N夜/第N天」分组 → 每组标题带条数 → 记录行（58px 高：摘要 + 时间·类型 + 状态徽标 + 17px 图标）
         图标是铅笔 = 点了直接进【态3 表单】；图标是箭头 = 点了进【态2 详情】——两种行为在暗光下几乎看不出差别
    └ 无结果时：一条虚线框「没有符合筛选的记录。」
  态2 详情（点了不可更正的记录）
    ← 「全部记录」返回链接 → 详情卡（昼夜·类型 + 摘要 + 状态徽标 + 要点列表 + 满宽跳转按钮「回白天改票型」等）
    → 更正链（≥2 条时才出现）→ 黄色说明条（条件显隐）
  态3 更正表单（点了可更正的记录，直接跳过详情）
    ← 「返回日记」→ 说明条（和页头副标题重复）→ 座位多选（6 列，≤560px 变 4 列）
    → 3~4 个 textarea（选择/结果/告知/更正原因）
    → 自造吸底条 [取消 .7fr | 确认追加 1.3fr]（未使用共享 StickyActionBar）
    ✕ 或 ESC 关闭 = 静默丢弃整张表单，无二次确认
```

</details>

**🔴 high · 信息架构｜一个页面同时承载「不可逆收尾」和「随便翻的历史阅读器」，性质相反的两件事共用页头和 tab**

- 证据：/Users/zhaofen2/Desktop/botc/AI-The-Storyteller-of-the-Blood-Stained-Clock-Tower/src/features/game-end/GameEndSheet.tsx:191-206（title="结束与复盘"、description="归档 · 历史复盘 · 重置"，hero 内 role="tablist" 两段切换）；同文件 :294-300 用三元表达式在同一位置切换「三步收尾流程」与「GameReviewPanel」；/Users/.../src/App.tsx:67 单个 GameEndSheet 承担两种用途
- 后果：说书人按「结束对局」时是在做一件不可逆的事（清空全场状态）。可此刻屏幕最显眼的位置放着一个把他带去阅读模式的 tab，而阅读模式又是散场后几分钟才用的低频功能。现场收尾时间紧、玩家在旁边催，误点 tab 会让已经填好的胜方选择和滚动位置全部丢失语境，回来还要重新找到第 3 步。

**🔴 high · 排版布局｜页面纵向内边距被未定义 token 整条打掉，危险区的对照卡文字贴边、状态胶囊变方块**

- 证据：/Users/.../src/features/game-end/game-end.css:758 `padding: clamp(var(--space-24), 4vw, var(--space-40)) 0 var(--space-48)`；:287 `padding: var(--space-14) clamp(var(--space-16), 2cqw, var(--space-22))`；:342 `border-radius: var(--radius-pill)`；:31 `clamp(var(--space-24), 3.8cqw, var(--space-44))`。全仓库 grep 确认 --space-48 / --space-44 / --space-22 / --radius-pill 均未定义（tokens.css 只到 --space-40，圆角只有 sm/md/lg/xl）。含未定义变量的声明属于 invalid at computed-value time，padding 会回落到初始值 0，而不是回落到 :15 那条旧规则
- 后果：实际渲染结果是：整页上下 padding 为 0（首张卡紧贴页头分割线、最后一块紧贴屏幕底边）、第 3 步「清空 / 保留」两张对照卡完全没有内边距（文字压在描边上）、「归档位置」胶囊变成直角方块。这三处恰好都在「我到底要销毁什么」这个判断上。暗光下贴边的小字最难读，说书人会跳过不读直接勾确认框。

**🔴 high · 视觉层级｜标题层级倒挂：屏幕上最大的字（最高 67px）是一句和页头重复的废话**

- 证据：/Users/.../src/components/ui/ui.css:146 页头 `.sheet-header h2 { font: 800 1.5rem }` 显示「结束与复盘」；/Users/.../src/features/game-end/game-end.css:52 `font: 900 clamp(2.7rem, 8cqw, 5rem)`、:384-386 `.game-end__hero--compact h3 { font-size: clamp(2.35rem, 6cqw, 4.2rem) }` 显示「结束对局」；而真正要读的步骤标题 :74-80 只有 `var(--type-section)`=20px，胜方选项文字 :119 仅 1.08rem。ui-design-system.md:12 规定字号只用 5 档 token，:9 的 --type-page-title 上限是 2.6rem
- 后果：从页头到 hero 的三行字（结束与复盘 / 本局收尾 / 结束对局）说的是同一件事，却占掉 iPad 竖屏首屏约 230px。真正需要注意力的「声明胜方」三选一和「保存本局」按钮反而是全页最小的元素。单手举着平板、被一圈玩家看着时，视线会先被 67px 的大字抓走，再往下找操作，每次进入都要重新扫一遍。

**🔴 high · 排版布局｜复盘详情里四层卡片套卡片 + 四个独立滚动区嵌套，直接违反设计系统禁令**

- 证据：卡片层级：/Users/.../src/features/game-end/game-end.css:570-576 `.game-review__detail` 有边框背景 → :804-811 `.game-ai-review` 又一层边框背景 → :924-934 `.game-ai-review__players article` 第三层 → :986-993 `.game-ai-review__roast` 第四层。滚动层级：/Users/.../src/components/ui/ui.css:151 `.sheet-body{overflow:auto}` → game-end.css:564-569 `.game-review__list{max-height:calc(100dvh - 260px);overflow:auto}` → :657-661 `.game-review__timeline{max-height:min(420px,48dvh);overflow:auto}` → :917-919 `.game-ai-review__players{max-height:420px;overflo
- 后果：四层描边在低光屏幕上会糊成一片灰框，看不出哪块是一个整体。更糟的是四个嵌套滚动区：手指在平板上滑动时，命中的是哪一层完全不可预测——想翻整页却只滚动了内层日志，说书人会以为页面卡死，在玩家注视下反复乱划。

**🔴 high · 排版布局｜复盘模式在竖屏平板上把归档列表整段堆在正文之前，要滚两三屏才看到内容**

- 证据：/Users/.../src/features/game-end/game-end.css:530-535 宽屏为 `minmax(260px,360px) minmax(0,1fr)` 双栏；:707-720 `@container (max-width: 860px)` 把 `.game-review` 压成单列，同时 `.game-review__list { max-height: none; grid-template-columns: repeat(2, minmax(0,1fr)) }` 解除高度限制；:722-735 `@container (max-width: 620px)` 再压成单列。列表项 :577-585 `min-height: 104px`。iPad 竖屏 768px 时 .game-end 容器约 690px，已落进 860 分支
- 后果：同一台 iPad，横屏是「左列表右详情」的正常复盘台；一转成竖屏，20 局归档就变成约 1000~2000px 高的卡片墙铺在最上面，复盘内容被推到看不见的地方。散场后说书人多半是竖着单手拿平板给玩家讲解，恰好落在最坏的那个方向上。

**🔴 high · 状态反馈｜提示信息只挂在「保存」这一步的 DOM 里，重置失败和导出成功的反馈实际看不到**

- 证据：/Users/.../src/features/game-end/GameEndSheet.tsx:256 `{notice ? <p className="game-end__notice" role="status">…}` 只写在第 2 步内部；但 notice 会被 :172 `setNotice('归档校验失败，当前局未重置')`（第 3 步的失败）、:148 `setNotice('归档JSON已导出')`、:85 `setNotice('本地后端不可用，已显示本机归档')` 写入。GameReviewPanel.tsx 全文没有渲染 notice 的位置
- 后果：点第 3 步的「重置游戏」失败时，唯一的错误说明出现在几百像素以上、多半已滚出视野的第 2 步里——说书人只会看到「什么都没发生」，于是再点一次。在复盘模式导出 JSON 则完全静默：不知道文件到底存没存下来，只能反复点导出，堆出一串重复文件。

**🔴 high · 状态反馈｜「已保存」是一次性布尔量，不跟踪脏数据，会对着过期归档说「本局已保存」**

- 证据：/Users/.../src/features/game-end/GameEndSheet.tsx:67 `const archiveCreated = Boolean(archiveRecord)`；:183 `if (archiveRecord) return \`已保存：${storageLabel(archiveRecordMode)}\``；:270 `{archiveCreated ? '本局已保存' : '请先保存本局'}`；:283 重置按钮的 disabled 条件也只看 archiveCreated。session 变化后没有任何比对逻辑
- 后果：典型现场：第 5 天先保存了一次，玩家又打了两轮，说书人回来看到「本局已保存」和绿灯状态的重置按钮，勾确认框、重置——最后两轮记录连同投票链一起消失，而界面从头到尾都在告诉他已经保存好了。归档是这个工具唯一的数据出口，这一条会直接毁掉一整局的复盘素材。

**🔴 high · 交互流程｜危险动作的确认勾选状态跨开关持久保留，重开页面时重置按钮已经是可点状态**

- 证据：/Users/.../src/features/game-end/GameEndSheet.tsx:60 `const [resetAcknowledged, setResetAcknowledged] = useState(false)`；:89-95 的 open effect 只重置 `mode / busy / notice`，没有重置 resetAcknowledged 与 winner；:283 `disabled={!archiveCreated || !resetAcknowledged || busy}`
- 后果：说书人勾了「我已保存本局，确认重置游戏」又临时改主意关掉页面，过一会儿为了查个记录再打开——复选框仍是勾着的，红色重置按钮已经解除禁用。原本设计成「两步确认」的安全阀退化成一次点击，而这一次点击的代价是整局清空。

**🔴 high · 交互流程｜「导出备份」会静默创建归档并顺带解锁重置按钮，语义与按钮名不符**

- 证据：/Users/.../src/features/game-end/GameEndSheet.tsx:141-149 `async function exportArchive(record = archiveRecord ?? selectedArchive) { let target = record; if (!target) { target = await saveCurrentGame() } … }`；:254 次要按钮「导出备份」调用它；saveCurrentGame（:112-139）会写 archiveRecord，而 :283 的重置按钮 disabled 只看 archiveCreated
- 后果：一个写着「导出」的次级按钮，实际执行了「保存」这个有副作用的主动作，并把旁边那颗危险按钮点亮。说书人以为自己只是导了个文件，界面状态却已经从「待保存」跳到「已保存」。设计系统 :70 的原则是「确认入口唯一」，这里等于开了第二个隐藏的确认入口。

**🔴 high · 交互流程｜日记里「可更正」的记录点一下直奔编辑表单，永远看不到它的详情**

- 证据：/Users/.../src/features/history/TimelineHistorySheet.tsx:115-124 `function openRecord(entry) { if (entry.canCorrect && isEditableEntry(entry.source)) { setSelectedEntryId(null); setEditingEntryId(entry.id); return } … setSelectedEntryId(entry.id) }`；两种记录在列表里的差别只有 :188 的 17px 铅笔/箭头图标和 /Users/.../src/features/history/history.css:14-15 的一圈 accent 描边
- 后果：夜间行动和白天技能恰恰是最需要「先看清原记录再决定改不改」的两类，却是唯一看不到详情页的两类——点下去直接进表单。暗光下 17px 的铅笔和箭头几乎分不清，说书人本想核对一下 3 号的验人结果，结果打开的是可写表单，一个误触就可能覆盖掉正确内容。

**🔴 high · 状态反馈｜更正成功的提示语永远不会显示：写 notice 的同时就切走了渲染 notice 的那个视图**

- 证据：/Users/.../src/features/history/TimelineHistorySheet.tsx:110-113 `setEditingEntryId(null); setSelectedEntryId(id); setNotice('更正已追加到原昼夜。')`；:177 `{notice ? <p className="timeline-history__notice" role="status">…}` 只写在列表分支里；:154 selectedEntryId 非空时渲染的是 TimelineHistoryDetail，列表分支不执行
- 后果：追加更正是这个模块唯一的写操作，做完之后连一句「已追加」都看不到，只能靠详情页上一个小徽标自行推断。现场更正往往发生在玩家质疑「你刚才是不是记错了」的时刻，说书人需要一个明确回执才敢抬头说「改好了」，现在只能再返回列表翻一遍确认。

**🔴 high · 一致性｜三套 BEM 命名在同一个面板里混用，且 CSS 里 14 个类名已经没有对应 DOM**

- 证据：/Users/.../src/features/game-end/GameReviewPanel.tsx:96 用 `game-end__stats game-end__stats--compact`、:109 借用 `game-ai-review__section-title`、:123 借用 `game-end__actions`，而它自己的命名空间是 `game-review__`。死代码（在全部 tsx 中无引用）：game-end__step / __grid / __side / __column / __control-shell / __control-main / __control-side / __end-layout / __review-list / __review-score / __signal-list / __archive-list / __save-panel / __summary-card，对应 game-end.css:61-65、128-133、166-187、199-244、414-437、445-449、522-529 等约 1/3 篇幅
- 后果：这是「排版混乱」的根因：没有抽象出可复用的区块，每次改版就贴一层新前缀、留一层旧样式（文件里还留着 `/* V3.12 */`、`/* V3.12.1 */`、`/* V3.13 */` 三代注释各自覆盖同一批选择器）。后果是任何一次微调都可能命中三代规则中的某一层，改一个间距要改三处，也正是下面那条「4 个格子塞进 3 列栅格」这类事故的温床。

**🔴 high · 排版布局｜共享的统计栅格被写死 3 列，复盘详情却塞进 4 个格子，「投票」孤零零掉到第二行**

- 证据：/Users/.../src/features/game-end/game-end.css:519-521 `.game-end__stats--compact { grid-template-columns: repeat(3, minmax(0, 1fr)) }`；/Users/.../src/features/game-end/GameEndSheet.tsx:247-251 传入 3 格（玩家/记录/死亡），而 /Users/.../src/features/game-end/GameReviewPanel.tsx:96-101 往同一个类里传入 4 格（玩家/记录/死亡/投票）
- 后果：复盘页最上方的关键数字区永远是「3 个一行 + 1 个孤儿」的破相排版，且孤儿正好是投票数——复盘时最常被玩家追问的数字。给玩家看屏幕时这种明显的错位会直接被读成「这软件没做好」，削弱说书人的判定权威。

**🟡 medium · 一致性｜字号、字重、圆角大面积绕开 token，与 ui-design-system.md 的硬性规定冲突**

- 证据：ui-design-system.md:12 规定只用 meta/label/body/section/page-title 五档、:13 规定圆角只用 8/14/22。实际：/Users/.../src/features/game-end/game-end.css 出现 1.05rem(:961)、1.08rem(:119)、1.1rem(:875)、1.35rem(:163)、2rem(:509)、clamp(1.35rem,3cqw,2rem)(:632)、clamp(2.35rem,6cqw,4.2rem)(:385)、clamp(2.7rem,8cqw,5rem)(:52) 等十余个一次性字号；:403 `border-radius: calc(var(--radius-lg) - 5px)` = 11px。/Users/.../src/features/history/history.css 通篇裸值：.7rem(:3,:10,:19,:36)、.72rem(:28,:47)、.74rem(:10)、.8rem(:37,:49)、.82rem(:6,:23,:33,:38)、.84rem(:30,:46)、.86
- 后果：.7rem ≈ 11.2px 的筛选标签在低光环境下基本不可读，而 tokens.css 里 --type-meta 的下限就是 12px；750/850 这类字重在非可变字体上会被各端分别取整成 700 或 800，同一个界面在 iPad 和 Windows 平板上粗细不一致。结果是「同一类信息在不同区块看起来不是同一类」，说书人无法靠字号快速判断哪行是标题、哪行是补充说明，只能逐字读。

**🟡 medium · 状态反馈｜选中态只靠底色，tablist 的可访问性语义也是残缺的**

- 证据：/Users/.../src/features/game-end/GameEndSheet.tsx:203-206 外层 `role="tablist"`，内部却是裸 button，只有 `className={mode === 'end' ? 'is-active' : ''}`，没有 role="tab"、没有 aria-selected；对应 /Users/.../src/features/game-end/game-end.css:409-413 的 is-active 仅改 background + color + inset ring。胜方选项 :105-111 把 `button:hover` 与 `button.is-selected` 写成完全相同的一条规则，选中态没有任何勾选图标。ui-design-system.md:55/:82/:113 三处要求状态必须同时用图标、文字和颜色
- 后果：「已选善良获胜」和「手指刚划过善良获胜」在屏幕上长得一模一样。低光下金色描边和悬停高亮的差别几乎为零，说书人无法在保存前确认自己选的是哪一方，只能盲存后再回头核对；胜方一旦存错，整份归档的复盘结论就是错的。

**🟡 medium · 信息架构｜关键数字口径不一致：同屏两个都叫「记录」的数字不相等，且摘要放在危险动作之后**

- 证据：/Users/.../src/features/game-end/GameEndSheet.tsx:249 第 2 步显示 `summary.entries.length`（全部有效条目）；:289-293 页面最底部的摘要横条显示 `summary.nightCount + summary.dayCount + summary.voteCount`（只含三类，见 /Users/.../src/services/archive/archiveService.ts:70-72，漏掉 execution、player_state、role_change、setup 等），标签写作「本局记录」
- 后果：这个摘要是「我到底要保存/销毁多少东西」的唯一量化依据，却既排在第 3 步的重置动作之后（决策信息在动作下方），又和第 2 步给出不同的数字。说书人看到「记录 42」和「本局记录 31」，第一反应是有 11 条丢了，于是不敢重置，反复退出来核对——在散场收桌子的两分钟里这是致命的犹豫。

**🟡 medium · 一致性｜更正流程的确认语义与规范不符，同一句免责声明重复三遍，且表单可被静默丢弃**

- 证据：ui-design-system.md:81 要求「更正态对应『暂不更正 / 确认更正』」，实际为 `取消 / 确认追加`（/Users/.../src/features/history/TimelineHistoryCorrectionEditor.tsx:96-97 与 :160-161）。「原记录保留」在 /Users/.../src/features/history/TimelineHistorySheet.tsx:134 的 Sheet 副标题、Editor:81 与 :147 的 intro 里各说一遍。/Users/.../src/features/history/TimelineHistorySheet.tsx:91-99 `onOpenChange(false)` 无条件清空 filters/selected/editing/notice，配合 /Users/.../src/components/ui/Sheet.tsx:39-41 的 ✕ 与 Radix 的 ESC，填了一半的更正表单会被直接丢弃，没有 ui-design-system.md:79 要求的页内确认条
- 后果：「确认追加」不告诉说书人追加的是什么、会不会发给玩家，而唯一解释这件事的那句话在 100px 内出现了两遍——真正需要区分的信息被自己的重复淹没了。更要命的是误触 ✕ 或 ESC 会让刚敲完的更正原因和结果凭空消失，而现场重打一遍中文长句需要低头十几秒。

**🟡 medium · 一致性｜两套吸底动作条并存，而最需要吸底的收尾页反而一条都没有**

- 证据：共享组件 StickyActionBar 存在于 /Users/.../src/components/ui/StickyActionBar.tsx 且样式在 ui.css:155-165（min-height 76px、含 safe-area 内边距）；/Users/.../src/features/history/history.css:52 却自造了一条 `.timeline-correction__actions { position: sticky; bottom: 0; grid-template-columns: .7fr 1.3fr; backdrop-filter: blur(12px) }`；而 GameEndSheet 完全没有吸底条——主动作在 /Users/.../src/features/game-end/GameEndSheet.tsx:252-255 页面中部，危险动作在 :282-285 页面底部，复盘模式的唯一动作在 GameReviewPanel.tsx:123-125 整个 AI 面板之后
- 后果：同一个产品里，更正表单的确认键固定在拇指位置，收尾页的保存键却要靠滚动去找，复盘页的导出键埋在几屏 AI 文字之下。单手持平板时拇指的可达区域是固定的，动作条位置不固定意味着每个页面都要重新找一次落点；两条自制/缺失的吸底条也让「.7fr/1.3fr」和「76px」两种按钮比例在相邻页面里打架。

**🟡 medium · 信息密度｜AI 复盘面板默认全展开且不可折叠，把逐人评分和「锐评」直接摊在会被围观的屏幕上**

- 证据：/Users/.../src/features/game-end/GameReviewPanel.tsx:122 无条件渲染 `<GameAIReviewPanel archive={selectedArchive} />`，没有折叠或开关；/Users/.../src/features/game-end/GameAIReviewPanel.tsx:57-80 逐个玩家渲染座位号、昵称、分数和 `player.roast`（「锐评」），:82-96 再列一份「优先回看」排名；样式上玩家评分区 game-end.css:917-923 是两列 420px 高的滚动网格，之后才轮到 GameReviewPanel.tsx:124 的「导出这局」
- 后果：复盘时平板通常是横在桌上给一圈玩家看的。一打开归档，屏幕上就是「几号几分 + 一句锐评」的公开排行榜，而这本是给说书人自己参考的草稿（面板自己都标注了「非客观评分」）。当场翻车的社交风险很高，说书人却没有任何折叠或隐藏的开关，只能靠飞快下滑躲过去——而下滑还要经过一个 420px 的嵌套滚动区。

**🟡 medium · 排版布局｜窄屏收窄边距的容器查询是死代码：容器查询作用不到容器自身，也作用不到页头**

- 证据：/Users/.../src/features/game-end/game-end.css:18 `.game-end { container-type: inline-size }`，:788-796 却写 `@container (max-width: 620px) { .game-end { --game-end-page-gutter: clamp(16px,5vw,24px); --game-end-card-pad: clamp(20px,5vw,26px) } .sheet-content--game-end .sheet-header { padding-inline: clamp(20px,5vw,28px) } }`。元素不能查询自身尺寸，.sheet-header 也不是 .game-end 的后代；全项目 container-type 只出现在 game-end.css:18、dashboard.css:6、day-workbench.css:1 与 :157、identity-deal.css:18，.game-end 之上没有任何祖先容器，该查询恒为 false。同时 history.css:56
- 后果：手机或小平板上本该收窄到 16~24px 的页面留白，实际仍按 clamp(28px,5vw,64px) 执行，页头留白仍是 clamp(32px,5vw,72px)——两侧各白白吃掉十几到几十像素，本来就要竖排的胜方三选一和步骤内容被挤得更窄更长。更麻烦的是「改了没生效」：下一个人调这里会以为是自己写错，继续往上叠新规则，文件再厚一层。

**🟡 medium · 一致性｜同一个功能有四个名字，入口按钮还不带条数**

- 证据：「最近记录」/Users/.../src/features/dashboard/Dashboard.tsx:200；「日记」按钮与 Sheet 标题 /Users/.../src/features/history/TimelineHistorySheet.tsx:147 与 :132；「本局记录」/Users/.../src/features/game-end/GameEndSheet.tsx:290；「当局日志」/Users/.../src/features/game-end/GameReviewPanel.tsx:110。ui-design-system.md:92 规定「页头常驻的是『本局记录 N』入口」，且该入口现在挂在卡片头部而非页头，标签里也没有 N
- 后果：玩家问「你刚才第二夜记的是什么」时，说书人得先在脑子里把「日记 = 最近记录 = 本局记录 = 当局日志」翻译一遍，才知道要点哪个按钮。入口不带条数也意味着无法一眼判断这局到底记了多少条、值不值得点进去，规范里那条「本局记录 N」的设计意图完全落空。

**🟡 medium · 信息密度｜iPad 竖屏首屏放不下第一步：巨型 hero 加上被迫单列的胜方三选一，主按钮已在屏幕之外**

- 证据：页头约 92px（由 /Users/.../src/features/game-end/game-end.css:13 `min-height: calc(100dvh - 92px)` 反推）+ hero :380-383 `min-height:128px` 与 :766-768 `padding-block: clamp(34px,4cqw,52px)`；:367-373 在 `@container (max-width: 760px)` 下把 `.game-end__winner-grid` 压成 1 列，每颗 :95 `min-height: 88px`；:736-738 在 ≤620px 时连步骤序号列（:464 `grid-template-columns: 44px minmax(0,1fr)`）也塌成单列。iPad 竖屏 768px 时 .game-end 容器约 690px，落在 760 分支内
- 后果：横屏时三个胜方选项并排、一屏能看到「选胜方 + 保存」两步；一转成竖屏，三个选项竖成 3 行近 300px，保存按钮被推出首屏，重置更要滚两屏。同一套流程在同一台设备上的步数随握持方向变化，说书人无法形成肌肉记忆，每次收尾都要重新找路。

### design-system

<details><summary>界面区块结构（点开）</summary>

```
整体样式架构（自上而下 4 层，无构建期约束）：

L0 全局重置 `src/index.css`（9 行，7 规则）——只做 `box-sizing`/`font: inherit`/`::selection`，无排版原语。

L1 设计令牌 `src/styles/tokens.css`（61 行，单个 `:root` 块，48 个变量）：字体 2 + 字号 6 + 行高 1 + 表面色 6 + 文字色 5 + 边框 2 + 语义色 20 + 间距 12（4/6/8/10/12/14/16/18/20/24/32/40）+ 圆角 4 + 触控 1 + 阴影 2。**没有 elevation 分级、没有 layout/容器宽度、没有 grid 列数、没有 z-index、没有断点、没有动效时长 token**。其中 4 个（`--input-bg`/`--shadow-panel`/`--text-placeholder`/`--type-action`）全仓 0 引用。

L2 共享组件层 `src/components/ui/`（6 个 tsx + 单一 `ui.css` 180 行 / 63 规则）：Button（32 文件引用）、StatusBadge（18）、Sheet（17）、SeatButton（6）、RoleDisc（5）、StickyActionBar（**仅 1**）。层内只覆盖「按钮 / 徽标 / 座位键 / 角色盘 / 浮层壳 / 吸底栏」六种原子，**没有 Card/Panel、PageShell、SectionHeading、FormField(Input/Select/Textarea)、EmptyState、Chip、Grid 任何一种布局或表单原语**。

L3 特性样式层 12 个 feature css（合计 6348 行 / 1770 规则）：dashboard 1741、game-end 1045、setup 980、night-workbench 772、ai-settings 597、identity-deal 373、day-workbench 270，其余 <110。全部经 `import './x.css'` 以**全局非模块化**方式注入（`src/main.tsx:3` 起共 23 处 import，无 CSS Modules / 无 scoped / 无 PostCSS 作用域插件），因此 L3 与 L2 处于同一层叠上下文，且**加载顺序 = 组件首次挂载顺序 = 用户导航路径**。

选择器规模：506 个不同顶层类名；层级深度分布 1 层 730、2 层 547、3 层 130、4 层 36、5 层 8、6 层 6。

响应式：`@media` 20 条 + `@container` 50 条，共 16 个不同断点（420/460/461/500/520/560/620/621/640/720/760/860/900/1024/1180/1280）；命名容器仅 3 个（`app-frame`、`workbench`、`night-content`）、`container-type` 声明仅 8 处，但 `cqw` 单位在 8 个无自有容器的文件中使用（含 tokens.css 自身）。

页面骨架：不存在统一 PageShell —— 55 处各自书写 `width: min(Npx, 100%)`，共 26 个互不相同的内容宽度；188 条 `grid-template-columns`、66 个不同取值；36 处各写 `padding: clamp(...)` 页面留白。
```

</details>

**🔴 high · 排版布局｜13 个从未定义的 token 被引用，24 处声明在计算期被整条丢弃（页面 padding 直接塌成 0）**

- 证据：tokens.css 只定义 space-4/6/8/10/12/14/16/18/20/24/32/40 与 radius-sm/md/lg/xl。全仓引用了但无处定义的变量共 13 个：--space-2/7/9/22/26/28/30/34/36/44/48、--radius-pill、--text。24 个使用点：ai-settings.css:4（.ai-settings-panel 根容器 padding，clamp 内含 --space-34）、:19（.ai-settings-connection padding，--space-26）、:67、:87、:121、:128、:412、:434、:516；dashboard.css:119（.dashboard__player-empty padding）、:332（.dashboard-player-seat gap）、:1260、:1349、:1695；game-end.css:31（.game-end 卡片 padding，--space-44）、:287、:342、:758；setup.css:232、:343、:413；day-workbench.cs
- 后果：AI 设置页、首页玩家空态、结算页卡片在真机上会出现「内容贴边、文字顶到卡片边框」的塌陷，而开发者在源码里明明写了 clamp 留白，于是把它当成「响应式没调好」继续加 @media 覆盖——这正是排版混乱不断复发却查不出原因的第一根因。线下主持时说书人低头快速扫读，贴边文字与丢失的间距会直接拖慢座位/状态的识别速度。

**🔴 high · 一致性｜dashboard.css 1741 行里 689 行（39.6%）是已删除组件的死样式——8 轮未清理的重设计沉积**

- 证据：dashboard.css 共 1742 行 / 411 规则。`.dashboard__phase-step` 作为顶层选择器出现 101 次、`.dashboard__phase-timeline` 19 次，二者相关规则块占据 689 行；但全仓 tsx/ts 中 `phase-timeline`/`phase-step` 命中数为 0（Dashboard.tsx:182-189 现在只用 .dashboard__phase-launch/__phase-button/__phase-icon）。文件内留有 22 条版本注释，对同一个阶段时间轴连续重写 8 次：:303 V3、:368 V3.2、:437 V3.3、:566 V3.4、:655 V3.5、:756 V3.6、:931 V3.6.1、:939 V3.7、:1053 V3.7.1、:1093 V3.7.2、:1101 V3.8「status card instead of decorative timeline」、:1251 V3.9、:1407 V3.9.1「reset old phase-card grid areas that caused text
- 后果：「dashboard 为什么 1741 行」的答案是：功能并不多（真实存活选择器约 39 个），而是每次改版都用追加覆盖而非替换，把 V3→V3.11 八套阶段条的样式全留在文件里。注释 V3.9.1/V3.9.2 自己承认前一轮遗留规则造成「文字重叠」和「padding 被清零」——这就是排版混乱的直接肇事者。后来者读文件时无法判断哪段生效，只能继续在文件末尾加 !important，缺陷复利增长。

**🔴 high · 一致性｜CSS 全局非模块化 + 特性层反向覆盖共享组件：.ui-button 被 11 个 feature 用 51 条规则改写，胜出者取决于导航路径**

- 证据：23 处 `import './x.css'`（main.tsx:3、app-frame.css、12 个 feature css、ui.css 被 6 个组件各 import 一次），无 CSS Modules/scoped/PostCSS 作用域。跨文件同名类共 19 个，最严重的 5 个：`.ui-button` 定义于 ui.css + 11 个 feature css（ai-settings/dashboard/day-workbench/game-end/leave-workbench-notice/history/opening-script/identity-deal/night-workbench/script-library/setup），共 51 条覆盖规则，其中 night-workbench.css:724/729 直接改写 `.ui-button--ghost` 的配色、:469 把 `.ui-button` 字号改成 1rem、:533/534/537 强加 min-width 132/172/144px、dashboard.css:321 把 min-height 顶到 60px；`.shee
- 后果：同样特异度的规则由后加载者胜出，而加载顺序等于组件首次挂载顺序。说书人从「首页→夜间」和从「首页→白天→夜间」两条路径进入同一个夜间工作台，按钮的最小宽度、字号、幽灵按钮配色可能不同——底部「确认本项」的宽度和换行都会变。这类只在特定操作路径下复现的排版抖动，本地几乎无法稳定重现，也是「共享组件层形同虚设」的根本机制。

**🔴 high · 排版布局｜缺少 Card/Panel 布局原语：165 个规则块各自手写同一套「1px 边框 + 圆角 + 表面色」卡片配方，并派生出 4 套互不相干的卡片体系**

- 证据：同时含 `border: 1px` + `border-radius` + `background` 的规则块统计：setup.css 40、game-end.css 27、night-workbench.css 25、day-workbench.css 20、dashboard.css 19、script-library.css 9、history.css 7、ai-settings.css 6、ui.css 6、identity-deal.css 5、ui-lab/opening-script/session-rail 各 1，合计 165 块。命名上并存 4 套卡片：`.dashboard-card`(+__header/--players/--recent/--end/--setup，dashboard.css 7 处根定义)、`.ai-settings-card`(+__heading/--form/--test/--assets/--archive，ai-settings.css 11 处)、`.day-card`(+__heading/--vote/--standing，day-workbench.css 2 
- 后果：卡片和区块标题是这个产品密度最高的两种容器，却没有任何单一真相来源，于是每个页面的内边距、圆角、边框亮度、标题字重/字号都在漂移（叠加上面的未定义 token，同一「卡片」在不同页 padding 从 0 到 40px 都有）。说书人在夜间/白天/结算之间高频切换，视觉节奏每换一页就重置一次，无法形成肌肉记忆；这也是 6348 行 CSS 中最大的一块纯重复，直接解释了各 feature 文件的体量。

**🔴 high · 排版布局｜容器查询上下文泄漏：ai-settings 用 4 条无名 @container + 11 处 cqw 却无自有容器；连 --type-page-title 这个 token 本身都是 cqw 定义的，同一 token 在不同页渲染成不同字号**

- 证据：全仓 `container-type`/`container` 声明仅 8 处：app-frame.css:4（`container: app-frame / inline-size`）、night-workbench.css:2（workbench）、:189（night-content）、dashboard.css:6、game-end.css:18、identity-deal.css:18、day-workbench.css:1、:157。而使用 cqw 但自身文件内无任何容器声明的文件有 8 个：ai-settings.css（11 处 cqw + ai-settings.css:201/356/557/593 四条无名 @container 查询）、setup.css（9 处 cqw，且 setup.css 全部 10 条响应式用的是 @media，见 :198/209/210/211/321/557/677/682/947/976）、script-library.css（2）、opening-script.css（1）、history.css（1）、leave-workbench-notice.css（1）、ui
- 后果：AISettingsSheet 面板固定 `width: min(980px, 100%)`，但它的 `@container (max-width: 720px)` 量的是整个 app-frame：在 1200px 平板上面板其实只有 980px 宽却永远走「宽屏」分支，而在带侧栏（app-frame ≥1024px 时出现 216/264px 侧栏）的布局下判断又会再偏一次。更糟的是 `--type-page-title` 用 cqw 定义，导致「同一个页标题 token」在 dashboard(1040px 容器)、night(1080px)、game-end(1180px)、无容器页(整窗) 上算出四种字号——设计系统里唯一那个响应式字号 token 是不确定的。这让「同一层级标题在不同页面大小不一」成为系统性现象而非个例。

**🔴 high · 交互流程｜StickyActionBar 采用率 1/32：至少 7 个底部确认区各自手搓，其中 2 个复制了 safe-area/backdrop 逻辑但参数不同**

- 证据：共享组件 import 文件数：Button 32、StatusBadge 18、Sheet 17、SeatButton 6、RoleDisc 5、**StickyActionBar 1**（唯一使用者是 night-workbench/components/NightActionBar.tsx）。手搓底栏：history.css:52 `.timeline-correction__actions{position:sticky;bottom:0;...background:rgb(13 19 26 / 96%);backdrop-filter:blur(12px)}`（背景色与 ui.css:166 的 `rgb(9 13 18 / 96%)`、模糊 14px 均不一致）、night-workbench.css:523 `.role-change-actions{position:sticky;bottom:0;box-shadow:0 -12px 20px var(--surface-panel)}`（无 safe-area 处理）、`.night-close-footer`(7 处规则)、`.preview-actio
- 后果：底部操作栏承载的是全产品最关键的不可逆动作（确认本项 / 确认更换角色 / 确认结束本局），设计文档 ui-design-system.md:78 明确禁止「覆盖内容的假吸底栏」。7 套实现意味着 7 套不同的 safe-area 内边距、7 种 z-index 和 7 种键盘弹起行为：night-workbench.css:523 的换角确认栏没有 env(safe-area-inset-bottom)，在带 Home 指示条的手机上确认按钮会被系统条压住。线下主持时说书人常单手持机、在昏暗环境快速点确认，底栏位置每页不同 + 偶发被遮挡，会直接造成误点或漏确认，进而污染牌局记录。

**🟡 medium · 视觉层级｜字号层级没有约束力：34 个硬编码字号并存，font 简写有 74% 绕过 type token（文档只允许 5 档）**

- 证据：ui-design-system.md:12 规定「字号层级只使用 meta/label/body/section/page-title 五档 Token」。实测：`font-size:` 声明 291 条，其中走 `var(--type-*)` 的 208 条（71%）；`font:` 简写声明 105 条，走 token 的仅 27 条（26%）——即 78 条简写完全绕过层级。硬编码取值去重后共 34 种，包括 .61/.62/.66/.68/.7/.72/.74/.75/.76/.78/.8/.82/.83/.84/.86/.875/.9/.95/1/1.02/1.03/1.04/1.05/1.06/1.08/1.1/1.12/1.15/1.18/1.2/1.25/1.35/1.5/2rem。其中 .61rem≈9.8px、.62rem≈9.9px、.66rem≈10.6px、.68rem≈10.9px、.7rem≈11.2px 共 12 处低于文档规定的 12px meta 下限。典型：ai-settings.css:272 `font: 850 1.18rem/1.2`、:289 `1.12rem`、:395 `
- 后果：「五档」在文档里，但代码里是 34 档且没有任何 lint 拦截，所以每个新页面都在 token 之间自由插值——结果是同为「区块标题」的文字在 setup、game-end、ai-settings 上分别是 1.08/1.1/1.18rem，层级差异小到无法一眼区分主次。线下昏暗环境 + 说书人 30cm 外快速扫读时，10px 级别的辅助文字（.61-.7rem 那 12 处）基本不可读，而它们承载的常是座位状态和记录时间等真正要看的信息。

**🟡 medium · 视觉层级｜tokens 缺 elevation 层级：73 条 box-shadow 只有 7 条走 token，37 种互不相同的阴影值，唯一的 --shadow-panel 从未被使用**

- 证据：全仓 `box-shadow:` 73 条，含 `var(--shadow-*)` 的仅 7 条（9.6%）；去重后 37 种不同阴影值。自造阴影分布：dashboard.css 45 处、night-workbench.css 9、ui.css 5（连共享组件层自己都在硬写，如 :24 `0 10px 28px rgb(224 177 93 / 18%)`、:89 `0 4px 14px rgb(0 0 0 / 24%)`、:93 `0 0 18px rgb(224 177 93 / 14%), 0 7px 22px rgb(0 0 0 / 28%)`、:130 `-24px 0 90px rgb(0 0 0 / 48%)`）、identity-deal 3、day-workbench 2、setup 1、game-end 1。tokens.css 只提供 `--shadow-sm`/`--shadow-panel` 两级，且 `--shadow-panel` 全仓 0 引用。相关地，rgb()/rgba() 硬编码在 dashboard.css 出现 231 次、night-workbench.css 102、game-
- 后果：没有 elevation 刻度就没有「哪个面在上」的共识：37 种阴影意味着卡片、浮层、吸底栏、当前角色 Token 的层级关系在不同页面互相矛盾（dashboard 的卡片阴影可能比 night 的浮层还重）。说书人需要靠深度快速判断「哪块是当前要操作的」，深度信号一乱，就只能靠读文字定位，现场节奏被拖慢。231 次硬编码 rgb 还使深色主题的整体明度无法一次性调校。

**🟡 medium · 排版布局｜tokens 缺 layout/grid 维度：55 处各自发明内容宽度（26 种不同值），188 条 grid-template-columns 用了 66 种取值**

- 证据：`width/max-width: min(Npx, ...)` 共 55 处，去重 26 种宽度：164/220/260/320/360/420/440/480/520/620/680/720/780/820/840/860/880/900/920/980/1040/1060/1080/1120/1180/1440px。页面级主容器就有 7 种互不相同：dashboard.css:2 `min(1040px,100%)`、:1676 `min(1440px,100%)`、night-workbench.css:48/:189/:538 `min(1080px, calc(100% - clamp(...)))`、setup.css:2/:171 `min(1120px,100%)`、:342 `min(1120px, calc(100% - 48px))`、:562 `min(100% - 28px, 1120px)`（同一文件三种写法）、game-end.css:12 `min(1180px,100%)`、:757 `min(1180px, calc(100% - var(--game-end-page-gutter))
- 后果：没有 PageShell/内容宽度 token，页面之间的左右边界永远对不齐：从首页(1040/1440px)切到配板(1120px)再切到结算(1180px)，正文起始 x 坐标每次都跳。dashboard.css:1674 的注释「keep all primary blocks on one width grid」和 :1699「gear floats; main stage keeps the same left/right edge」证明团队为对齐问题反复返工，但每次只在单页内打补丁。线下主持时页面切换极频繁，边界跳动会让人反复重新定位视线焦点。

**🟡 medium · 排版布局｜响应式策略双轨且断点无 token：16 个不同断点、70 条查询，同一 feature 内 @media 与 @container 混用**

- 证据：`@media` 20 条（其中 2 条是 prefers-reduced-motion）+ `@container` 50 条 = 68 条尺寸相关查询，断点并集 16 个：420/460/461/500/520/560/620/621/640/720/760/860/900/1024/1180/1280px。tokens.css 未定义任何断点变量。宽度断点使用频次高度分散：620px 出现 11 次、760px 11 次、520px 10 次、560px 9 次、720px 7 次、460px 6 次、860px 5 次、420px 5 次。策略双轨：setup.css 全部用 @media（:198/209/210/211/321/557/677/682/947/976，10 条）却同时用 9 处 cqw 单位；ai-settings.css 全部用 @container（:201/356/557/593）却无自有容器；night-workbench 同时存在 `@container workbench (max-width:520/640/860)`、`@container night-content (max-w
- 后果：同一块屏幕上，父容器按 @container(620) 折行、子元素按 @media(560) 折行，两者在 560-620px 区间冲突，产生「卡片已经变窄但内部按钮还按宽屏排」的错位——这正是 620/621 与 460/461 这种 +1px 补丁出现的原因（补丁本身证明有人被这个问题咬过）。平板横竖屏切换、手机小窗、以及带侧栏(1024/1280 断点)的桌面视图下，说书人会遇到布局在某些宽度区间莫名断裂，且这些区间无法穷举测试。

**🟡 medium · 一致性｜完全没有表单原语：input/select/textarea 的同一套样式在 9 个 feature css 中重复手写**

- 证据：`src/components/ui/` 下无 Input/Select/Textarea/Field 任何组件（仅 Button/RoleDisc/SeatButton/Sheet/StatusBadge/StickyActionBar）。原生控件被各 feature 直接选择：day-workbench.css 8 处、setup.css 7、night-workbench.css 7、ai-settings.css 3、game-end.css 3、history.css 3、opening-script.css 2、dashboard.css 1、identity-deal.css 1。重复的配方几乎逐字相同但参数漂移：setup.css:16 `select{min-height:var(--touch-min);padding:0 var(--space-12)}`、setup.css:27 `input,select{min-height:40px}`（低于 44px 触控下限）、setup.css:193 `input{min-height:52px;padding:0 var(--space-14)}`、s
- 后果：setup.css:27 的 40px 座位输入框违反了 tokens 里 44px 的 --touch-min，而配座位恰恰是开局时最密集的输入操作——线下 12 人局说书人要连续点 12 次昵称/角色下拉，40px 目标在昏暗环境下误触率显著上升。焦点态三套实现意味着键盘/外接设备操作时有的控件有可见焦点环、有的没有，无障碍不一致。

**🟡 medium · 一致性｜StatusBadge 已存在（48 处使用）却仍有 40 个自造药丸徽标散落 10 个文件**

- 证据：`border-radius: 999px` 共 41 处，其中只有 1 处是共享组件 ui.css:32 的 `.status-badge`，其余 40 处是各 feature 自造：dashboard.css 15（`.dashboard-state-chip`、`.dashboard-state-mark`、`.dashboard-player-seat::after`、`.dashboard-player-seat__life em`、`.dashboard__phase-step small` 等）、night-workbench.css 9（`.choice-chip`、`.record-count`、`.outcome-button small`、`.carousel-current__eyebrow`、`.settlement-assist__line span`）、setup.css 4（`.setup-panel__advice-entry em`、`.setup-panel__bluff-top em`、`.setup-panel__bluff-grid small`）、script-library.c
- 后果：状态徽标是这个产品的核心语义载体（存活/死亡/中毒/醉酒/标记/AI建议/已更正），设计文档 ui-design-system.md:55 要求「每种状态同时使用图标、文字和语义颜色」。40 个自造徽标各自决定字号、内边距、圆角和配色，导致同一个「中毒」在首页状态卡、夜间轮播、结算复盘里长得不一样。说书人靠徽标形状做零阅读识别，形状不稳定就退化成必须逐字读——这是现场最不能付出的时间成本。

**🟡 medium · 一致性｜命名体系三轨混用：506 个类里 129 个非 BEM、96 个无 feature 前缀直接暴露在全局命名空间**

- 证据：顶层类名去重 506 个：含 `__`/`--` 的 BEM 风格 377 个，纯扁平 kebab 129 个。无任何 feature 前缀、可被任意页面命中的裸全局类共 96 个，全部定义在 feature css 内却随全局样式表生效，例如 `section-heading`/`section-kicker`（night-workbench）、`action-bar__primary/__secondary/__hint`、`choice-chip(s)`、`outcome-button/outcome-grid`、`carousel-current/carousel-neighbor/carousel-hit`、`wake-facts/wake-history/wake-recorder/wake-workspace`、`role-change-*`、`private-reveal*`、`privacy-curtain`、`public-timer-*`、`player-state-bar*`、`settlement-assist*`、`token-card/token-grid`、`disc-row`、`compon
- 后果：`token-card`、`component-row`、`section-heading`、`inline-warning` 这类通用名一旦被第二个 feature 无意复用，就会静默继承另一个页面的排版（结合上面「加载顺序不确定」，表现为间歇性错版）。三套状态约定意味着新增一个状态时开发者要先考古决定用哪套，实际结果是继续新增第四种写法；`.is-active` 已经在 6 个文件里各有一份定义。这让「排版混乱」从视觉问题固化成了不可维护的结构问题。

**🟡 medium · 一致性｜圆角刻度在 文档 / tokens / 代码 三层互相矛盾**

- 证据：ui-design-system.md:13 写「圆角：8 / 14 / 22，禁止随机圆角」；tokens.css:54-57 实际定义的是 sm 8 / md 12 / lg 16 / xl 22 —— 文档说的 14 在 token 里根本不存在，token 里的 12 和 16 文档也没写。代码层再加一层：不走 var(--radius) 且非 50%/999px/inherit 的硬编码圆角 19 处，取值 18px×3、24px×3、20px、26px、28px、30px×2、34px 等 8 种额外刻度。间距 token 同理——文档 :9 写「间距：4/8/12/16/24/32/40」共 7 档，tokens.css 实际给了 12 档（多出 6/10/14/18/20），而代码又引用了 11 个不存在的档位（见「未定义 token」条）。触控规格文档 :10 写「最小 44px，主按钮建议 52px」，tokens 只有 `--touch-min:44px`，52px/60px 全靠硬写（ui.css:52 seat-button 52px、night-workbench.css:469 与 dashbo
- 后果：设计文档不是可执行契约，token 不是唯一真相，代码是第三套现实——三者没有任何一处 lint/测试对齐。评审时拿文档对不上代码，改 token 又改不动硬编码，于是每次「统一一下圆角/间距」的努力都只能覆盖 1/3 的现场。这是所有其他问题得以长期存活的元原因：系统里没有任何机制能让违规被发现。

**⚪ low · 一致性｜39 处 !important 构成覆盖军备竞赛，且集中在死代码区**

- 证据：`!important` 全仓 43 处：dashboard.css 39、ai-settings.css 2、setup.css 2。dashboard.css 中 39 处的行号为 589/590/606/607/687/688/722/725/788/789/817/818/933/934/935/965/966/1002/1004/1007/1017/1020/1409/1417/1418/1423/1424/1425/1428/1431/1434/1440/1444/1448/1458/1459/1462/1687/1688——其中 36 处落在 `.dashboard__phase-*` 死区（行 589-1462），配套注释为 :931「V3.6.1 fix: remove legacy current-card fill」、:1407「V3.9.1 reset old phase-card grid areas that caused text overlap」、:1438「V3.9.2 final spacing: old responsive rules zeroed the current card p
- 后果：!important 的分布本身就是「上一轮样式没删干净」的化石记录：团队不是在写新样式，而是在用 !important 压制自己三轮之前写的样式。这些声明现在压制的是根本不会渲染的选择器，属于纯负债；但它建立的先例（遇到覆盖不了就加 !important）会被下一个改 dashboard 的人继承，让层叠彻底不可推理。

**⚪ low · 可发现性｜ui-lab 视觉基准只覆盖 6 个原子，卡片/页壳/表单/空态/底栏没有任何回归面**

- 证据：UiLab.tsx 共 5 个 section：01 语义颜色与状态(StatusBadge)、02 动作层级(Button 5 变体)、03 角色 Token(RoleDisc)、04 座位号码卡(SeatButton)、05 独立展开夜序(Sheet)。ui-lab.css 仅 22 行 / 22 规则。未被 ui-lab 覆盖的高频模式：Card/Panel（165 个实现）、页面壳与内容宽度（55 处）、区块标题（80 处 font-display）、表单控件（9 个文件）、空态（7 套）、底部操作栏（7 套，StickyActionBar 本身也不在 ui-lab 里）、chip/pill（40 个）。ui-design-system.md:37-41 的「业务组件」清单（NightPlayerCarousel/NightQueueList/CurrentWakeCard/GameRecordList/RoleChangeSheet）也没有任何一个进入 ui-lab。
- 后果：ui-lab 是团队唯一能一眼比对「同类东西长得一样吗」的地方，而它恰好只收录了已经被抽象好的那 6 个。真正在漂移的卡片、标题、底栏、空态没有并排对比页，漂移就无法被肉眼发现，只能等说书人在现场遇到错版才反馈。抽象缺口与观测缺口重叠，是问题自我延续的闭环。

**⚪ low · 一致性｜CSS 书写密度从 1.0 到 7.2 行/规则，行数指标失真且 diff 噪声大**

- 证据：各文件平均行/规则：identity-deal 7.17（373 行/52 规则）、ai-settings 6.35（597/94）、game-end 6.25（1045/167）、opening-script 6.11、dashboard 4.23（1741/411）、setup 3.56（980/275）、ui.css 2.85（180/63）、night-workbench 2.21（772/348）、day-workbench 1.86（270/145）、session-rail 1.60、script-library 1.05（79/75）、history 1.03（59/57）、ui-lab 1.00。即 night-workbench 用 772 行装了 348 条规则，而 game-end 用 1045 行只装了 167 条——按规则数排序时 night-workbench(348) 其实比 game-end(167) 复杂一倍以上。仓内无 stylelint/prettier 对 CSS 的格式约定（无 .stylelintrc）。
- 后果：「dashboard 1741 行 / game-end 1045 行」这组数字会误导优先级判断：真正的复杂度排序是 dashboard 411 > night-workbench 348 > setup 275 > game-end 167 条规则。同时单行超长规则（script-library/history 几乎每条规则压在一行内，最长超 190 字符）在 code review 里无法逐属性 diff，改一个 padding 会显示成整行重写，样式回归很难在评审阶段被拦下。

**⚪ low · 一致性｜4 个 token 定义即死代码，暴露 token 层未经使用验证**

- 证据：tokens.css 48 个变量中，全仓 0 引用的有 4 个：`--input-bg`(:16)、`--shadow-panel`(:60)、`--text-placeholder`(:21)、`--type-action`(:7)。其中 `--type-action: 0.9375rem` 与 `--type-body` 取值完全相同却另起一名；`--shadow-panel` 的缺席对应上面 37 种自造阴影；`--input-bg`/`--text-placeholder` 的缺席对应「无表单原语、9 个文件各写输入框」。反向看，代码需要但 token 未提供的维度包括：elevation 分级、内容宽度、网格列数、断点、z-index（ui.css 里硬写 12/50/51/60/61，night-workbench.css:465 硬写 101）、动效时长（文档 :14 规定 150-260ms，实际硬写 160/180/220ms）。
- 后果：token 层是「设计者以为需要什么」而非「页面实际需要什么」的产物：它提供了没人用的 4 个，却缺了各页被迫自造 165 次卡片、55 次宽度、73 次阴影所需要的那几类。只要 token 的覆盖面继续与真实需求错位，各 feature 就会继续绕过它自行发明——这是「排版混乱」在 token 层面的结构性成因。
